# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
变更分类参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)：
**新增 / 变更 / 修复 / 安全 / 文档 / 已验证 / 已知边界**。

> **证据标记约定**（与工作区 `AGENTS.md` 一致）：
> `documented` = 官方文档写了；`observed` = 本机实测过；`pending` = 还没在真机验过。
> 凡写「实测」的，都能在仓库的 `tests/` 或工作区的 `tools/` 里找到对应的探针与断言。

---

## [未发布]

> 下一个版本的东西写这里。（`0.2.0` 已发 —— 它下面那一整段就是这一版的正文。）

### 新增

- **★ `tools/lint.mjs` + `npm run lint`（语言层绊线，已接进 `npm test`）** —— 作者要求「源码优化一下 需要健壮
  能复用就复用 找找有没有专门这插件的优化语言的思路」。**它上线第一天就抓到一条真 bug**：
  `miliastra_sim` 的 `parameters` 里有**两个 `all`**（一个给 `op=keys`、一个给 `op=cases action=remove`）——
  JS 对象字面量里**后者静默覆盖前者**，于是「`op=keys all:true` 拿全量键名」这条说明**从来没有到达过 AI**。
  测试全绿、功能"能用"，只有 `no-dupe-keys` 看得见它。
  设计取舍：**只开"能过的硬规则"**（20 条：`no-undef` / `no-dupe-keys` / `no-unreachable` / `use-isnan` /
  `valid-typeof` / `no-new-func` …），风格类一律不开 —— 绊线的价值在于**长期是绿的**，一旦允许几十条风格警告就会被无视。
  配置**写在代码里**（不落地 `eslint.config.mjs`）：一份真身，少一个会和代码走散的文件，顺带避开
  "配置文件在 A 目录、被扫文件在 B 目录"时 ESLint 直接拒扫的坑（实测踩过）。`eslint` 是 **devDependency**，不进用户运行时。
- **★ `lib/fsx.mjs`：写盘只有**一个**实现（原子写从 `codefile.mjs` 搬来这里，`codefile` 转发保持兼容）** ——
  源码体检（同一轮）发现当时的状况是**三种写法并存**：`codefile` 走硬化路径（同目录 tmp + **`fsync`** + rename + 失败清理），
  `sim.mjs` 自己**手搓** tmp+rename（没 fsync、失败会把 `.tmp<pid>` 漏在盘上），截图与导出更是**裸 `writeFileSync`**。
  现在 `atomicWriteFile` / `atomicWriteJson` / `prettyJson` 一处实现、多处复用，`sim.mjs` 里
  **裸写 0 处、手搓 rename 0 处**（源码绊线钉住）。

### 新增

- **★ `op=controls {runtime:true, geom:true}`：一次给出"屏幕上有哪些控件、在哪、上面什么字"** ——
  补齐 `op=hud` 只解决"字"的那一半：AI 要**点**东西时还需要坐标。每行加 `x/y`（**世界坐标**，左下原点，可直接喂
  `play action=pointer/click`）+ `w/h` + `text`；底层是**一次** `get{inspect:true, view:true}`（引擎里这两段是独立填充的，
  能同时给），但**摊平后只回紧凑行**（实测回执 1.4 KB，而那次底层响应是 **75 KB**）。
  ⚠️ 顺手挖出一个**真事实**：**场景 ≠ 控件树** —— `scene` 只含**会被渲染**的控件（工厂默认工程 `tree` 11 行 / `scene` 5 个节点；
  真实关卡 31/31）。所以回执如实报 `geomCovered` / `geomMissing`，并在 `geomNote` 里直说
  「没坐标**不是不存在**，只是现在不在画面上」——否则 AI 会把"不可见"误判成"没建出来"。
- **★ `op=hud`：只回"画面上的字"**（`textbox.text` + **世界坐标**）—— "边玩边判断"的最短路径。
  实测（真机《冰镜·火烛》在跑的那一局）：`get{view:true}` 的快照 **30 177 B / 31 个节点**，
  而 `hudTexts()` 只挑出 **2 行**（`第1关 教学 (1/3) 分数 0 …` / `方向键 / A D 移动…`）⇒ 回执 ≈300 B，**省掉约 100×**。
  坐标沿 `parent` 链累加（局部 `ty=374` + 容器 `450` = 世界 `824`），AI 拿着就能点。没有会话时**明确报错**（不静默回空）。
- **★ `op=keys` 加 `all:true`（全量 164 个键名）与 `press`（可照抄的请求形状）** —— 针对作者的原话
  **「你看找个按键这么久 这就是优化的空间」**：
  ① `all:true` 直接从引擎的 `buildEnumTree()` 拿**正源**（不抄表，抄一份就会过期）；
  ② `press` 给出 `key` / `pointer` / `click` / `step` / `hud` 的**请求体示例** ——
  以前 AI 为了知道 `play` 的 `key` 怎么发，要翻 `session.js` → `browser-session.js` → `controller.js` → `worker.js`
  **4 个源文件**；现在一次调用就说全（`smoke` 加绊线钉住这两件事）。
- **★ `op=keys` 改成"两路并扫"键名**（2026-09-24 实测暴露的漏检，见「问题修复」）：
  `KeyEventType.X` / `KeyEventType["X"]` **之外**，再扫**裸字符串键名** —— 也就是真脚本的写法
  `try(bindHold("KeyboardMoveRightKeyDown", "KeyboardMoveRightKeyUp", …))`。
  回执新增 `found[{name,via}]`（`via` = `enum-member` / `enum-index` / `string-literal`，**AI 据此判断可不可信**）、
  `byVia` 计数、`releaseNote`（提醒 `…Down` 要配对 `…Up`）。扫不出时不再只说"没扫到"，而是**告诉你去试发哪个候选键**。
  配套 `stripLuaComments()`：**认字符串**的注释剥离器（字符串里出现 `--` 不会被当注释截断），
  所以「注释掉的绑定」不会变成假阳性。真身验证：`双相.lua` 的扫描结果 **0 → 25 个键名**，无假阳性。
- **★ schema 补上「AI 自己"玩"」的口径**（作者问「你能在模拟器中游玩吗」，于是**当场量了一遍**）：
  发一次输入 ≈ **5ms**（纯注入、不回快照）、读一次 `get{view:true}` ≈ **200ms**、出 PNG 秒级 ⇒ **发得快、看得慢**；
  所以「回合制闭环」（`pause`+`step`）与「一次调用里跑实时循环」都可行，**"逐帧看画面再反应"做不到**（带宽上限）。
  同时写进 schema 的还有三条 AI 猜不出来的事实：① `get{view:true}` 的 **`textbox` 节点带 `text`**
  —— 不用截屏就能读 HUD/分数/关卡，可当闭环条件；② ⚠️ **`frame` 不是秒表**（实测静置 30fps、连续注入期间 **41.7/s**，
  注入会顺带推帧，计时用 `time`）；③ ⚠️ **按了 `…Down` 必须配对 `…Up`**，否则等于一直按住
  （实测把小人一路推到掉出边界重生）。`smoke` 里加了一条绊线钉住这几段。

### 修复

- **★★ `miliastra_sim` 的 schema 里有**两个 `all`** —— 后一个把前一个的说明**整个吃掉**（`no-dupe-keys` 抓到的真 bug）**：
  AI 读到的 schema 里 `all` 只被描述成「`op=cases action=remove` 删整组」，而「`op=keys all:true` 拿全量键名」
  那条**从未到达过 AI**（同一个键在对象字面量里出现两次，后者胜）。现在合成**一个** `all`，两种用法都写在同一个说明里，
  并在源码里留注释说明**为什么不能写成两个键**。
- **★ `lib/client.js` 的 `var s29` 重复声明（`no-redeclare` 抓到的维护地雷）**：第 692 行它是 `uiInfo` 的 state、
  第 707 行**又声明一次**当 `panelTab` 用 —— `var` 复用同一个绑定，当前靠"先读后写"侥幸正确，
  但只要有人调换两段顺序，`uiInfo` 就会**静默变成** panelTab。改名成 `panelTabState` 并留注释。
- **★ `sim.mjs` 的写盘三处各写各的**（见「新增」的 `lib/fsx.mjs`）：手搓 tmp+rename 没有 `fsync`、失败会漏 `.tmp<pid>`；
  截图与导出是裸写（被杀进程可能留下半截 PNG）。现在统一走 `atomicWriteFile`。
- **★★ `op=keys` 把"为什么没扫到"说反了（真机实测）**：游戏日志写着
  `[yuan-code] 收到首个按键事件: KeyboardMoveRightKeyDown（来源=KeyEventType）`，而工具回「脚本源码里没出现 `KeyEventType`」
  —— 其实**出现了**（`local candidates = { "KeyEventType", … }`），只是按键名是裸字符串、旧的单路正则匹配不到。
  后果不是"少一个功能"，而是 **AI 只能去猜键名**，而键名猜错是**静默失败**。
  现在三种情况分开说（扫到了 / 有 `KeyEventType` 但不是我认的写法 / 真的没有），并且都指明下一步怎么**试**。

### 文档

- `docs/模拟器与视图.md` 新增 **§4.14「AI 自己玩到底能到什么程度」**：把上面那些**实测数字**（4.6ms / 216ms / 30fps / 41.7fps /
  0.17% 像素差 / Δx=138.7）、三种玩法与代价、两条只有实测才知道的坑一次性摆齐（含"逐帧看画面做不到"这条负结论）。
- README 想法池：把**已经落地的**两条打勾（浏览器试玩页内嵌进面板、`op=keys` 漏检修复），
  并新增一条待办：**`op=play` 的"实时循环"封装**（真实时钟 + 条件分支 + 回 trace）—— 今天是 AI 自己手写 pwsh 循环才跑起来的，
  这个动作值得变成工具的一个 op。

### 已验证

- `npm test` 退出码 **0** = **637 项**：**先跑 `node tools/lint.mjs`**（46 个文件 / 20 条硬规则 / 0 命中），
  再 `readme` 14 · `smoke` 55 · `deploy` 33 · `probe-deploy` 16 · `lualint` 32 · `shot` 104 · `sim` **167** ·
  `sim-play` 41 · `client-render` 45 · 引擎 130。
- **语言层绊线的战果（不是"装了个工具"，是它抓到了什么）**：
  `no-dupe-keys` → `miliastra_sim` 两个 `all` 静默覆盖（真 bug，AI 侧少一条说明）；
  `no-redeclare` → `client.js` 的 `var s29` 重复声明（维护地雷）；
  `no-new-func` → 把读 `client.js` 的两处无头渲染标成**有理由的例外**（`eslint-disable-next-line` + 原因）。
- **复用验收**：`sim.mjs` 里 `fs.writeFileSync` **0 处**、`fs.renameSync` **0 处**（源码绊线断言）；
  `atomicWriteFile` 的失败路径（写到目录上）**抛错 + 清 tmp + 旧文件完好**有断言；
  字符串入参 + **无 BOM** 有断言（原神遇到 BOM 会报 Lua error）。
- **真机验证两条新 op 的提取逻辑**（新 op 本身要重启 Host 才生效，所以拿**在跑那一局的真快照**喂新纯函数）：
  · `hudTexts`：30 177 B / 31 节点 → 2 行文字，坐标 `(800,866)` / `(800,824)` 与手工算法一致（局部 `ty=374` + 容器 `450`）。
  · `sceneNodes`：31 行全部换算成世界坐标（28 个图片来源 x `115~1440` / y `27~842.4` 正好铺满 1600×900）。
- 真身验证（不是构造的样例）：用新扫描器读**真正的** `双相.lua`（42 947 B）⇒ 25 个键名，含 `KeyboardMoveRightKeyDown`/`…Up`、
  `KeyboardJumpKeyDown`（**只有 Down 没配 Up** —— 跳跃是一次性触发，与 HUD 上「跳跃=切相」一致）、手柄键 `Controller*`。

---

## [0.2.0] — 2026-09-24

> `0.1.0` 之后**融合分支继续推进**的产出。作者原话：「融合分支继续推进，写好每次的提交记录」。
> 目标（作者原话）：「**我目标是让 ai 更好使用这个插件**」—— 所以 W2 两条腿走路：人侧的浏览器试玩页，**和它给 AI 的那条路**。

### 新增

- **★ `op=bind`：把真机那份工程搬进模拟器（一条命令）** —— 原来"让双相在模拟器里跑起来"是**手写探针**
  （建模板 → 存盘 → 手改 JSON 里的 guid → 读回 → 挂脚本 → 起会话）；现在给
  `source`（活文件绝对路径）+ `templates:[{guid,kind,name?}]`（**创作者交接的控件模板索引**）+ `containerId` 就够了。
  两个关键点各有一条断言钉住：① **模板 guid 必须用交接值**（引擎另编一个号 ⇒ `InstantiateClientUIControl` **静默什么都不建**）
  —— 为此引擎 `addTemplate` 支持显式 `guid`（带合法性与重号校验）；② **起完会话要先走 `settleSec`（默认 0.5s）再读控件数**
  —— 脚本的构建发生在进入 RUNNING 之后，停在 frame 0 只读到 1（容器自己），会被误读成"什么都没建"（实测踩到）。
  默认 `fresh:true`（重置两个资产、清掉已有脚本与出厂橱窗控件 ⇒ 同一份参数 → 同一份工程）、`run:true`（顺手起一次会话，
  回 `run.logs` + `run.controlCount`）。真机实测（双相，370ms）：`就绪（3 关，控件 31…）`、`controlCount=32`。
- **★ `op=cases`：验收单（人/AI 读同一份）** —— 存进模拟器工作区的 `cases.json`：
  `add`（`set` + `cases[]`；**同名覆盖**；加 `fromHistory:true` 就把**刚跑过那一局**变成用例）/
  `list`（只给计数与 kind，省 token）/ `show` / `run`（自动项确定性重放，**人工项不代跑只列出**）/ `remove`（默认 dryRun，要 `confirm:true`）。
  **人工项** `{manual:true, note:"人要看什么、看到什么算过"}`：工具**不代跑也不代判**，`run` 的 `manual[]` 等人打勾，
  回执直说「自动项已跑完 —— 但**整组没算过**：还有 N 条人工项」。`op=verify caseSet=<名字>` 是同一份入口。
- **浏览器试玩页（W2）：`GET /miliastra/play`** —— PixiJS（WebGL）真能玩的一页，工具栏（开始/暂停/单步/重开/结束 + 设备·人数·视角）+ 侧栏（状态 / 服务端变量 / 日志 / **这一局的操作时间线**）。
  复用引擎自带的 `PixiPlayRenderer` + `createPlaySession`（与上游 DSH 插件/Web **同一条浏览器循环**），数据走 `POST /miliastra/engine` ——
  **与面板、与 AI 工具共用同一个会话与同一份工程**。面板「模拟器」页加了入口。
- **esbuild 构建脚本 `tools/build-sim-play.mjs`**：打成单个 ESM（552 KB，含 pixi）**入库**在 `lib/sim-play/dist/play-renderer.js`（装插件的人不做构建也能用）；
  `--check` 现打一份比字节，`prepublishOnly` 也跑一遍，防止"改了源码忘重打包 → 页面跑旧引擎且不报错"。
- **★ `op=verify fromHistory:true`：人玩的那一局直接变成回归用例** —— 人在试玩页里报「刚才这么点就错了」时，
  AI 不用手抄 events，直接拿那一局的真实操作序列确定性重放 + 判定。这是浏览器页**对 AI**（而不只是对人）的价值所在；
  因此页面**关掉时刻意不停局**（否则 history 归零，这条路就断了）。

### 变更

- **试玩页页脚改成"自我诊断"**（作者说「下面这块删除试试」之后的处理）：原来那行装饰文案
  （"画面由 PixiJS（WebGL）渲染 · 逻辑在与面板/AI 同一个 Worker 里跑"）**删掉**，换成两组**数字**：
  · `窗 <iframe高> / main <页面中间那行高> / 台 <舞台宽×高>` —— **一眼分清是哪一层被压**：
  窗小 = 面板没给够高度；窗正常但 main 小 = 页面行没拉满；main 正常但台小 = 舞台没填满 main。
  · `v<大小>-<mtime>` **页面版本戳**（Host 在 `/miliastra/play` 响应里盖）：跟磁盘一比就知道
  "面板里那份是新是旧"——作者已经因为"改了没生效"来回猜过两轮，这条把它变成可读的数字。
  ⚠️ 戳由 **Host 侧**注入 ⇒ 没重启 `dsh web` 时页脚会显示字面量 `v__PLAY_STAMP__`（这本身也是信号）。
  页脚只有 26px 高 —— 它**不可能**是画布被压小的原因（`可放 542×90` 说明是舞台那一层小）。
- **画布 / 人数 / 视角**改成**固定 PC 16:9 / 1 人 / P1**，面板与试玩页上的三个下拉**整块删掉**
  （作者原话：「我希望固定这样 把切换的功能去掉吧」）。两条理由：① 引擎的 `device` 会**重建整个运行时** ——
  画布一变，16:9 的舞台尺寸与 AI 记下的操作坐标都要重算；② 试玩页那份**手写**的设备清单里有引擎**不存在**的预设
  （`pc-4-3` / `phone-16-9` / `phone-4-3`；引擎只有 `pc-16-9`/`pc-21-9`/`mobile-16-9`/`mobile-19.5-9`/`mobile-4-3`）
  ⇒ 选中就报 `unknown canvas preset`（作者截图里那条红条）。**能力没丢**：AI 仍可用
  `miliastra_sim {"op":"play","action":"device"|"view"}` 与 `playerCount` 改；页面上只留一行**只读**回显
  （`PC 16:9 · 1 人 · P1`）。两条**反向断言**：下拉不许回来、页面上不许再出现引擎里不存在的画布预设。
- **模拟器面板按作者要求改成 1:2**：右 2 份 = **试玩页 iframe**（`src=/miliastra/play`，PixiJS WebGL，人真能在里面玩），
  左 1 份 = ① 画面与日志（缩略，横排）、② 试玩操作 + 操作时间线、③ 验收单、④ 工程与控件树 / 工程适配（折叠）。
  **AI 驱动的是同一个会话**（`op=play` 的 key/pointer/step）—— 页面每 33ms 轮询 `sceneRev` 增量，所以 AI 一动画面就跟着变；
  人玩完那一局，AI 用 `verify fromHistory:true` 直接变成回归用例。窄屏（浮层 880px）`@media(max-width:1000px)` 塌成一列。
  顺带确认 iframe 能嵌的前提：**`/miliastra/play` 不要鉴权**（`curl` 实测 `200`；`/` 才是 401），也没有 X-Frame-Options/CSP 拦。
- **模拟器面板补上两条新能力的入口**（此前按「新能力优先做 AI 侧」只落在工具里）：**工程适配**（扫描活文件 → 候选交接值 →
  勾选 + 选 kind → 一键搭进模拟器，回执显示"模板 / 脚本 / 运行时控件数 / 脚本 print"）与**验收单**（读清单 / 跑这一组 /
  从刚跑的那一局存用例 / 存人工项 / 逐条 ✅❌ 与"人工项待打勾"）。

### 新增

- **★ `op=handover`：交接值体检（别靠人抄）** —— 列出这台机器上的**活文件**（★ 标出"当前正在开发的那张图"），
  再读那份 Lua，把源码里 `local NAME = <9 位以上整数>` 的**候选交接值**摆出来（含变量名）+ 一份 `suggestedTemplates` + `containerId`。
  为什么值得单开一步：交接值**抄错一位** → 脚本静默什么都不建（不报错、日志无痕）；抽真值比让人抄一遍可靠。
  `kindHint` 只看变量名（IMAGE→image / TEXT→textbox / CONTAINER→role=container），**控件类型必须创作者确认**。
  双相实测：三个值全中（1073741866/67/68），`local LIMIT = 60` 这类小整数不会被误收。

### 修复

- **★★★ "画面咋就这么点大"的第三次根因：`#fatal` 是 `display:none` ⇒ 它不算 grid item，自动排行把 main/footer 整体上提一格**
  （作者原话：「重载会大一点 然后马上又被挤扁了」）。下面那条修的是 `main` **内部**的行，**没修 `main` 自己被放在哪一行**。
  `display:none` 的元素不生成盒子、也就不是 grid item ⇒ 自动排行把剩下三个依次塞进第 1/2/3 行：
  `main` 落到第 2 行（`auto` ⇒ 行高 = 舞台高 = **又回到"自己量自己"**）、`footer` 落到第 3 行（`minmax(0,1fr)` ⇒ 吃掉全部剩余高度），
  真正那 26px 的第 4 行没人占。**footer 实测 217px**（`窗 374 / 主 90 / 脚 217`）—— 作者截图里 footer 文字下面那一大片"空白"
  就是它，跟画面一点关系都没有。修法：`header{grid-row:1}` / `#fatal{grid-row:2}` / `main{grid-row:3}` / `footer{grid-row:4}`
  （隐藏的那行塌成 0），并给 `#fatal` 加 `max-height:40%;overflow:auto`（报错再长也不许把舞台挤没）。
  **footer 的诊断数字从 3 个加到 5 个**：`窗 · 头 · 主 · 脚 · 台` —— 上一版正好漏掉当时真正出问题的 `脚`，于是白跑一轮。
  离线复核（不用等人截图）：`chrome --headless=new --window-size=570,374 --screenshot=… http://127.0.0.1:3080/miliastra/play`，
  按列扫像素带 ⇒ 修后 `头 39 · 主 308 · 脚 25 · 台 570×308`、画布 **512×288**（修前 160×90）。
  ⚠️ 这个坑**读代码发现不了**（CSS 逐行看完全正确）—— 是**对截图按列打印色带**量出 footer 底色从 y348 铺到 y564 才定的案。
- **★★ 舞台高度被画布"自己量自己"锁成一条缝（作者两次实测：「右侧这么大的画面也太小了吧」「直接打开是这样」）**：
  footer 上一直写着 `可放 542×90` / `542×168` —— **宽度一直正常、高度一直不对**。
  根因在 CSS：`main` 只写了 `grid-template-columns`，**行是 auto** ⇒ 行高 = 内容高 = **画布的高度**，
  而画布高度又是 `fit()` 从舞台盒子量出来算的 ⇒ 某次算小之后一路变小、最后锁死（列宽由容器分配，所以列没事）。
  修法一行：`main{grid-template-rows:minmax(0,1fr)}` + `#stageWrap{height:100%;overflow:hidden}` ——
  **舞台高度由容器决定，永远不看画布**。`sim-play-test` 里钉住这两条（别让它再变成"自己量自己"）。
- **★★ 重启 `dsh web` 后模拟器工程回到「出厂默认」（作者实测：「咋画面在左下角 右侧啥都没」的第二个成因）**。
  Host 是启动快照 ⇒ 内存里的工程被重置，试玩页里只剩默认的「文本 / 预设按钮 / 五角星」，**看起来就像什么都没画**。
  现在：① 引擎 `get()` 上报 `factoryDefault`，`op=state` 直说；② **成功 `op=bind` 会记一份配方**
  （`simulator/last-bind.json`：源文件 + 交接值 + 容器索引 + 画布），重启后 `{"op":"bind","last":true}` 一键重搭；
  ③ 面板顶部出现粉色提示条 + **「一键重搭上次：《双相》」**按钮（**不自动重搭** —— 不擅自在人没要求时改工程）。
- **★ 面板左列「按钮/文字叠在一起」（作者截图）**：左列是**固定高度的 flex 列**，子项默认 `flex-shrink:1` + `min-height:0` ——
  内容一超就被**压扁并互相重叠**。现在子项一律 `flex:0 0 auto` + `min-height:auto`，滚动交给容器；
  同时左列加 **300px 宽度下限**（880px 浮层的 1/3 只有 285px，塞不下那些按钮），面板自己的按钮精简到 4 个核心动作，
  其余（连帧 / 刷新画面 / 按键 / 设备·人数·视角 / 导出 GIA / 重置工程）收进**默认折叠的「② 试玩操作（高级）」**。
- **★ 试玩页在面板里（~570px 宽）工具栏折成两三行、把舞台挤没**：加 `@media (max-width:820px)` ——
  长标题换短标题、按钮与间距收一档，工具栏努力保持一行。
- **★★ 试玩页的画布被缩成一个小方块（作者实测：「右侧这么大的画面也太小了吧」→「能不能默认固定下 16:9」）**：
  三个原因叠在一起，现在逐个修掉：
  ① **面板的窄屏断点是 1000px，而浮层面板正好 880px 宽** ⇒ 浮层里永远命中媒体查询、1:2 塌成一列，
  右列高度改由内容决定（iframe 的 `min-height`），舞台只剩 **168px** 高 —— footer 上白纸黑字写着
  `画布 263×148（可放 542×168）`。断点下移到 **760px**：880px 的浮层保持 1:2（300px 左列 + ~545px 试玩页）；
  真到窄屏塌成一列时，给 iframe `min-height:min(420px,55vh)`。
  ② **16:9 现在由 CSS 声明**：`#stage{aspect-ratio:1600/900}` + `#cv{width:100%;height:100%}`，
  JS 只挑宽度（`min(可用宽, 可用高×16/9)`）—— 布局再怎么抖，比例也不会算歪（引擎那边本来就是
  "CSS owns the scaled stage size, Pixi only owns the high-DPI backing buffer"）。
  ③ **尺寸不稳**：盒子 <200×150 先不写、下一帧再量；由每一帧快照**节流重算**（≥350ms）—— 0.35 秒内追平。
  并把数字印在 footer：`画布 523×294（16:9 固定；可放 543×332）` —— 这类问题以后肉眼可核。
- **★★ `/miliastra/engine` 把请求体里的 `op/action` 吃掉了（真事故：面板试玩按钮与浏览器试玩页双双静默失效）**。
  路由早先写成「有 `body.args` 就用 `body.args`」，而 **`op=play` 的参数本来就装在 `args` 里**
  （`{op:'play', action:'click', args:{x,y}}`）⇒ `op/action` 一起被丢，`simOp` 退化成默认的 **`op=state`**，
  而且**全程不报错**。后果：① 浏览器试玩页的 `attach()`/轮询拿到的都是"工程快照"而不是"试玩快照"，
  永远 attach 不上、也永远不 start ⇒ **iframe 一片黑、Frame 停在 0**（作者 2026-09-24 实测截图）；
  ② 面板的「开始试玩 / 按键 / 单步 / 连帧」全部白发（返回的是 state 快照）。
  现在请求体**原样**交给 `simOp`（抽成 `engineArgsFromBody`，`smoke` 里两条断言钉住：
  转换原样 + 同一形状喂给 simOp 必须进 `play` 分支；另有一条"别再把 `body.args` 拆出来"的源码绊线）。
  取证：修复前 `POST {"op":"play","action":"history"}` 返回的是 **state 快照**（有 `treeCount`、没有 `action` 字段）。
- **试玩页的报错以前看不见（同一个黑屏体验的另一半）**：`#fatal` 原来放在 `#side` 里，而 `#side` 默认 `display:none`
  （要点「侧栏」才出现）—— 于是「WebGL 起不来 / 会话没跑起来」一个字都看不到。现在报错条常驻在 header 下面（`grid-template-rows` 多一行 `auto`），
  WebGL 初始化包了 `try/catch`（拿不到就明说并建议用「新窗口」开），舞台尺寸为 0 时**下一帧重试**（不再硬套 120×80 的小方块），
  另挂 `ResizeObserver` 跟着面板缩放。三条都有测试。
- **面板：画面被挤到左下角 + 右侧试玩页被滚出视野**（作者实测反馈）：
  左列顺序改成 **① 画面与日志 → ② 试玩操作 → ③ 验收单 → ④ 工程与控件树**（最常看的在最上面），
  ④ 那块（含工程适配与控件树）默认**折叠**成 `<details>`；滚动模型改成**左列自己滚、右列不动**
  （`-simbody` 不再整页滚动 —— 否则左列一长，滚下去 iframe 就出了视野，看起来就是"右侧啥都没"）。

- **`imageType` 放开为可写（引擎，带真机证据）**：上游保守拒绝（`observed-contract.md` §14「只有部分图片支持设置」+ 一条测试钉住），
  但真机关卡 `1073741833`《冰镜·火烛》的脚本对**即时实例化**的图片写 `imageType = Enum.ImageType.Stretch` 并**真机跑通**
  （`.gia`：`就绪（3 关，控件 31…）`，无 `no such field`）⇒ 继续拒绝会变成**假阴性**（模拟器跑不起来、真机没问题）。
  改动：`scene.js` 的 `KIND_RW.image` 加 `imageType`，`runtime.test.mjs` 断言**翻转**（`img_type_set true` + 回读 `Stretch`），两处引擎文档同步。
  渲染口径：**按尺寸拉伸**（与脚本用的 `Stretch` 一致），Slice/Tile 未实现。
- **`slimPlay` 无条件丢掉 `scene`/`paint`** —— 与「`summaryOnly` 只去体积、`false` 时给全量」的约定矛盾，直接后果是浏览器页永远拿不到画面（黑屏）。
  现在 `summaryOnly:false` 原样给 `scene`，默认仍是只回计数。
- **`sanitizeLabel` 先剥尾点、后截断** → 截断处又留下一个尾点（实测文件名成了 `…-frames-t0.`）。
  改成截断后再剥一次；`op=frames` 的帧标签同时缩短为 `t<秒>`（标签上限 24 字符，写长了会把 `t0.5` 截掉）。

### 新增

- **`op=frames`：按时间出多帧 + 帧间像素差 + 字段级变化**（回答「动画到底动没动、动的是哪一块」）。
  `{"op":"frames","frames":[0,0.5,1]}` → 每个时间点一张 PNG + `changedPixels` / `changedRatio` / `maxDelta` / 变化区域 `bbox`
  + `changedControls`（**哪个控件的哪个字段**变了）。实测（容器节点 1 秒线性 Tween 右移 100）：
  `matrix.tx 800 → 850 → 900` 与像素 `bbox.x 452 → 502`（+50/帧）**互相印证**；静态工程 `identical:true` / 0 像素（负对照）；
  同一调用跑两遍数字一样（**可复现**）。
  两条实现决定：内部用「**暂停 + 单步**」推进时间（否则 worker 自己的 30FPS 时钟会让帧对不上时间点）；
  每帧回全量场景由 Host 自己比字段（**不依赖增量协议**）。上限显式：帧 ≤12、步数 ≤1200，超了报错并给出「换更大的 dt」。
  ⚠️ 踩坑记录：场景里**位置在 `matrix.tx/ty` 上**（不是 `anchoredPositionX`）—— 不摊开 `matrix` 这一层，`changedControls` 会永远是空的。
- **`op=verify` 支持拖拽（`steps[{drag:{from,to,steps?,gap?}}]`）**：自动展开成 `down → move… → up`（默认 6 段 / 每段 0.05 秒）。
  以前只能发 `click`，**拖拽/滑动这类交互根本测不了**；引擎的 `CursorBeginDrag` / `CursorDrag` / `CursorEndDrag` 都在 `move` 里发，
  所以必须展开成一串事件。另配 `pointer:{type,x,y}` 供手排裸指针事件（`down`/`move`/`up`/`click`）。
- **`op=verify` 新增 `count` 断言**（`{kind:"count", name?|controlKind?, equals|atLeast}`）：**数建了几个**。
  `tree{exists}` 只能回答「建了吗」，回答不了「建了几个」—— 而「每按一次加一条」这类动态 UI（列表项、金币图标、连击星）恰恰要问数量。
  落地时补了 `format.js` 的白名单与搬运（**归一化会丢掉未知字段**，不补就会退化成「数所有控件」）与 `runner.js` 的 `needsTree`（漏了它 count 恒为 0）。

### 文档

- **补上「模拟器在流程里的位置」= 真机试玩之前的「预测试」**（作者 2026-09-24 提问：「按我理解是预测试模式，是我想的这样吗」→「补充上吧」）：
  `docs/模拟器与视图.md` 开头新增 **§0**（流程图 + 三档表 + 三条边界），并写清 **②交互试玩 vs ③确定性判定** 的区别
  （②是"活的一局"、时间真在走；③**冻结时钟**、按时间点重放、可复现；`fromHistory` 之所以会结束那一局，就是因为它把活的一局转成了③）。
  同时也说明了「**能不能拿我真机的工程进来预测试**」目前还差最后一环节（GIA 两个方向都 `pending`，只能人试）。
- **同一份定位写进 `miliastra_sim` 的 schema 与提示段**（AI 只看得到这两条通道）：`预测试` / `模拟器通过 ≠ 真机通过` / `三档（①看 ②玩 ③判）`；
  `smoke` 加了一条**定位绊线**（少任一项就红）—— 没了这段，AI 会拿模拟器结果当"验过了"来汇报。README 的按需加载索引行同步。
- **README 顶部加「大字」目标声明 + 「AI 反馈区」**（作者要求：「在 git 主文档写上大字：目标是让 AI 更好用，希望各家 AI 积极提出意见」）：
  居中 `🎯 目标是让 AI 更好用` 横幅（中英各一份）+ 一节反馈区 —— 含**四条可执行尺子**（`smoke` 里钉住的那四条不变量）、
  **六条「什么意见最值钱」的真实例子**（每条都标了它后来变成了什么功能）、三种提法（加一行想法池 / 开 Issue / 贴真实卡住的调用与回执），
  以及**想法池**（我自己的待办，写明「可被反驳」，并留了一行给来提意见的 AI）。
- **同一份邀请也写进了系统提示段**（`PROMPT_RULES` 第 6 条）—— 因为**只有工具 schema 与提示段能自动到达 AI**，
  只写在 README 里等于没跟 AI 说过。提示段正文 1 445 → 1 850 字（仍只占工具 schema 的 9%）。

### 已验证

- `npm test` 退出码 **0** = **597 项**：`smoke` 50 → **53**（engine 路由不吞 `op` 的两条 ★）、`sim` 88 → **132**（`op=bind` 21 + `op=cases` 14 + `op=handover` 7）、
  `sim-play` 29 → **41**（报错条可见 / WebGL 兜底 / 舞台尺寸重试 + **四格显式行号**与**报错条封顶**）、`client-render` **45**（1:2 + iframe + 左列顺序与滚动模型）；其余 `readme` 14 · `deploy` 30 · `probe-deploy` 16 · `lualint` 32 · `shot` 104 · 引擎 130。
- **`op=bind` 在真机上跑通了**（双相《冰镜·火烛》，370ms 建好）：`run.logs` 出现它自己 print 的
  `就绪（3 关，控件 31，画布 1600x900）`（与真机 `.gia` 同一句），`run.controlCount=32`（容器 1 + 双相 31），
  渲染图 `~/.dsh/miliastra/shots/sim-play-双相-bind-第1关-20260924-110733.png` 里第 1 关的平台/熔岩/冰墙/小人/状态栏都在。
- 无浏览器的**协议冒烟**：`summaryOnly:false` 带 `scene`（`tree-v1`）、带 `sceneRev` 的增量轮询能接着拿、`play action=history` 拿得到时间线、`fromHistory` 跑通且没有活会话时明确报错。
- `node tools/build-sim-play.mjs --check` 能真跑（Windows 上"字符串拼 `file://`"的主模块判定会**静默不执行** —— 踩过，已钉住）。
- 真机形态的拖拽用例实测：`drag:{from:[800,450],to:[830,470],steps:3}` → 命中 `DRAG_BEGIN` / `DRAG_MOVE` / `DRAG_END`，
  且**不发** `CursorClick`（引擎只在原地 `up` 才算点击）—— 两条都写成了断言。
- ⚠️ **仍待人工**：浏览器试玩页的**观感**（打开 `http://127.0.0.1:3080/miliastra/play` 看画面/手感）——本机没有浏览器自动化，产物与数据通路有测试钉住，但这最后一米只能人看。

---

## [0.1.0] — 2026-09-24

> 承接 `0.0.11`。本版是**能力版**：把 [miliastra-beyond-simulator](https://github.com/1475505/miliastra-beyond-simulator)
> 的引擎**吸收进本仓**（58 文件 / 596 KB），在游戏之外长出「搭界面 → 跑 Lua → 出画面 → 自测逻辑」的闭环。
> 也是**许可版**：上游是 GPL-3.0-only，吸收代码后本仓同步换成 GPL-3.0-only（既有 `≤0.0.11` 仍按 Apache-2.0）。
> 本版 11 个提交 / 70 文件（+18 187 / −240）里，**AI 侧的调用体验**是主线 —— 新增 1 个工具、12 个 op，工具 schema 25 253 → 31 680 字符。

### 新增

- **`miliastra_sim`：内置千星模拟器**（引擎来源与归属见 `NOTICE`）。12 个 op：
  `state` / `patch` / `play` / **`verify`** / **`controls`** / `shot` / `keys` / `export` / `import` / `load` / `save` / `reset`；
  配套 `POST /miliastra/engine` 路由。**用户 Lua 一律跑在可终止的 Worker 里**（默认单次操作 8 秒超时后 `terminate`），
  工作区固定在插件数据目录的 `simulator/` —— 不碰游戏存档、地图与活文件。
- **`op=verify`：AI 自测逻辑的主入口**。一次调用 = **操作 + 断言 + 判定**（引擎在全新会话里**确定性重放**，所以可重复）：
  - `steps[]` 八种：`key` / `click:{x,y}`（左下原点）/ `clickName` / `setVar` / `sendSignal` / `view` / `pause` / `resume`；
  - `expect[]` 六种：`log` / `control` / `var` / `signal` / `tree` / `lua`（自写查询脚本）；
  - `at` **可省**（默认 = 最后一个事件之后 0.1 秒）⇒ **AI 不用自己算时序**；没过时回一条**人话 `hint`**（第几条、期望 vs 实际、`failedAt` / `frame`）。
- **`cases[]`：一组用例一次跑**。每个用例**各开一个全新会话**确定性重放（用例之间互不影响，所以能当回归套件）；
  默认跑完全部，`stopOnFail:true` 则第一个不过就停；**通过的用例不回日志、不落图**（不给 AI 加噪音）。
- **失败自动取证**：判定没过时顺带回 `shot`（**失败点附近**一帧 PNG —— 先把 Worker 时钟 `pause` 冻结，否则 30FPS 会继续往前走）
  与 `runtime.controlNames`（**运行时**控件名清单，编辑器工程树里没有的就是脚本动态创建的）；`shotOnFail:false` 可关。
- **`op=controls`：写断言前先看控件**。只回 `{id,name,kind,depth}` 四个字段 + `names`（可直接抄进 `expect`）+ `kinds`（类型直方图）+ `scripts`；
  `runtime:true` 看**运行中**会话的树。`state` 的每条 tree 行有 12 个字段，对「写一条断言」全是噪音 —— 实测一行 **≤ `state` 同行的 1/3**。
- **会话区三个视图**（`conversation.view`：初级功能 / 高级功能 / 模拟器）+ 面板内页面切换条；侧边栏浮层入口保留。
- **面板上「能玩」**：连帧 5fps（**固定名覆盖写**，磁盘只留一张当前帧）/ 点画布任意位置 / 按键 / 设备五档 · 人数 1–8 · 视角切换 / 单步。
- **GIA 导出与导回**（`op=export` / `op=import`，落在模拟器工作区的 `exports/`）。

### 变更

- 版本线从 `0.0.x` 升到 **`0.1.0`**：许可变更 + 引擎整体吸收 + 新工具，按语义化是 minor。
- 面板三页**各自独立平铺**，去掉原有的「全部」混合页。

### 修复

- **初级页第三列空白**：`-bodyfill` 的 `repeat(3,…)` 被后写的 `-body` 盖掉 —— 根因是 **CSS 顺序**，不是布局逻辑。
  改成复合选择器 `.‑body.‑bodyfill` 放在 `-body` 之后，并加断言钉住「顺序必须在 `-body` 之后」（读的是**注入后的真实 CSS**）。
- **切设备后人数悄悄掉回 1** → 再切视角就报 `playerIndex must be an integer 1-1`：Host 记住 `start` 时的人数、`device` 时自动沿用，错误信息也说明原因。
- **运行时的嵌套控件树被当成拍平行** → 只数顶层会把控件**少算一个数量级**（实测 **1 vs 17**）：
  新增 `flattenControls()` 先拍平再数（`op=controls runtime:true` 与失败取证都走它）。这条是**演示时抓到的**，不是推断出来的。

### 安全

- 许可 **Apache-2.0 → GPL-3.0-only**（吸收 GPL 代码的必然结果）：`LICENSE` / `NOTICE` / `package.json` / `README` 四处一致，`NOTICE` 标注上游与提交锚点。
- 本版新增的写入面只有 `miliastra_sim`（只写自己的模拟器工作区）；**截图与备份一律不自动删**，清理永远要人显式点。

### 文档

- 新增 `docs/模拟器与视图.md`（两条轴别混 / tab 契约出处 / 画面怎么来的 / 面板上怎么玩 / **AI 怎么自测逻辑** / 三条安全纪律 / 已验证 vs 未验证 / 排障）。
- 新增 `docs/需求边界清单_融合beyond-simulator_2026-09-24.md`（R↔FP 100% 追溯、六轮决策、W1–W7 工单与复测记录）。
- 新增 `docs/上游源码索引与吸收地图.md`（**全量上游源码在哪 / 搬了什么 / 没搬的读哪篇**）。

### 已验证

- `npm test` 退出码 **0**：`readme-test` 14 · `smoke` 49 · `deploy-test` 30 · `probe-deploy-test` 16 · `lualint-test` 32 · `shot-test` 103 · **`sim-test` 68** · `client-render-test` 43 · **引擎 129** = **484 项**。
- 失败帧**看图验证**（不是"接口返回 ok"就算过）：1600×900 / 124 491 B，画面上正是脚本运行时动态创建的模板面板（标题 + 图标 + 「确认」按钮）。
- 失控脚本护栏：`while true do end` 超时后**返回错误**而不是挂起，Worker 被 `terminate`，随后 `stop` 仍可用。
- 用例隔离：前一个用例按过的键**不会**漏进下一个用例（各开新会话），有专门断言钉住。

### 已知边界

- **导出的 `.gia` 能否被官方编辑器接受：未验证**。引擎侧导出→导回往返已成立，但**真机导入没有自动化通道，只能人做**。
- **模拟器通过 ≠ 真机通过**：官方素材（图片 `100001–100006` 之外显示缺失框）、真机渲染、真实网络联机都不覆盖。
- 浏览器 WebGL 试玩页（上游 `web/` + `pixi` 那条路）**未搬** —— 面板目前是 **PNG 帧**驱动；1–8 人是**本地多客户端模拟**，不含真实联机。

---

## [0.0.11] — 2026-09-24

> 承接 `0.0.10`（首个 Release，发布时 tag 停在 `7ca66f3`）。本版是**文档与面板**的一次整理，
> 外加两个真 bug 修复 —— **没有新增工具、没有改任何工具的调用方式**，四条 AI 调用不变量全部保持。

### 新增 · 英文入口与双语发布说明

作者要求「**做成中英文版本的文档，格式参考 dsh 的 git**」：
README 顶部加 `[中文](#…) | [English](#english-overview)` 语言锚点，
并新增 `## English overview` 一屏英文速览（是什么 / 30-second quick start / **8 个工具各一行** /
安装三要点 / 四条 ground rules）。**它只做导航**：完整的 op 与参数指向下面那份生成的清单，
所以中英两侧**不会各维护一份会漂移的副本**（`readme-test` 断言英文速览也必须覆盖 8/8 工具）。
发布说明同样按 DSH Release 的格式做**中英双语**
（`[中文] | [English]` 语言锚点 + `<h3 id="cn-…">` / `<h3 id="en-…">` 分节 + 四段分类 + `Full Changelog`），
从本版起每版一份、放在 `.github/release-notes/`。


### 文档改「渐进式披露」：入口精简 + 细节按需加载

作者原话：**「优化下文档，参考技能的渐进式思路」**（那次 `skills-add` 的三级加载：
元数据常驻 → 正文触发时加载 → 捆绑资源按需加载；SKILL.md 目标 <500 行，超了就把细节下沉并**写清「什么时候读哪个文件」**）。

先量了一下**谁会进上下文**：

| 文档 | 改前 | 改后 | 什么时候进上下文 |
|---|---|---|---|
| 工作区 `AGENTS.md` | 531 行 / 26 183 字符 | **155 行 / 6 751 字符** | **每个会话的模型基线**（每轮都在花） |
| 插件 `README.md` | 1 063 行 / 46 109 字符 | **408 行 / 24 876 字符** | 按需（有人点开 / AI 决定读） |

工作区 `AGENTS.md` 是大头：里面**约 380 行是「每轮都用不到」的细节**（控件实例化的完整实测、
`.gia` 全字段、按键表、插件工具表、面板、安全约定、截图纪律、测试清单），却**每轮都载入**。

**做法**（两处同一套）：

1. **入口只留「不看就会做错」的东西**：铁律、路径规则（不写死）、编码（无 BOM）、交接清单、
   每轮生效的插件规矩（开发优先级 / AI 调用四条不变量 / 只有两条通道能到达 AI）。
2. **细节下沉成独立文件**，入口给一张**「你手上在做的事 → 读哪个文件」**的表。
   工作区新增 5 篇：`千星奇域_客户端控件与按键实测` / `千星奇域_运行时日志与试玩信号` /
   `dsh-miliastra_使用与安全详述` / `工作区目录结构` / `工作区常用操作`；
   插件仓库新增 4 篇：`功能详解` / `面板与活文件安全` / `关键知识与能力边界` / `配置与开发`。
3. **加了绊线**（`readme-test`，14 项）：README 里 `docs/…md` 的链接**必须存在**，
   且 `docs/` 下**不许有孤立文件**（没人链接 = 没人会读）。**双向都查**。
4. 顺手补了生成块的**参数说明列** —— 以前只带「类型/必填/取值」，
   所以「改了 description 要跑生成器」这句话**对参数不成立**；现在成立。

> 搬运用脚本按 `##` 标题**机械切分**（逐行原样），不改写内容 —— 手抄 400 行中文必出错。
> 踩的坑：`promote()` 把 `#` 数量写反（`'#$1 '` = 加深一级），导致四个文件的层级都反了、目录空掉；
> 已按文件分别削 `#`（带 ``` 代码围栏保护，免得误改 `powershell` 里的 `#` 注释）。

### 修复 · `summaryOnly`：`byLevel` 没瘦身，还会「越瘦越大」

真机验证时被 `smoke` 抓到 —— 而且**第一版断言本身是错的**：

- **断言脆**：它写死了键名 `x`。晚上 23:33 那局的 `.gia` 里只有 `容器交接值`，没有 `x` → **假红**。
  → 改成**数据无关**：键集合一致 + 全量里有 `core`/`hotBin` 的摘要里必须还在 + 有箱的键才报 `binsOmitted`。
- **真 bug 一**：`slimLoose()` 把 `byLevel` **整块原样带出去**（`byLevel` 里每一层也是一份
  `numericStats`）→ 只瘦了顶层，等于白瘦。
- **真 bug 二**：`slimStats()` 永远加 `binsOmitted`。**没有箱子可省时，摘要是比全量更大的** ——
  实测单点数据 **1601B → 1630B**。→ 改成「**有箱可去才报**」，并给 `byLevel` 的每一层都套上瘦身。
- **「省体积」这条改用固定夹具验**（12 个箱 × 多键）：**546B → 188B**。
  真机那一局只有 1 个数、1 个箱 —— 不具代表性（回执里那段 `note` 文案本身就比省下来的还长），
  所以真机只验「不丢结论」，省体积交给夹具。**别让真机数据的偶然形态决定测试的松紧。**

### 新增 · 面板版本行与「Host 自陈旧」（夜战补充 P0-C 的 GUI 面）

夜战补充那份 10 条想法**没有一条是 GUI 功能**，逐条对过之后，能落到面板上的是 **P0-C**：

- **面板以前连版本都不显示**（标题只有 `Miliastra Wonderland · 千星奇域`）。
- 而 Host 半边是**启动时的快照** —— 今晚源码 22:41 改到 `0.0.9`，Host 是 **22:18 启动**的、仍报 `0.0.5`，
  定位花掉 **5 个调用**（grep `package.json`、grep `VERSION`、查进程 `CreationDate`、手工拼时间线）。
  **这件事该由 Host 自己说。**

改法：

- `hostStaleness()`（**导出的纯函数**，好测）：源码 `package.json` 版本 ≠ 载入的 `VERSION`，**或**
  `index.js` 的 mtime 晚于进程启动时刻 → `stale:true` + `deltaMin` + **带「重启 dsh web」的下一步**；
  读不到源码**不报假警**。
- `hostSummary()`：`version` / `startedAt` / `pid` / `uptimeSec` / `source`，
  **`/miliastra/status` 与 `miliastra_health` 共用同一份**（判据只有一份，免得两处漂移）。
- **面板**：顶部常显 `Host vX · 启动 HH:MM · 源码 vY`；**陈旧时才变琥珀色**并把 hint 接在后面。
- 测试：`smoke` 补 Host 陈旧判据的形状用例（同版本+旧 mtime → 不报；版本变了 / 晚 23 分钟 → 报）；
  `client-render-test` 补「版本行 + 陈旧警告」（面板 status 也按既有约定支持 `props.__status` 注入）。

> **没做的（说明为什么）**：P0-A 动态包络、P1-E `waitNew`、P1-F 截图裁剪 —— 都是**工具侧**能力，
> 上了面板也只是「显示一个工具结果」，不如先把工具做对；P1-D 后台连拍在面板上**早就有 GUI 版**
> （「开跑自动截图」开关）；P2-9 的「本局编号」面板**也已经显示**（还有一句「与 .gia 的 instance 对得上号」）。
> 另外 P0-B（部署对账两条口径）要接部署指纹才说得准，属于工具侧那一批，不塞进这轮 GUI。

---

### 已验证 · 测试

`npm test` 全绿，合计 **462 项**（446 + 16 契约自检）：

| 套件 | 项数 | 本版变化 |
|---|---|---|
| `smoke` | **48** | +Host 陈旧判据 / `summaryOnly` 夹具省体积 |
| `readme-test` | **14** | +语言锚点 / 英文速览覆盖 / **README 与 `docs/` 双向可达** |
| `client-render-test` | **32** | +**面板版本行与陈旧警告** |
| 其余 8 个套件 | 352 | — |
| 契约自检 `selftest.mjs` | 16 | — |

```
✓ Host 陈旧判据：同版本 + 旧 mtime → 不报；源码版本变了 / index.js 晚 23 分钟 → 报并要求重启（deltaMin=23）
✓ slimStats 在「多箱」夹具上真的省体积 → 546B → 188B（省 358B）
✓ README 与 `docs/` 的「按需加载」双向可达 —— 链接 4 篇，docs/ 无孤立文件
```

---

## [0.0.10] — 2026-09-23

**系统提示段：从「手写一段话」改成「从数据生成 + 有断言」。**

起因是作者问了一个架构问题：**「作为 AI，怎么判断插件有什么功能？是否需要都写出来？」**
这个问题本身翻出两个事实：

1. **只有两条通道能自动到达 AI**：**工具 schema**（name/description/parameters 直接进模型工具列表）
   与**系统提示段**（`systemPrompt.section`）。面板与 HTTP 路由对 AI 是**黑洞** —— 除非截图，它"看不见"。
2. 本插件那段提示的「什么时候用」**只覆盖 5 个工具**：`miliastra_playtest` / `miliastra_shot`
   （`0.0.4`~`0.0.8` 加的）**在 AI 的开场提示里没有任何指路** —— 而没有任何机制会发现这件事。
   **和 README 第一段停在 `0.0.1` 是同一个病**：第二份副本没人管。

### 变更

- 提示段文案改为 `renderPromptSection()` 从 **`PROMPT_GUIDE` / `PROMPT_RULES` 数据生成**，不再手写段落。
- 补上此前缺失的指路：**`miliastra_playtest`**（开跑信号在 `output_log.txt`，**`.gia` 里没有**）、
  **`miliastra_shot`**（连拍是**一次调用**、可先 `dryRun` 看计划）；
  以及后来的三条纪律：**只报数字不下判决** / **截图与备份绝不自动删** / **试玩按钮只能人点**。
- **`PROMPT_SKIP`** 只豁免 `miliastra_echo`（纯调试工具，不需要开场指路）。

### 新增 · 断言（防它再落后）

`smoke` 里那条「系统提示段覆盖」：

- **不在 `PROMPT_SKIP` 里的工具，必须有一条「什么时候用」** → 加新工具忘了补就**当场红**；
- 指路的工具必须**真实存在**（改名/删除后忘了同步 → 红）；
- **工具名必须与该条说明写在同一行** → 不然读的人不知道「· 先用它定位…」的「它」是谁；
- 每条必须有实质说明（长度下限）。

判据刻意做成**绊线**而不是"质检"：豁免是显式的，所以**不会误报**；代价只是「加工具时要想一句什么时候用」——那一句本来就该想。

**实测比例**：工具 schema **13 049 字**（AI 自动看见的全量）；提示段 **1 166 字 ≈ 9%** —— 只写 schema 表达不了的**决策**，**不复述参数/返回值**。

### 修复（「断言查错对象」的第二次）

第一版断言写成 `text.includes(工具名)` → **假通过**：工具名在第一行的工具清单里就有，
于是「每条 bullet 根本没带工具名」也照样绿 —— 渲染出来是「· 先用它定位『当前关卡…』」，
**读的人不知道「它」是谁**。→ 渲染补上工具名，断言改成**按行**查「`工具名：`」。

> 同一类错误今晚出现过两次：一次是「回执不含 base64」因为注释里写着 base64 而误判，
> 一次就是这个。**教训：断言要断"实质"，而且要查对对象。**（已写进技能 `tool-design-for-ai.md` §11）

### 已验证 · 测试

发布时的快照（**445 项** = 429 + 16 契约自检）：

| 套件 | 项数 | 本版变化 |
|---|---|---|
| `smoke` | **46** | +1：系统提示段覆盖 |
| `readme-test` | **10** | 新增：主文档与代码一致性 |
| 其余 9 个套件 | 383 | — |
| 契约自检 `selftest.mjs` | 16 | — |

> 之后的增补（英文速览断言、`docs/` 双向可达、Host 陈旧判据、`summaryOnly` 夹具）算 **0.0.11**，见上。
### 主文档（README）重做：功能写进主 md，且**不再会过时**

作者原话：**「将功能可以写到主 md 中，我发现主 md 一直都有点过时，插件的文档更新下 ——
目标是傻子 AI 都能一下明白怎么用」**。

先承认现状：README 当时 **810 行 / 53.6 KB**，但它**确实一直在落后**，而且不是一两处：

| 过时的地方 | 当时写的 | 实际 |
|---|---|---|
| 工具数量 | 「变成 **6 个**工具」 | **8 个** |
| 测试项数（开发一节） | `smoke` **45** / `deploy` **28** / `probe-deploy` **10** | **46 / 30 / 16** |
| 验证阶梯合计 | **444 项**（397 + 31 + 16） | **455 项**（408 + 31 + 16），**本轮重排后 460**（413 + 31 + 16） |
| AI 调用不变量 | **三条** | **四条**（系统提示段那一条是这一版加的） |
| 工具表 | 只列了部分 op（`shot` 少了 `targets`、`map` 少了 `strings`、`code` 少了 `backup`/`backups`、`probe` 少了 `list`） | `TOOLS` 里 **29 个 op 取值 / 69 个参数** |

**根因**：README 是**第二份副本**，手写的清单**必然漂移** —— 和「README 第一段停在 `0.0.1`」、
「系统提示段漏了 4 个版本」是**同一个病**。

**改法**（三条，按重要性排）：

1. **功能写进主 md，而且工具清单改成「生成」的**：
   `node tools/gen-readme-tools.mjs --write` 从 `index.js` 的 **`TOOLS`** 生成
   「工具速查」块 —— 每个工具的**完整 description + 每个参数的类型/必填/取值**
   （**8 875 字符**，就是 AI 在 schema 里看到的那份）。
   单一真身 = schema，README 只是它的**投影**。
2. **新增 `tests/readme-test.mjs`（14 项）钉住它**：
   生成块与 `TOOLS` **逐字一致**（差一个字符就红，并报出**第一处差异在第几行**）、
   导航表覆盖 **8/8** 工具、README 里**不许出现已不存在的工具名**（改名/删除后忘改文档 → 红）、
   **29 个 op 取值与 69 个参数**都必须出现在生成块里、第一段版本号 = `package.json`，
   以及**英文速览也必须覆盖 8/8 工具**（见下）。
3. **加一节「30 秒上手」**（README 最前面）：三步 —— `miliastra_health {}` 定位 →
   `miliastra_code op=inspect` / `op=deploy` 读写 → `miliastra_playtest` + `miliastra_log` 验证，
   外加三条铁律（**路径不写死 / 写操作只在 code 与 probe / 不做越界的事**）。
   **不看别的，照着这三步就能干活** —— 这就是「傻子 AI 也能一下明白」。
顺带修掉的文档硬伤：`6 个工具 → 8 个`、测试项数（`45/28/10 → 46/30/16`）、
合计（`444 → 455`）、不变量（`三条 → 四条`）、导航表补全 `targets` / `strings` / `backup`+`backups` / `list`。

**踩到的坑（值得记）**：edit 工具把 README 写成了 **CRLF**，而生成块是 LF →
`readme-test` 报「不一致，差 **148** 个字符」（= 147 行各多一个 `\r`）。
→ 生成器改成**跟着文件自己的行尾**（`eolOf`），比对前统一成 LF。
**行尾也算「内容」** —— 一个字节都不许含糊。

## [0.0.9] — 2026-09-23

**AI 调用体验整修**。纯 AI 侧，没动面板。

起因是作者问了一句：**「都是方便 AI 的功能吗？方便调用吗？」** —— 目标是「方便 AI」，
但**「AI 侧」不等于「方便调用」**。所以这一版把三处**真的不好调**的地方逐个收掉，
并且把它们变成**测试钉住的不变量**（否则下一次加 op 时顺手就破坏了，而且不会有任何报错，只是变回难用）。

### 整修 ① 参数名不再撞车：`which` → `stage`

`op=levels` 原来是 `which`（玩法里第几关），而同工具里 `level` 是**地图关卡 ID**。
中文里两个都叫「关卡」，**参数名撞车 = AI 一定会填错**。

- `which` → **`stage`**；
- `level` 的参数描述改成「**地图关卡 ID / 品牌**（哪张图，不是玩法里的第几关 —— 那个用 `stage`）」，
  `miliastra_map` / `miliastra_log` / `miliastra_playtest` 三处的 `level` 描述也一并说明；
- `stage` 的描述里两句话点明区别。
- **测试**：断言全库 `parameters` 里**不再出现 `which`**。

### 整修 ② 大返回可以瘦身：`summaryOnly:true`

实测这些返回**看一眼很贵**（≈5k tokens）：

| op | 全量 | `summaryOnly:true` |
|---|---|---|
| `miliastra_code op=levels` | **18 917 B** | **3 126 B（17%）**（省掉每块平台的坐标） |
| `miliastra_map op=clientui` | 7 592 B | **1 636 B（22%）**（省掉逐条 `records` 与谱系文字） |
| `miliastra_log op=metrics` | 3 241 B | 2 561 B（省掉直方图分箱，`binsOmitted` 报出少了几个） |

**默认行为一个字节都没改**（不偷偷改语义）—— 想省就显式传 `summaryOnly:true`。

**新增纯函数**：`levelSummary(card)`（一关一行摘要）、`slimStats/slimMil/slimLoose`（去分箱留结论）。

> ⚠️ 瘦身**只去体积、不去结论**：`metrics` 的 `core`（集中区）与 `hotBin`（热区）**都留着** ——
> 那才是真正要看的两个数。测试里专门断言了「省下来了」**且**「数字没丢」。

### 整修 ③ 每个工具的 description 末尾带「典型调用」

以前描述只讲「为什么」，AI 还得自己拼参数组合。现在 8 个工具每个都有一行可照抄的 JSON，例如：

```
**典型调用**：`{"op":"inspect"}`（体检 + 看有没有被编辑器写回旧版）｜
`{"op":"deploy","source":"D:\\code\\双相\\双相_v9.lua"}`（投代码）｜
`{"op":"levels","summaryOnly":true}`（先扫全部关卡几何）→ `{"op":"levels","stage":3}`（再钻第 3 关）
```

- **测试**：断言**每个**工具的 description 都含「典型调用」。

### 修复

- **README 第一段一直写着 `版本 0.0.1`** —— 从 `0.0.1` 一路到 `0.0.9`，六次发布都没改过
  （作者在 GitHub 上发现的：「我看 git 打开第一段还是版本 0.0.1」）。
  同一处还漏了两样：**安装示例 `dsh-miliastra@0.0.1`**、**开发命令那段的 `smoke（30 项）`**。
  根因不是手滑，是**没有任何断言在管门面**：功能都对，README 上写着旧版本。
  → 现在 version 三处（`package.json` / `index.js VERSION` / README 第一段）**+ 安装示例**都有断言
  （`smoke` 里那条「版本号三处一致」）。
  **这正是「不变量测试」的价值：功能测试永远不会发现门面过时。**

### 新增 · `docs/dsh-miliastra_更新说明_0.0.4~0.0.9_2026-09-23.md`（工作区）

一页把 `0.0.4`~`0.0.9` 讲完：**AI 调用速查表**、逐版本变化与证据、三条纪律、
真机证据汇总、已知边界、验收方式。给人看，也给下一个接手的 AI 看。

### 已验证 · 测试

`npm test` 全绿，合计 **444 项**（428 + 16 契约自检）：

| 套件 | 项数 | 本版变化 |
|---|---|---|
| `smoke` | **45** | +6：三条不变量（每个工具都有「典型调用」/ 全库无 `which` / `summaryOnly` 真省且不丢数字，后两条各对 3 个 op 断言）+ **版本号三处一致 + 安装示例同步** |
| `deploy-test` | 30 | — |
| `probe-deploy-test` | 16 | — |
| `lualint-test` | 32 | — |
| `shot-test` | 103 | — |
| `playtest-test` | 63 | — |
| `giaruns-test` | 25 | — |
| `leveldata-test` | 44 | — |
| `metrics-test` | 39 | — |
| `client-render-test` | 31 | — |
| 契约自检 `selftest.mjs` | 16 | — |

> 那三条不变量的日志是照抄可查的：
> `✓ 每个工具的 description 都带「典型调用」（8 个）` /
> `✓ 没有 which 参数了` /
> `✓ op=levels 的 summaryOnly 真的省上下文 → 全量 18917B → 摘要 3126B（17%）…且摘要里的数字都在`

---

## [0.0.8] — 2026-09-23

**P2-7 截图连拍**。纯 AI 侧，没动面板。

### 新增 · `miliastra_shot op=burst`

```jsonc
// ① 立刻连拍 5 张
{ "op": "burst" }
// ② 「等试玩开跑 → 再等 3 秒 → 连拍 5 张」——**一次调用完成**
{ "op": "burst", "awaitPlaytest": true, "afterSec": 3, "count": 5 }
// ③ 先看要花多久、拍几张（一张都不拍）
{ "op": "burst", "count": 5, "dryRun": true }
```

为什么做成**一次调用**：拆成「先 `playtest op=wait` 再逐个 `capture`」两次调用时，
两次之间的往返延迟（1~3 秒）会直接毁掉时间精度 —— 而连拍的全部意义就在时间上。
判据与 `miliastra_playtest op=wait` **共用同一份实现**（`waitForPlaytestStart`），
两条路各写一套早晚会漂移，而漂移过的判据比没有判据更坏。

细节：`count` 夹在 1~20；间隔地板 800ms（小于它会被夹住并在回执里标 `clamped`）；
**某一张失败就停手**（不白耗剩下的时间），回执里给 `abortedAt`；
缩略图在**同一次进程**里顺带生成（省一次起进程）；回执**不带图片二进制**，只给
`GET /miliastra/shot?name=<file>`（原图）与 `&thumb=1`（小图）两个入口。

### 重要发现 · `burstMs` **不是**「每 N 毫秒一张」

真机第一次连拍就暴露了：要 `burstMs=800`，实测帧距是 **2600ms**。

原因是 `burstMs` 只是**两张之间的额外等待**，没算上单张自身的耗时 ——
本机（起 PowerShell + `PrintWindow` + 顺带出缩略图）**约 2.6 秒/张**。

**参数看起来是那个意思、其实不是，这种最坑**，所以：

- 回执里恒定给 **`measuredIntervalMs`（实测帧距）**与 `timing.note`，把「单张耗时才是真实上限」写在脸上；
- 实测帧距比请求值大 1.5 倍以上时，`note` 里直接加一句「⚠️ 差额是**单张自身耗时**，不是没生效」；
- 参数描述里也写明它只是「额外等待」。

### 真机证据（一次性，已跑）

```
op=burst count=3
ok=true  count=3  okCount=3  suspect=0  span=5201ms  measuredIntervalMs=2600
  #1 game-burst-verify-1-…png   0ms  1456×939  2.4 MB  suspect=false
  #2 game-burst-verify-2-…png  2640ms  1456×939  2.4 MB  suspect=false
  #3 game-burst-verify-3-…png  5201ms  1456×939  2.4 MB  suspect=false
op=burst count=2 → measuredIntervalMs=2510（第二次复现，稳定在 2.5~2.7 秒）
```

⚠️ **由此定一条边界**：单帧寿命 **<1 秒**的特效，这个速度**注定只能抓到 1~2 帧** ——
是引擎/进程启动的地板，不是参数没调好。要看快特效得改脚本（把过程画在控件上、或分多局抓）。

### 已验证 · 测试

`npm test` 全绿，合计 **438 项**（422 + 16 契约自检）：

| 套件 | 项数 | 本版变化 |
|---|---|---|
| `shot-test` | **103** | +17：`clampBurstMs` / `planBurst`（偏移、总时长、上下夹、被夹标记）/ `burstSummary`（成功失败可疑计数、真实 elapsedMs、**实测帧距**、超请求间隔的警告、失败帧带 error、**回执不含图片二进制**、单张与空集） |
| `smoke` | 39 | — |
| `deploy-test` | 30 | — |
| `probe-deploy-test` | 16 | — |
| `lualint-test` | 32 | — |
| `playtest-test` | 63 | — |
| `giaruns-test` | 25 | — |
| `leveldata-test` | 44 | — |
| `metrics-test` | 39 | — |
| `client-render-test` | 31 | — |
| 契约自检 `selftest.mjs` | 16 | — |

> 自动化测试里**故意不真拍**：每张约 2.6 秒、还会往用户的截图目录里写 2.4MB ——
> 测试不该占用户的磁盘。真机连拍留一次性证据（上面那段）。

### 顺带 · 重跑 `miliastra_playtest op=wait` 的回归

`op=wait` 的判据刚被抽成共用的 `waitForPlaytestStart`，所以这一版跑了一次真机回归：
`op=wait timeoutSec=5` 仍能被打断并如实回 `hit:false`（smoke 里那条断言覆盖）。

---

## [0.0.7] — 2026-09-23

**P1-5 指标约定**：让脚本能「吐指标」，工具能「看指标」。纯 AI 侧，没动面板。

### 起因

那一晚救命的**不是** 36 条日志正文，而是作者自己加的那一行：

```
[yuan-code] 落出边界 -> 重生（第 3 关，摔死处 x=814，最后站立=#2 移动）
```

有了它「卡在哪一段」一目了然；而这类埋点**每次都要重新发明一遍格式**。

### 新增 · `miliastra_log op=metrics`

**两层，都不猜语义：**

**① 严格约定**（可选，用上就给更完整的汇总）—— `[MIL]` 开头 + `k=v`：

```
[MIL] evt=death lv=3 x=814 stand=2
[MIL] evt=clear lv=3 ms=8420
[MIL] death lv=3 x=814              ← 事件名不带 evt= 也行（取第一个裸词）
```

| 键 | 含义 |
|---|---|
| `evt` / `event` / `e` | 事件名（缺省取第一个裸词） |
| `lv` | 关卡号（也认 `第 3 关`） |
| `x` / `y` | 位置 |
| `ms` | 耗时毫秒 |
| `n` | 计数 |
| 其它 | **原样保留**，工具不解释 |

汇总成「每个事件：次数 / **分关次数** / 数值字段的分布」+ 一条时间线。

**② 宽松抽取**（**不用改脚本**）—— 任何日志行里出现 `k=<数字>` 就收，配上关卡线索
（`第 N 关` / `lv=N`）给出**按键**的分布与集中区。这条拿**现在的日志**就能跑。

### 关键设计：两个「集中」指标，因为它们回答不同问题

| 字段 | 含义 | 抗离群值 |
|---|---|---|
| `core` | **四分位距 p25~p75**（中间 50% 落在哪） | ✅ 是 —— 「x 集中在 700~860」用的是**这个** |
| `hotBin` | 直方图命中最多的那一箱（看形状） | ❌ 会被离群值摊薄 |

实测这组数（12 个 x 跨 460~1086，其中 9 个挤在 694~860）：`hotBin` 最多只装到 **3 个**，
而 `core` 给出 **706~825** —— 一眼就能说「集中在 700~800 这一段」。

> 这条是**做的时候被测试逼出来的**：我第一版只报了直方图热区，
> 断言按「热区应该覆盖 700~860」写，结果实测热区是 `647.8~710.4`（只 3 个）——
> 不是代码错，是**指标本身不抗离群值**。补 `core` 之后两边的解释力才够。

### 真机跑出来的结果（不用改脚本）

```
2026-09-23_21-46-05_157.gia：30 条记录，宽松命中 17 条（MIL 0 条，忽略 13 条）
  键 x    ：10 次，458~1011，中位数 623，**集中区 502.5~703**，热区 458~513.3
    第 3 关：10 次
  键 玩家x： 6 次，386~679，中位数 454.5，**集中区 454~455**，热区 444.6~473.9
  关卡分布：{"3":10,"(未标注)":7}
```

（顺带看到：这一局的第 3 关摔死 x 是 **502~703**，而 21:29 那局是 **694~1086** ——
落点在改几何之后**明显前移**了。这是**数字**，不是判定。）

### 安全与纪律

- **只读** `.gia`，一个字节都不写；没有指标格式的行**一律静默忽略**（`ignored` 计数如实报出）。
- 输出里**没有** verdict/pass 这类判决字段（有测试守着）：只说「N 次里有 M 次落在 a~b」，不说「这关有问题」。
- `[MIL]` 行**不进**宽松集合（不重复计数）。

### 已验证 · 测试

`npm test` 全绿，合计 **421 项**（405 + 16 契约自检）：

| 套件 | 项数 | 本版变化 |
|---|---|---|
| `metrics-test` | **39**（新） | 解析（含中文键、引号、`#2` 不当数字）/ **静默忽略** / 分布与分位数 / 热区 vs 集中区 / 分关统计 / 时间线 / **不许出现判决字段** / 真机宽松抽取 |
| `smoke` | **39** | +1：`log op=metrics` 只读用例 |
| `deploy-test` | 30 | — |
| `probe-deploy-test` | 16 | — |
| `lualint-test` | 32 | — |
| `shot-test` | 86 | — |
| `playtest-test` | 63 | — |
| `giaruns-test` | 25 | — |
| `leveldata-test` | 44 | — |
| `client-render-test` | 31 | — |
| 契约自检 `selftest.mjs` | 16 | — |

### 已知边界

- `core` / `hotBin` 都需要**足够样本**才有意义；样本少时如实给出 `n`，别过度解读。
- 宽松抽取是**按键**统计，它**不知道** `x` 是「摔死位置」—— 键名是脚本自己起的，工具只统计。
- 想让工具认得事件名与关卡，**采用 `[MIL]` 约定**（`op=metrics` 回执里带 `convention.how` 与字段表，可照抄）。

---

## [0.0.6] — 2026-09-23

**P0-1 的 A 方案**（作者定）：把关卡表变成**机器读得动**的，插件只做「读 → 算数字 → 报出来」，
**判决留在玩法那一侧**。仍然是纯 AI 侧，没动面板。

### 起因为什么要这个

那一晚白跑了两局：按设计文档的 ASCII 图**脑算**排坐标，结果 A 的横向占位 `[400,740]` 与 B 的 `[720,1060]`
**水平投影重叠 20px**、A 最高时玩家头顶离 B 底面只剩 **6px**（原话：「碰到空气墙的感觉，被踢回来」）。
这两个数字**本来算得出来** —— 缺的只是「让工具读得到那张关卡表」。

### 新增 · `miliastra_code op=levels`

从**活文件**里抽出 `local LEVELS = { … }`，给出每关的**几何事实**：

| 字段 | 是什么 |
|---|---|
| `plats` | 每块平台 `{ n, x, y, w, h, kind, kindName, mv, iso }`（`n` = 它在表里的 1-based 序号） |
| `facts.adjacent` | **相邻声明对**：`dxOverlap`（水平投影重叠多少 px）/ `dxGap`（缝多少 px）/ `dyRise`（后者高多少）/ `pattern` |
| `facts.risingOverlaps` | 其中形状是「**向上 + 水平重叠**」的那几对（**那次白跑就是这个形状**） |
| `facts.nearMiss` | 两两之间「水平投影重叠 且 垂直净空 ≤ `nearPx`」→ `clearance` 就是「还剩几 px」 |
| `facts.overlaps` | 两块矩形**真的相交** |
| `facts.kinds` / `bbox` / `spanX` / `spanY` | 分类计数与包围盒 |

**纪律（有测试专门守这条）**：输出里**不出现** `pass` / `reachable` / `verdict` 这类字段 ——
「跳得过去吗」取决于跳跃初速、重力、移动平台相位，那是**玩法**，是作者定的。
`nearPx` 是**筛选阈值**不是判定；`adjacent` 按**声明顺序**（脚本注释说那是通关路径顺序）。

**解析边界**：只认**纯字面量表** —— 数字 / 字符串 / `true` / `false` / `nil` / 常量名（含
`local A, B, C = 1, 2, 3` 这种**多变量一行**）/ 嵌套表 / 命名段 / 尾随逗号 / 行与长注释。
表里一旦出现**算出来的值**（算术、拼接、函数调用、变量引用），**报行号 + 那一行原文**，
绝不猜、也不静默少算一块。

实测本机活文件 `双相.lua` v9.2 的表**全部落在「认」的一侧**：
`local K_PLAIN, K_ICE, … = 0, 1, 2, 3, 4, 5`、`{ 1380, 420, 40, 140, K_ICEWALL, nil, true }`、
中文都在字符串里；画布常量叫 `DESIGN_W, DESIGN_H`（不是 `CANVAS_*`，候选名单里都认）。

### 真机跑出来的结果（只报数字）

```
双相.lua：3 关，画布 1600×1000
  第1关 教学    ：平台 15（iso 1）bbox 1600×880  相邻重叠 1 处（其中**向上重叠 0**）真相交 0  净空≤48px 0
  第2关 长链    ：平台 30（iso 0）bbox 1600×550  相邻重叠 1 处（其中**向上重叠 0**）真相交 0  净空≤48px 0
  第3关 移动平台：平台 5 （iso 0）bbox 1600×560  相邻重叠 0 处                    真相交 0  净空≤48px 0
```

那两处「相邻重叠」都查了原始坐标：一处是 `x[1240..1580] y[560..650]` → `x[1280..1600] y[700..790]`
（**向下**走 140px），另一处是第 2 关两行之间的**向下**换行 460px —— 都是关卡里正常的行间过渡。
**「向上重叠」三关都是 0**，也就是 v9.2 那次「相邻平台水平投影不再重叠」的修复**确实生效了**。

### 修复 · 序号口径混用（自己踩的，写下来）

第一次把结果打出来时，`adjacent.i/j` 报的是「**非 iso 平台里的第几个**」，
而卡片里 `plats` 是**含 iso 的全表** —— 拿前者索引后者，就会指到另一块平台
（实测指到了第 1 关那块 `iso` 冰墙，差点得出「冰墙和地面重叠 300px」的错误结论）。

现在 **`i/j` 一律是「表里的 1-based 序号」**，并且每条里**内联 `aBox` / `bBox` 坐标** ——
不看序号也能直接读。序号口径这种事**必须只有一种**。

### 已验证 · 测试

`npm test` 全绿，合计 **381 项**（365 + 16 契约自检）：

| 套件 | 项数 | 本版变化 |
|---|---|---|
| `leveldata-test` | **44**（新） | 去注释 / 常量表 / 抽表 / **认不出时如实报行号**（算术、函数调用、没闭合）/ 几何事实 / 形状分类 / 序号口径 / **「不许出现判决字段」** + 真机抽表 |
| `smoke` | **38** | +1：`code op=levels` 只读用例 |
| `deploy-test` | 30 | — |
| `probe-deploy-test` | 16 | — |
| `lualint-test` | 32 | — |
| `shot-test` | 86 | — |
| `playtest-test` | 63 | — |
| `giaruns-test` | 25 | — |
| `client-render-test` | 31 | — |
| 契约自检 `selftest.mjs` | 16 | — |

### 已知边界

- 只**读**活文件，不写、不改；解析失败时返回 `ok:false` + `line` + `lineText`，由人决定怎么办。
- 坐标**按表里怎么写就怎么报**（该表约定设计坐标 y 从顶向下）；脚本转控件坐标时会翻 y
  （实测 `canvasH / 2 - dy * sy`）—— 回执里带了 `coordinateNote` 提醒，**别直接和控件坐标比**。
- 移动平台只报 `mv = { range, period }`，**不推算行程包络**（相位算法是玩法的）。

---

## [0.0.5] — 2026-09-23

按作者定的两条：**① 关卡体检走「A 方案」**（另开一轮）；**② 新能力优先做 AI 侧，GUI 面板不同步实现**。
所以这一版是**纯工具层**的四件事 + 一个顺手修掉的脆弱测试。

它们都来自同一份实战清单（`docs/dsh-miliastra_优化想法_2026-09-23.md`），逐条都先核过证据 ——
清单里 3 处硬伤（跨两张图比 `.gil` 大小、误诊「两个工具口径不一致」、`.gia` 局中持续写）
在这个版本里按实测改写了，不照抄。

### 新增 · `miliastra_code op=fixbom`（活文件带 BOM 时只去那 3 个字节）

BOM **不是本工具加的**（部署路径会拒绝带 BOM 的源文件）—— 实测它来自**新建关卡时编辑器自己写的活文件**：
本机备份里确实有一份 `EF BB BF` 开头的 **22449** 字节版，同一份内容无 BOM 是 **22446** 字节，**正好差 3 字节**。

安全顺序与部署同源：

| 情况 | 行为 |
|---|---|
| 本来就没有 BOM | **什么都不做**（不为「修一下」去动不需要动的文件，连 mtime 都不碰） |
| 备份失败 | **立即中止**，一个字节都不动 |
| 正常 | 备份 → 原子写 → 校验（**逐字节比对：除了那 3 字节，其它必须完全相同** + 无 BOM + 仍是合法 UTF-8） |
| 校验不过 | **自动回滚**回带 BOM 的那一版 |

⚠️ 回执里会**提前写明副作用**：`<原名>.bak` 这时指向「带 BOM 的那一版」，
而 `op=restore` 会**故意拒绝**还原它（那种文件还原回去 Lua 照样报错）。

### 新增 · 部署指纹（发现「活文件被静默写回旧版」）

`op=deploy` 成功后会在备份目录写一份 `.miliastra-deploy.json`（SHA / 字节 / 行数 / 来源 / 时刻）。
之后 `op=inspect` 每次都比对：

- 一致 → `sameAsDeploy:true`；
- **不一致** → `changedSinceDeploy:true` + 字节差 / 行数差 + 一句「**多半是编辑器把脚本面板里的内存版存回了磁盘**」。

为什么值得做：实测**编辑器保存时会把脚本面板里的内存版写回活文件**（那次保存让 `.gil` 从 54.5KB 涨到 65.9KB，
差值 ≈ 那次部署的脚本增量）。也就是说**部署成功 ≠ 编辑器拿的是新版**，而以前这种情况**没有任何迹象**。

> 写指纹失败**不影响部署成败**，只降级成一条 warning。

### 新增 · `ErrorLog.txt` 巡检（`miliastra_health` 与 `op=inspect`）

官方文档写得清楚：**循环调用 / 挂载失败这类错不进 `.gia`**，只在客户端脚本同目录写 `ErrorLog.txt`。
所以「`.gia` 里干干净净」**不能**单独当成「脚本没出事」的证据。
现在两个入口都顺手扫一眼：有 → 报大小 / 行数 / 首几行 + 提醒；**没有也如实显示「没有」**（省一次人工翻目录）。

### 新增 · 部署后自动对账（回执里直接给结论）

部署完立刻用 `.gil` 里嵌的脚本源码快照与刚投进去的活文件比 SHA，把结论写进回执：

- 一致 → `nextStep`：「停掉当前试玩 → 重新试玩一局」；
- 不一致 → `nextStep`：「**先在编辑器里存盘**（地图里嵌的还不是这一版）→ 再重新试玩一局」；
- 没有 `.gil` / 地图里没有脚本映射 → **如实说对不了账**，且 `nextStep` 里**不许出现「可以试玩了」**。

（起因：实测真有「部署完没重开局、白等 8 分钟」这一次。）

### 新增 · `miliastra_log op=runs`（按「局」切分 + 局间 diff）

一个 `.gia` 里可以装多局（实测 `21-24-16_155` 装了两段完整生命周期）。以前只有整文件倒序的 `op=tail`，
读的人得**人肉**把 30 多条倒过来、再分清哪段属于哪局。

现在 `op=runs` 按 `instance` 第一段切局，每局一行摘要：
`kind`（`47504` 试玩 / `90003` 编辑器主屏）、`epochSec`（**开跑时刻，与 `miliastra_playtest` 报的是同一个值**）、
`startedAt`、记录数、**疑似异常次数**、**错误样式数** + 首末行 + 就绪横幅，
并给最近两局**局间 diff**（新增/消失的错误样式 + 各项 delta + 一句结论）。

配套加了 `run=<epoch 秒或 instance 片段>` 过滤：`op=tail run=1790171162` 就**只看那一局**。

> `faultCount` / `errorSample` 是按**通用词**（重生/死亡/失败/`nil value`…）归的「**疑似**」计数，
> 不是平台给的分类 —— 回执里带 `caveat` 说明，具体含义以脚本里那行 `print` 自己的文案为准。

### 修复

- **截图列表在 mtime 并列时顺序不确定**：连着截两张常常落在同一毫秒里，
  只按 `mtimeMs` 排就退化成「`readdir` 给什么顺序就是什么顺序」，面板上同一批图每次刷新顺序都可能变。
  实测这条让一个测试**偶发失败**（断言「新的在前」时拿到 `a,b` 而不是 `b,a`）。
  现在按名字倒序兜底（文件名里带时间戳，≈ 按时间倒序），测试里也显式把两份的 mtime 拉开。

### 已验证 · 测试

`npm test` 全绿，合计 **336 项**（320 + 16 契约自检）：

| 套件 | 项数 | 本版变化 |
|---|---|---|
| `smoke` | **37** | +3：真机上 `health.errorLog` / `code.deploy` 指纹的形状、`log op=runs` 只读用例 |
| `deploy-test` | **30** | +2：指纹比对（一致 / 被改写 / 没指纹）、`fixbom` 的零改动与「备份失败即中止」 |
| `probe-deploy-test` | **16** | +6：工具层端到端的 `op=inspect` / `op=deploy` / `op=fixbom`、ErrorLog 巡检、对账失败路径 |
| `lualint-test` | 32 | — |
| `shot-test` | 86 | 修掉 1 条偶发（mtime 并列） |
| `playtest-test` | 63 | — |
| `giaruns-test` | **25**（新） | 分组 / 排序 / 计数去重 / 局间 diff；**真机回归**：`155` 必须切成 2 局试玩 |
| `client-render-test` | 31 | — |
| 契约自检 `selftest.mjs` | 16 | — |

### 文档

- README 与工作区 `AGENTS.md` 补上这四件事的用法与理由。
- `AGENTS.md` 记下作者定的开发优先级：**新能力优先做 AI 侧（工具 / 纯函数 / 回执），GUI 面板不同步实现**。

---

## [0.0.4] — 2026-09-23

补上**「试玩开跑」这个时刻本身** —— 之前所有能力都只能等这一局**结束**。

作者的原话是：「**能不能监听游戏开始玩，就能让 AI 实现游戏开始玩几秒后截图或者看日志**」。
这句话逼出一个之前一直没问对的问题：**「开跑」到底有没有可观测的信号？**

### 已验证 · 「开跑」有实时信号（本机实测）

先说结论能不能用：**能**，延迟 0.07~0.18 秒。

| | |
|---|---|
| 信号来源 | `%USERPROFILE%\AppData\LocalLow\miHoYo\<品牌>\output_log.txt`（游戏客户端自己写的 Unity 日志，**每行带毫秒时间戳、持续追加**） |
| 开跑 | `Genshin Loading Log: BeyondLevelPlayModule SetCurLevelData guid: 0 serverVersion: 0 isTrial:True` |
| 结束 | `Genshin Loading Log: StartQuickSwitchSceneAction … reason:QuickSwitchToBeyondSettleSceneNormally` |
| 实测延迟 | 2026-09-23 21:46 那局：开跑行自己写的是 `21:46:02.420`，**21:46:02.600 就读到了（Δ +0.18s）**；结束行 `21:46:58.960` → `21:46:59.033`（**Δ +0.07s**） |
| 附带好处 | 这是**平台级**标记 —— 脚本一行都不 print、磁盘上没有 `.gia` 的局，它照样记（本机 20:36 / 21:11 两局就是这样） |
| 对号 | 它带的 `NowTimeStamp:<epoch 秒>` **等于**那一局 `.gia` 里 instance 的第三段（`47504-<账号>-<这个>-<毫秒>`），所以「实时开跑」和「事后日志」能对上 |

### 修正 · 关于 `.gia` 的一条老结论

README / AGENTS 之前写过「`.gia` 文件还在变大 → 在试玩」——**这条是错的**。实测：

| 文件 | 名字时刻 | **落盘时刻（CreationTime = LastWriteTime）** | 该局结束 |
|---|---|---|---|
| `…21-46-05_157` | 21:46:05 | **21:47:07** | 21:46:58 |
| `…21-29-40_156` | 21:29:40 | **21:31:40** | 21:31:33 |
| `…21-24-16_155` | 21:24:16 | **21:26:14** | 21:26:06 |

本机 22 个 `.gia` **全部** `CreationTime == LastWriteTime`，且都晚于文件名时刻。
→ **局在跑的时候磁盘上根本没有这个文件**；「盯 `.gia`」只能等这一局结束。
（`2026-09-23_21-46-05` 这个文件名里的时刻 = 该局首次 print 的时刻，不是落盘时刻。）

### 新增

- **`miliastra_playtest`（第 8 个工具）** —— 试玩开跑/结束的实时侦测。
  - `op=status`：现在在不在试玩 / 开跑时刻 / 已跑多少秒 / 本局编号（epoch）/ 最近 8 局（起止 + 时长）/ 信号文件路径。
  - `op=wait`：**等下一次开跑**。`timeoutSec`（默认 90，上限 300）到点就回 `hit:false`，**不报错、不挂死**；
    `backSec` 可回扫刚过去的那一局（人点了试玩再叫 AI 的场景）；
    `afterSec` 命中后再等 N 秒才返回 —— 这就是「开跑 N 秒后」。
  - 典型用法：`miliastra_playtest op=wait afterSec=3` → 命中 → `miliastra_shot op=capture target=game`
    = **游戏开跑约 3 秒后的画面**。
- **`lib/playtest.mjs`** —— 解析 / 分类 / 归约 / 命中判定全是**纯函数**（可单测），I/O 无状态。
  处理了三件容易漏的事：① 日志行被拆到两次读取之间（carry）；② 游戏重启导致 `output_log.txt` **换代**（size 回退）；
  ③ 连续两次开跑没看到结束（**隐式收尾**，不把两局并成一局）。
- **面板「试玩开跑」卡片**（③ 日志与画面 栏最上面）：状态行（🟢 试玩中 · 已跑 N 秒 / ⚪ 未在试玩）、
  本局编号、最近几局胶囊、信号来源与**实测延迟**，以及
  **「开跑自动截图：开/关」+ 3/5/10 秒**（默认**关** —— 磁盘是用户的，没人点过就不该自己往里写图片）。
- **`tools/playtest-watch.mjs`** —— 只读侦测器：增量 tail `output_log.txt`，对每条命中行打印
  **Δ = 读到的时刻 − 它自己写的时刻**。这条数据就是上面那张表。
- **`tools/gia-timeline.mjs`** —— 按 `instance` 把 `.gia` 切成局，并列每局的 `level2` 区间。

### 安全

- 自动截图**默认关**；开了也只**新增 PNG 到插件数据目录**，不删、不改、不碰活文件与 `.gia`。
- 侦测只**读**游戏自己写的日志文件；不读内存、不连游戏端口、不冒充编辑器。
- `op=wait` 的时间参数全部夹在区间内（`timeoutSec` 5~300 / `afterSec` 0~120 / `backSec` 0~3600 / `pollMs` 100~5000），
  非法值给默认、不静默接受离谱值。

### 文档

- 记录一条**反例**：`.gia` 记录里的 `level2` 字段**不是局内秒表**（151 那局 25 条全是 1、155 第 1 局从 34 起、
  `90003` 那一路能到 842）→ 语义仍 `unknown`，**要时间轴只认日志行自己的毫秒时间戳**。
  （这条是自己在做本版时先信了、被实测推翻的，写下来免得后人再踩。）

### 已验证 · 测试

`npm test` 全绿，合计 **300 项**（含 16 项契约自检）：

| 套件 | 项数 |
|---|---|
| `smoke` | **34**（+3：`op=status` 形状、`op=wait` 超时可打断、试玩状态只读） |
| `deploy-test` | 28 |
| `probe-deploy-test` | 10 |
| `lualint-test` | 32 |
| `shot-test` | 86 |
| `client-render-test` | **31**（+2：开跑触发判据纯函数、试玩卡片文案） |
| `playtest-test` | **63**（新） |
| 契约自检 `selftest.mjs` | 16 |

---

## [0.0.3] — 2026-09-23

按作者反馈做减法 + 补上截图真正缺的那一环。

两条反馈直接定了这一版：

1. 「**最近一局是 2 小时前写的（18:44:57）做个是什么鬼东西 下面一大段一直在**」
   —— 那是 0.0.1 加的「试玩体检」结论条。它**常驻**：只要最近一局是几小时前写的，
   红框 + 「可能原因」+「下一步」就永远挂在日志卡片最上面。作者当然知道自己没在试玩。
   → **整块删掉**（前端 + 工具 op + 判据模块 + 测试）。
2. 「**图片最好有缩略图预览**」→ 补上，并且**不让页面去拉原图**。

同一天里还有一条硬要求：**删除试玩自动化相关的越界线索**（详见「安全」）。

---

### 变更

#### 移除「试玩体检」（`miliastra_log op=diagnose` + 面板结论条）

删掉的东西（不留半截）：

| 位置 | 删了什么 |
|---|---|
| `miliastra_log` 工具 | `op=diagnose` 整个分支；`enum` 从 5 个变 4 个（`sessions` / `tail` / `grep` / `tags`） |
| `lib/logdiag.mjs` | 整个文件（判据纯函数） |
| `tests/logdiag-test.mjs` | 整个文件（9 项），并从 `npm test` 里摘掉 |
| 面板 | 结论条（红框 + 可能原因 + 下一步）、「试玩体检」按钮、「打开面板/切关卡自动跑一次」的 effect、`__logDiag` 测试钩子 |
| 渲染测试 | 两条针对它的断言块 |

**保留**：「试玩完自动取」—— 它只是守望 `.gia` 文件、试玩结束后把那一局日志填好，
是纯读取，与「替用户下结论」无关。

「最近一局是什么时候写的」这个**事实**本来就在日志卡片的 KV 行里，看一眼就够。
工具描述里也换成一句无结论的提醒：`.gia` 里只有脚本自己 `print` 的东西，
最坑的一次是**压根忘了开试玩**（游戏客户端开着 ≠ 在试玩）。

#### 截图：缩略图预览

面板上能看到缩略图，但**页面不会去拉原图** —— 一张原图 2.4 MB，列十几张就是 30 MB，
每次刷新都要重下。所以：

- **截图时就顺手生成预览**（`capture-window.ps1 -ThumbOut`）—— 用**同一个 bitmap** 缩小，
  **不额外起 PowerShell**（起一个进程约 1 秒，而面板一开就要十几张）
- 预览放 `<shots>\_thumbs\`，与原图**同名**；`listShots` 只收顶层文件，所以预览不会被当成截图
- `op=clean` 删截图时**连预览一起删**（否则 `_thumbs` 会攒一堆孤儿）
- 老的截图没有预览 → `GET /miliastra/shot?...&thumb=1` 会**按需生成**，`lib/thumbnail.ps1` 兜底
- 缩略图比原图旧就重做（改了图不会一直显示旧预览）
- 面板：最近一张**放大显示**（「我刚才截的对不对」是第一眼要看的），下面 6 张网格（可展开到 24），
  全部 `loading="lazy"`，点开在新标签页看原图

#### 新增只读图片路由 `GET /miliastra/shot?name=<文件名>[&thumb=1]`

插件里**唯一**返回非 JSON 的路由，所以守卫写死在这里：

| 检查 | 结果 |
|---|---|
| `name` 为空 / 含 `/` 或 `\` / 是 `.` 或 `..` | **400**（`../`、子目录、绝对路径一律拒绝） |
| 不是 `.png` | 400 |
| 解析后的父目录 ≠ 截图目录 | 400（越界） |
| 文件不存在 | 404 |
| 通过 | 200 + `image/png`，`Cache-Control: private, max-age=31536000, immutable`（文件名带时间戳，内容不会再变） |

判据抽在 `lib/shot.mjs` 的 `resolveShotFile()`（纯路径运算，**13 项测试**盯它）。

---

### 修复

#### 截图选错了窗口 —— 抓回一张 160×28 的标题栏碎片

**复现**：点「截编辑器」得到一张 **160×28** 的 PNG（1536 字节），内容是一小条菜单栏，而回执是 `ok:true`。

**根因**：进程 ≠ 一个窗口。本机实测 `BeyondEditor.exe` (5608) 同时有
**900×800 的日志窗**和 **160×28 的最小化残片**，而 `Process.MainWindowHandle` 指向了**后者**。
原实现 `foreach ($p in $procs) { if ($p.MainWindowHandle -ne 0) { ...; break } }`
就是「第一个有主窗口的进程」，于是挑中了碎片。

**修法**（`lib/capture-window.ps1` 重写选窗部分）：

1. 用 `EnumWindows` **枚举该进程的全部可见顶层窗口**（不再只看 `MainWindowHandle`）；
2. 按**面积从大到小**取第一个，同等大小优先没最小化的；
3. 最大的仍小于 **200×150** → **拒绝**（报错 + 列出所有候选），不往截图目录写垃圾；
4. 新增 `window=<标题子串>` 参数手动指定（例如 `window=日志`）；
5. 回执带 `candidates[]`（`title/w/h/area/minimized`），**选窗依据看得见**。

**顺带补的第三个可信度指标 —— `uniformRatio`**：
原来只量黑像素。但 **PrintWindow 返回全白**时黑比是 0，照样判「可信」。
现在同时量「**单一颜色占比**」> 98.5% 也判 `suspect`。
另外把「尺寸小得离谱」也纳入 `judgeCapture`（作为第二道防线）。

---

### 安全

- **不留越界线索**：上一版把「编辑器↔客户端存在一个私有本地通道、要驱动它就得冒充编辑器」
  连同端口号一起写进了文档。作者的要求是**全删，只留一句「没有自动化通道，不做」** ——
  已从 README / CHANGELOG / AGENTS.md 一并清掉，避免以后（人或 AI）照着去试。
- **只读图片路由的路径守卫**：见上。删截图时连预览一起删，不留孤儿文件。

---

### 已验证

| 层 | 命令 | 项数 |
|---|---|---|
| L1 工具层 | `node tests/smoke.mjs` | 30 |
| L1 部署与备份安全 | `node tests/deploy-test.mjs` | 28 |
| L1 探针部署 / 多活文件 / 两种关卡布局 | `node tests/probe-deploy-test.mjs` | 10 |
| L1 Lua 结构校验器 | `node tests/lualint-test.mjs` | 32 |
| L1 **截图（含缩略图路径、路径守卫、可信度四查）** | `node tests/shot-test.mjs` | **86** |
| L3 真实 React 渲染（含缩略图走小图路由） | `node tests/client-render-test.mjs` | 29 |
| L1 技能契约自检 | — | 16 |
| L4 真机 | `node tests/live-check.mjs` | 9 |

**合计 231 项**（215 + 自检 16）。

**本版真机验到的**（`observed`）：

- 选窗修复：`editor` 目标现在挑中 **900×800 的日志窗**（回执 `title=日志`，候选里能看到那个
  160×28 的碎片被标了 `minimized:true`）。**打开 PNG 看过**，确实是日志窗。
- `capture` 一次同时产出原图（92.7 KB）与预览（39.2 KB）——**打开预览看过**，是同一张图。
- 游戏目标回归正常：`YuanShen` → 1456×939、`printwindow`、黑比 0.0312。
- 活文件**未被本轮改动触碰**。

**仍未真机验的**（`pending`）：`api-check` 探针、`Tween`/`EaseType` 那套动画 API、
`game.IsTestPlay()` 的返回值、`.gia` 的落盘时机（承接前两版，仍标「尚未完全实测」）。

---

## [0.0.2] — 2026-09-23

补上工具链里一直缺的一环：**画面取证**。

前三件工具回答的是「代码对不对」（活文件 / 地图 / 日志），但**回答不了「画面对不对」** ——
控件到底挂上去了没、位置歪没歪、颜色对不对。之前这一环只能靠人截图再手动贴回来，现在做成工具；
同时把作者提的两件要求落进设计里：**它得跟日志一样主动告诉用户东西存在哪，而且要提示清理。**

本版**没有做任何侵入行为**：不碰游戏进程、不碰地图、不碰存档；唯一的写操作是往插件自己的数据目录写 PNG。
顺带把「试玩按钮能不能自动化」查清了，结论是**不能**（见「已知边界」）。

---

### 新增

#### `miliastra_shot` —— 第 7 个 Host 工具

| op | 干什么 |
|---|---|
| `capture`（默认） | 立刻截一张。`target=game`（`YuanShen.exe`，默认）/ `editor`（`BeyondEditor.exe`），也可 `process=` 指定任意进程名 |
| `list` | 截到哪去了、有多少张、占多大、最近一张 |
| `clean` | 清理。**默认 `dryRun`（只报告）**；真删要 `dryRun:false` + `confirm:true` 双钥匙 |
| `targets` | 可截目标 + 它们此刻在不在跑（复用 `lib/proc.mjs` 的 10 秒缓存） |

**回执恒带窗口身份**：`process` / `pid` / `title` / `mode` / `blackRatio`。
这不是装饰 —— 第一版抓回来的其实是**恰好压在前面的浏览器**，而当时回执只有 `{ok:true,path}`，
**光看返回值完全无法发现**，是靠人眼看 PNG 才暴露的。

#### `lib/shot.mjs` —— 判据与 IO 分开

里面有**删除判据**，所以按纯函数写、可单独测：

| 导出 | 性质 | 说明 |
|---|---|---|
| `shotFileName` / `sanitizeLabel` / `stampOf` / `nextFreeName` | 纯 | `game-<标签>-20260923-204915.png`；**允许中文**（只清 Windows 非法字符与控制字符）；同秒重复自动顺延 `-2` |
| `planClean` | 纯 | **删除判据**：`all` 或 `olderThanDays` 必须显式给；`keepLast` 是任何模式下都生效的保护网；**既不给条件就一张都不删**并说明原因 |
| `judgeCapture` | 纯 | **可信度判据**：`screen` 路线且不在前台 → `suspect:true`；黑像素 > 98.5% → `suspect:true` |
| `listShots` / `removeShots` | IO | 只收 `.png` **文件**（同名目录不算）；删除失败进 `failed[]`，**不抛** |
| `dataRoot` / `shotsDir` | IO | `MILIASTRA_DATA_DIR` > `DSH_HOME/miliastra` > `~/.dsh/miliastra`，再下 `shots\` |

#### `lib/capture-window.ps1` —— 窗口截图助手

Node 在 Windows 上没有内建窗口截图能力，所以这一段是 PowerShell。**源码刻意保持纯 ASCII**
（PowerShell 5.1 在没有 BOM 时按 ANSI 解码 `.ps1`，任何中文都会变乱码并可能打断解析）。

两条路线按可信度排序，不靠运气：

| 顺序 | 路线 | 何时用 | 可信度 |
|---|---|---|---|
| 1 | `PrintWindow(hwnd, dc, PW_RENDERFULLCONTENT=2)` | 总是先试 | **不需要窗口在前台**，被遮挡也行 |
| 2 | 屏幕抓取（`CopyFromScreen` + `AttachThreadInput`/`SetForegroundWindow` 抢前台） | PrintWindow 失败，**或第 1 步的黑像素 > 98.5%** | 只有目标窗口真的在前台才对 → 回执里标 `front` |

黑比是**量出来的**，不是猜的：Unity/D3D 的 swap chain 有时会让 PrintWindow 返回全黑，
所以不能「返回成功就当作成功」；`judgeCapture` 会把这种情况标成 `suspect`。

同时做了 **DPI 感知**（`SetProcessDPIAware`）—— 不声明的话，缩放不是 100% 时窗口矩形与像素对不上，图会被裁。

#### 面板 · 「游戏截图」卡片（第 ③ 栏改名 **「③ 日志与画面」**）

作者的要求原话：「**做游戏截图希望有这个功能跟日志一样要提示用户，是存到插件运行目录要提示用户清理**」。
所以卡片的设计目标**不是「能截到图」**（工具已经截出来了），而是**让用户始终知道东西落在哪**：

| 卡片上永远可见 | 为什么 |
|---|---|
| **存放目录**（绝对路径，随 `status` 一起来，不用点按钮才看得到） | 不然用户根本不知道图去哪了 |
| **现有 N 张 · 占多大** + 最近一张文件名 | 不然不知道要不要清 |
| **「不会自动删。清理要你自己点：先看『会删哪些』，确认了才真删。」** | 用户会默认工具会自己收拾 —— 这句必须写在卡片上，不能藏在文档里 |
| 截图回执：截到的是「原神」pid=3284 · 1456×939 · 窗口自绘 · 黑比 0.0312 | **自证**：不用相信工具，一眼看出它截的是不是那个窗口 |

按钮：`截取游戏画面` / `截编辑器` / `列目录` / `清理…`。清理是**两步**：
未规划时**不给**「确认删除」按钮（测试守住这条），规划出来后列出将删清单 + 确认 + 取消。
`suspect=true` 时用醒目样式（`-err`）弹出 `warning`。

#### `tools/shot-live.mjs`（开发工具，不随包发布）

真机验收用：截图这件事的正确性**只有看图才知道**，单元测试能测的只有命名 / 清理规划 / 目录解析。
`node tools/shot-live.mjs game` 会真截一张并把路径打出来让人去看。

---

### 变更

#### 探针描述的模板清单改为**生成**，不再硬编码

`miliastra_probe` 的工具描述原本写死「**四个模板** `ping` / `tree` / `instantiate` / `api-surface`」——
第 5 个 `api-check` 加进去之后**描述就悄悄过期了**（真机上一查才发现：描述里只有 4 个，`enum` 里却是 5 个）。
现在数量与清单都从 `PROBE_TEMPLATES` / `PROBE_INFO` 生成，**加模板不可能再漏**。
测试名与注释里同样过期的「四个模板」一并修掉。

---

### 修复

- **PowerShell 5.1 重定向 stdout 走的是控制台代码页（本机 936/GBK），不是 UTF-8**。
  窗口标题「原神」出来是 `d4ad c9f1`，Node 按 utf8 解码成 `ԭ��`。
  确认方式：`execFileSync` 拿原始 bytes 看 hex 尾巴。修法：脚本把**所有非 ASCII 转义成 `\uXXXX`**，
  整行输出纯 ASCII，任何解码器都不会错（已复验：`title` 解析回 `原神`）。
- **`package.json` 的 `files` 只有 `lib/**/*.js` 和 `lib/**/*.mjs`，漏了 `lib/**/*.ps1`** ——
  **发布到 npm 会缺掉 `capture-window.ps1`，别人装上截图直接坏**。已补。
  （这类「本地测全绿、装给别人就坏」的问题只有对着发布清单逐条核才看得见。）

---

### 安全

截图模块按**「磁盘是用户的」**这个前提设计，和备份同一套原则：

| 约定 | 为什么 |
|---|---|
| **绝不自动删** | 删除不可恢复。`clean` 只按**显式条件**干活（`all` 或 `olderThanDays`） |
| 没给条件 → **一张都不删**并说明原因 | 「默认帮你清一清」是最糟的行为 |
| 真删要 **`dryRun:false` + `confirm:true` 双钥匙** | 和 `noBackup` 双钥匙同源：不给随手绕过 |
| `keepLast` 在**任何模式**下都生效 | 保护网：哪怕 `all:true` 也至少留最新 N 张 |
| 落盘**不放两个地方** | ① 不放 `external_lua_file\`（米哈游存档，会污染「活文件 = 目录里的 .lua」这类判断）；② 不放 `node_modules\dsh-miliastra\`（**插件升级/重装会整个替换掉，用户的图会凭空消失**）。所以放第三个地方：插件自己的数据目录 |
| 工具回执里**永远报绝对路径 + 张数 + 体积** | 用户要能自己决定清不清 |

**本版没有做任何侵入行为**：不读游戏内存、不连游戏端口、不改地图、不动存档。

---

### 文档

- 本文件新增 `[0.0.2]` 段；`README.md` 工具表加 `miliastra_shot`、新增「截图」小节与「试玩按钮为什么不能自动化」；
  版本号 `0.0.1` → `0.0.2`。
- 工作区 `AGENTS.md` §9 工具表加 `miliastra_shot`、测试项数更新、新增**画面取证纪律**与
  **试玩按钮通道对照表**（三个通道逐条给证据）。

---

### 已验证

| 层 | 命令 | 项数 |
|---|---|---|
| L1 工具层 | `node tests/smoke.mjs` | 30 |
| L1 部署与备份安全 | `node tests/deploy-test.mjs` | 28 |
| L1 探针部署 / 多活文件 / 两种关卡布局 | `node tests/probe-deploy-test.mjs` | 10 |
| L1 Lua 结构校验器 | `node tests/lualint-test.mjs` | 32 |
| L1 试玩体检判据 | `node tests/logdiag-test.mjs` | 9 |
| L1 **截图：命名 / 清理规划 / 目录解析 / 可信度判据 / 真删双钥匙** | `node tests/shot-test.mjs` | **59** |
| L3 真实 React 渲染（+ 截图卡片、两步清理） | `node tests/client-render-test.mjs` | 30 |
| L1 技能契约自检 | — | 16 |
| **L4 真机** | `node tests/live-check.mjs` | 9 |

**合计 214 项**（含自检 16 项）。

**本版真机验到的**（`observed`）：

- 截图抓的是**原神窗口本体**：`process=YuanShen pid=3284 title=原神`，`mode=printwindow`、
  `blackRatio=0.0312`、1456×939；**打开 PNG 看过**，是游戏画面（UID 201170108），不是浏览器。
- `PrintWindow` 路线**确实不需要窗口在前台**（回执 `front:false` 而图是对的）。
- 端到端：`op=targets` / `op=list` / `op=capture` 走通，落盘 `~/.dsh/miliastra/shots/`。
- 活文件**未被本轮改动触碰**：`双相.lua` = `97266B79…E028`、22446 字节（写操作只发生在插件数据目录）。

**仍未真机验的**（`pending`）：`api-check` 探针、`Tween`/`EaseType` 那套动画 API、
`game.IsTestPlay()` 的返回值、`.gia` 的落盘时机（维持 0.0.1 的结论：尚未完全实测）。

---

### 已知边界

- **「试玩」按钮没有自动化通道 —— 不做。** 编辑器没有官方 CLI、没有插件/本地 RPC 通道，
  主界面是 **Unity 自绘**的（UIA 子孙节点 = 0），没有可驱动的原生控件。
  试玩由人在编辑器里点；插件负责把人的动作接住（试玩完自动取回最新一局日志），
  **不做任何越界的事**（不读游戏内存、不连游戏进程的端口、不冒充编辑器）。

- **截图不能按组件取景**：没有蛋仔 `eggy_shot focus=prefix:椅子` 那种「按组件包围盒自动移镜头」，
  因为千星奇域这边**还没有读场景对象的工具**（`.gil` 的 `#7` 场景对象字段已能读，但没做成工具）。
  目前只截整个窗口。
- **`target=editor` 截到的是编辑器的日志/节点图窗口**，不是 3D 编辑视图 —— 3D 视图在**游戏客户端窗口**里
  （所以 `target=game` 才是看画面的那个）。
- **`.gil` 里 30+ 个顶层字段仍未解读**（承接 0.0.1）。

---

## [0.0.1] — 2026-09-23

首个版本。这套工具链不是设计出来的，是**在原神·千星奇域 7.1 正式服上排一次真实的 bug 排出来的** ——
那次「`game.InstantiateClientUIControl` 对任何索引号都返回 `nil`」折腾了好几小时，
根因是**「客户端控件模板库为空」**。踩过的坑都固化成了工具能力与 README 的「关键知识」。

本版开发过程里有相当一部分时间花在**推翻自己**上：先后纠正了 7 条想当然的结论
（见「修复 · 被实测推翻的结论」）。所以下面每条「实测」都尽量带证据、标状态。

---

### 新增

#### Host 半边 · 6 个工具

| 工具 | 能力 |
|---|---|
| **`miliastra_health`** | 环境体检：客户端安装 / 关卡 / 活文件 / 地图 / 日志目录定位。**两种关卡布局都扫**（`<id>\<id>.gil` 与根目录的 `<id>.gil` —— 只扫前者会漏掉大部分图）。另含编辑器 / 游戏**进程状态**（`BeyondEditor.exe` / `YuanShen.exe`，10 秒缓存、best-effort 不抛） |
| **`miliastra_code`** | 活文件 `inspect` / `read` / `deploy` / `backup` / `backups` / `restore`。部署一律 **先备份 → 二进制拷贝 → 比对 SHA-256 → 校验无 UTF-8 BOM** |
| **`miliastra_map`** | 读 `.gil`（protobuf）：`summary` / `clientui` / `script` / `strings`。`clientui` 给出**客户端控件谱系 + 可被脚本创建的模板清单**；`script` 给出**地图快照 vs 活文件的一致性** |
| **`miliastra_log`** | 读 `.gia` 运行时日志：`sessions` / `tail` / `grep` / `tags`，结构化到 `{时间, 账号, 玩家, 关卡, 正文}` |
| **`miliastra_probe`** | 探针模板化（见下），`render` → `deploy` → `collect` |
| **`miliastra_echo`** | 回显参数，确认参数真的传到了 Host |

另外：

- **同源 HTTP 路由** `/miliastra/{status,tools,tool}` —— 面板取数据的唯一合法通道，
  带**本机访问守卫**（非 `127.0.0.1`/`localhost` 一律 403）、未知路由 404、`Cache-Control: no-store`。
- **系统提示段**：让 agent 知道「有这么个插件、什么时候该用它」，而不是去拼 PowerShell。
- **`/status` 的 `clientHalf` 自检**：查询宿主 `dsh-client-modules` 的 boot 图，
  直接回答「面板 bundle 有没有进 `window.__DSH_BOOT__`」——
  把原先「只能靠人眼看面板出没出来」变成可断言的数据（含 `rev` = bundle 内容哈希）。

#### Client 半边 · 侧边栏面板

- 注册到**官方槽位** `sidebar.footer.action`（不是 DOM 注入）；**三栏**布局（① 关卡 / ② 代码 / ③ 日志），每栏独立滚动
- 粉蓝配色；入口图标内联 PNG（像素画，`image-rendering: pixelated`）
- **关卡切换 + 自动跟随**：默认跟着 `miliastra_health` 判定的「当前关卡」走；点某一关切成手动锁定，一键回自动
- **进程状态**：免截图看编辑器 / 游戏在不在跑（实测：编辑器是**独立进程**，不是游戏子进程）
- **多活文件**：一个关卡可以有多个 `.lua`（不同角色各一个），选择器切换，所有操作跟着走
- **活文件体检**：大小 / 行数 / SHA-256 / 是否带 BOM / 编码是否合法 / 有无中文注释
- **脚本一致性**：地图里嵌的脚本快照 vs 本地活文件 —— 回答「跑的是不是本地这版代码」
- **备份清单 + 一键还原**（详见「安全」）；份数过多只提醒，**永不自动删**
- **试玩完自动取回**：守望 `.gia`，试玩结束自动把那一局的日志填好（见「变更」）
- **历史日志**：列出最近 12 局，点某一局只看那一局；TAG 汇总与按 TAG 过滤
- **诊断日志三连**（刻意保留）：`factory 已执行` / `apply 运行中` / `入口已注册` ——
  client 半边失败默认是**全静默**的，这三行让排障一眼定位卡在哪一步

#### 部署前的 Lua 结构校验（`lib/lualint.mjs`）

**一段语法错的 Lua 投进沙箱，试玩会静默不生效** —— 脚本根本没起来，日志里只会是「什么都没有」，
这比报错难查得多。所以 `miliastra_code op=deploy` 与 `miliastra_probe op=deploy` 默认先扫一遍结构：

| `lintMode` | 行为 |
|---|---|
| `strict`（默认） | 缺 `end` / 括号不配平 / 字符串或长注释没闭合 / `repeat` 少 `until` → **拒绝部署，且不碰活文件、不产生备份** |
| `warn` | 照投，但问题进 `warnings[]`，不静默吞掉 |
| `off` | 不校验（逃生舱）；未知取值明确报错，**不静默降级成 off** |

零依赖的结构级启发式（**不是**完整 Lua 解析器）：先严格剥注释与字符串（含 `--[[ ]]` / `[==[ ]==]`），
再统计块开闭与括号配对；报的是**「第 N 行的 function 没有对应的 end」**，不是「文件最后一行」。

#### 5 个探针模板（`lib/probes.mjs`）

| 模板 | 大白话 | 干什么 |
|---|---|---|
| `ping` | 探活 | 确认脚本到底有没有跑起来（日志空着时先跑它） |
| `tree` | 看控件 | 屏幕上挂着哪些客户端控件、父子关系、画布多大 |
| `instantiate` | 试钥匙 | 拿一串索引号去试，看哪个真能被脚本创建 |
| `api-surface` | 翻字典 | 把枚举和成员列出来（比如按键的真名） |
| `api-check` | 核文档 | 官方文档写的接口，真机上到底有没有（见「文档」） |

> 所有模板都内建 `script:EnableUpdate(true)`（漏了它 `OnUpdate` 永不触发，只会打三行空壳），
> 且长输出统一走 `pChunked()` 分片（见「修复」）。

> **⚠️ 下面这一节已在 0.0.3 移除**（作者反馈：那段结论条**常驻**在面板最上面，
> 「最近一局是 2 小时前写的」+ 一大段「可能原因/下一步」对用户没有信息量）。保留原文仅作历史记录。

#### 试玩体检（`miliastra_log op=diagnose`）

背景：作者试玩了一会儿回来说「看看」，**磁盘上却没有新的 `.gia`** —— 而当时的工具只会
安安静静把**上一次的旧日志**端上来。他不知道漏了哪一步，最后自己查出来是**忘了从编辑器开试玩**。
（原话：「是我忘记开了 **这情况要提示用户**」。）

这类「空结果」最坑的地方是：它和「脚本没 print」「日志被配置关掉」「试玩还在进行中」
**长得一模一样**。所以把「人肉判断」抽成可断言的**纯函数** `lib/logdiag.mjs`：

| 结论 | 触发 | 给什么 |
|---|---|---|
| `fresh` | 最近一局 ≤ 120 秒前写的 | 一句话：这应该就是你刚才那局 → 直接取日志 |
| `stale` | 最近一局更旧 | 醒目提示「**你刚才那局没有写出日志**」+ 按证据给可能原因 + 下一步 |
| `no-logs` | 一个 `.gia` 都没有 | 这张图从没试玩过（并点出编辑器 / 游戏在不在跑） |

`stale` 时的可能原因**按「能拿到的事实」排**，不笼统说「不知道」：

1. **其实没点「试玩」**（最常见）—— 实测同一个游戏进程跨了一整天的全部试玩，所以「进程在跑」不能当判据
2. 或者试玩还在进行中 —— ⚠️ `.gia` 落盘时机**尚未完全实测**，这里如实标「有可能只在结束时写」，**不写成定论**
3. 地图刚存过盘（有事实才提；地图比日志旧就不提，免得误导）
4. 编辑器「日志」面板里 `客户端脚本` 没勾选 → 脚本 print 不落盘

面板上放在**日志卡片最上面**（`stale` 用醒目样式、`fresh` 只一句淡色说明），
打开面板 / 切关卡时**自动跑一次**，并留一个「试玩体检」按钮随时再问。
`logdiag-test.mjs` 9 项专门盯这个判据（**判错会把人带偏**：比如游戏都没跑却说「试玩进行中」）。

#### 离线知识库抽取（工作区产物，**不进本仓库**）

内容是官方文档的抽取产物（版权归米哈游），本仓库只放可发布的代码。产物在工作区 `docs/`：

| 产物 | 抽取工具 | 规模 |
|---|---|---|
| `千星奇域_API参考.md` | `tools/extract-api-reference.mjs` | **153 个接口 + 27 张枚举表 / 394 个成员** |
| `千星奇域_按键事件枚举对照表.md` | `tools/extract-key-enums.mjs` | `KeyEventType` 164 / `KeyboardKeyCode` 59 / `ControllerKeyCode` 25 |
| `千星奇域_补间动画.md` | `tools/extract-api-reference.mjs` | `Tween` / `TweenSequence` / **`EaseType` 31 条缓动曲线** |

---

### 变更

#### 探针从面板上撤下来，收进可折叠的「高级诊断」

背景：作者反馈「**折腾探针对人没啥用啊**」。这是对的 —— 探针解决的是「AI 写代码时不知道某件事」，
而创作者要为它付出四次手工操作（部署 → 停试玩 → 重开 → 收回 → 还原）。

- 探针收进右栏「高级诊断」折叠卡片，**默认收起**（收起态不渲染内容，不是藏起来）
- 同一折叠区里放进**「读界面控件」**（只读，从 `.gil` 读，不碰任何文件），
  两块**风险分开标注**：① 只读 ／ ② 会临时覆盖你的脚本

#### 日志从「一坨文本」改成格式化列表

| 改前 | 改后 |
|---|---|
| 时间/标签/正文同色同字号挤成一行 | 时间（暗）+ `[TAG]`（胶囊）+ 正文（自动折行）分列 |
| 出错那行混在几十行里看不出 | 疑似异常 → 左红竖线 + 底色；警告 → 粉线 |
| 超长行把整块撑爆（枚举 dump 上万字符） | 超 400 字符折叠成「共 N 字符，点开看全」 |
| 高度上限 210px | 52vh（约 3.7 倍） |
| 无筛选 | 「只看异常」开关 + `N 行 · 疑似异常 X · 警告 Y` 计数 |

> ⚠️ 异常判色是**关键词启发式**（地图脚本 `print` 不带级别），只用来「显眼」，不用来「判定」。
> 有反例测试守住：`err=nil` 是**正常**输出，不能一见 `nil` 就标红（否则「只看异常」全是噪音）。

#### 试玩结束自动取回最新一局

守望 `.gia` 文件：出现新文件 / 文件变大 → 说明试玩在动；安静 6 秒后自动取回那一局。
判据抽成**纯函数** `autoFollowStep`（定时器不好测，判据好测）：

| 情况 | 行为 |
|---|---|
| 出现新文件 / 文件变大 | 记为「在动」，不取 |
| 还在变大 | 状态显示「这一局还在写（试玩中）」 |
| 安静够 6 秒且「看见动过」 | **自动取回** |
| 打开面板时最新一局是旧的（几小时前） | **不取** —— 别拿旧局面糊用户 |
| 打开面板时最新一局刚写完（30 秒内） | 算「刚玩过」，直接取 |
| 同一版日志（名字 + 大小没变） | 不重复取 |
| 0 字节 | 不取，显示「这一局还没写出内容」 |

#### 备份命名统一：固定名 + 本地时间戳各一份

每次备份写**两份**：固定名 `<原名>.bak`（= 最近一次覆盖前的版本，**还原默认用它**）
+ 历史 `<原名>.<本地时间戳>_备份.lua`（永不自动删，同秒撞名顺延 `-2`/`-3`）。
时间戳从 **UTC 改成本地时间**（原来比本地少 8 小时，看着像错的）。

#### 地图体检扩展（静态读 `.gil`，不用试玩、不碰任何文件）

| 字段 | 读出来是什么 | 实测（本机地图） |
|---|---|---|
| `#29` | 客户端 + 资源版本 | `CNRELWin7.1.0`　资源 `48145775 / 48379043 / 48455349 / 48474090` |
| `#11` | 阵营 / 出生点 / 预设点 | 阵营 3 个（初始玩家/物件/造物阵营 + `UI_MarkPlayer_Faction_*`）、出生点「出生点1」、预设点「新建预设点」 |
| `#7` | 场景对象 | **403 条**（`地形01` …） |

#### 界面文案改成「使用者视角」

背景：作者反馈「**这探针描述很抽象啊 有没有大白话的说明**」「**gui 界面也没看懂探针作用**」。
根因是按**实现者视角**写文案（「只读诊断脚本」「最小连通性」「读画布上所有活控件的控件模板索引并逐个试创建」）。

| 位置 | 改前 | 改后 |
|---|---|---|
| 工具描述 | 「探针（只读诊断脚本）模板化」 | 「探针 —— **「问游戏一句」的工具**」，写明为什么需要它、代价是什么、四步怎么走 |
| 面板小标题 | 只读诊断脚本 | 问一句就答 |
| 模板按钮 | `ping` / `tree` / `instantiate` | **探活（ping）/ 看控件（tree）/ 试钥匙（instantiate）/ 翻字典（api-surface）/ 核文档（api-check）** |
| 选中的模板 | 无说明 | 「做什么 / 什么时候点它」两行展开（来自 Host 的 `PROBE_INFO`，单一口径） |
| 面板顶部 | 无 | 「探针是干嘛的 / 什么时候用 / 要付什么代价」+ 四步流程 |

---

### 修复

#### 契约类（错过一次就够难受的）

- **`exports.inject` 没声明 `slots`** → cordis 的服务代理直接抛
  `cannot get property "slots" without inject`，**整条 loader entry 失败**。
  症状最难看：宿主报 `failed to apply loader entry`，而页面上**一条我们的日志都没有**。
  更坑的是当时把诊断日志写在了 `try` 外面 —— 一行探针把整个插件打死了。
- **Client 的 `callTool` 少剥一层信封** → 面板上「本机存档 —」「关卡数 undefined」全是空的。
  信封是三层 `body{ok,data:{name,ok,data:<业务返回体>}}`，这个错**犯了两次**
  （一次在 `live-check.mjs`、一次在面板里）。现在剥离逻辑抽成独立函数并加了回归测试。

#### 扫描与路径

- **关卡扫描盲区**：`Beyond_Local_Save_Level` 下有两种布局，只扫带文件夹的那种会**漏掉大部分图**
  （实测 8 个只看见 3 个；修复后 18 个）。
- **`MILIASTRA_LOCALLOW` 写错会静默回退到真实存档根** —— 手滑就会去动真文件。
  改成「显式指定就照做，指错就明确报错」。
- **解析 5 字节 varint 只解 4 字节**，漏掉整整一段 ID 区间（工具早期版本）。

#### 备份与还原

- **备份同秒撞名**：时间戳只到秒，同一秒连部署两次会覆盖前一份备份。改为自动顺延 `-2` / `-3`。
- **备份文件的 mtime 沿用源文件**（`copyFileSync` 的行为，已实测）→ 若按 mtime 排序取「最新备份」会**选错**，
  界面上显示的备份时间也是错的。改为：备份 mtime 显式设成**创建时间**，排序**优先用文件名里的时间戳**。
- **`stampOfName` 解析下标算错**（按含 `-` 的字符串算的，实际拼起来时 `-` 已去掉）——
  由新加的断言抓出。

#### 结构校验器

- **换行被丢掉** → 「行尾标识符 + 下一行行首词」粘成一个标识符
  （`local sx, sy` 换行接 `local` → `sylocal`），**行首的 `end` 被吞进上一行尾巴**。
  症状极具迷惑性：**真文件恒报「缺 29 个 end」，而单行小样例全绿**。
  逐行拆 token 才抓到（`L130:sylocal`）。现在换行占位保留，有 4 条回归断言盯它。
- **缺 `end` 时报的是「文件最后一行」**，对排障几乎没用。改为记开块栈，报**最内层没关上的那个块**的行号。

#### 日志与探针

- **探针漏 `script:EnableUpdate(true)`** → `OnUpdate` 永不触发，游戏里只打
  `OnInit`/`OnEnable`/`OnStart` 三行空壳。模板已内建这行，并有断言守住。
- **探针长输出被日志静默截断**（实机跑 `api-surface` 时发现）：
  **单条日志消息上限实测正好 10000 字符**，超出部分不报错、不提示，直接没了。
  `Enum.KeyEventType`（164 项）那份刚好打满，排在字母表后面的
  `KeyboardMoveLeftKeyDown` / `KeyboardJumpKeyDown` 恰好全被切掉 ——
  **看日志的人会以为「枚举里没有这个键」**。现在长输出走 `pChunked()`（3500 字符 / 片，带 `(1/3)` 标记），
  测试守卫「模板必须有分片 helper 且 `CHUNK < 10000`」。
- **`api-surface` 模板里的语法错**（`if … then … end else … end`）—— 由新加的模板结构校验抓出。
  那种错**投进去就是整段静默失效**。

#### 面板

- **漏列 `api-surface`**（硬编码了 3 个模板名）→ 改成**从 Host 的 `op=list` 拿清单**，
  加模板不用改面板；拿不到才用面板内兜底文案，并有测试核对两边不脱节。
- **按钮编号缺「②」**（只有 ① 部署 / ③ 收回，中间那步要人做却没写）→ 补一行显式说明。
- **部署后没有「还原我的脚本」入口** → 新增 **「④ 还原我的脚本」** 一键按钮
  （用部署时自动生成的那份备份；未部署时该按钮不出现）。
- **界面文案里残留 Markdown 记号**（`**粗体**` 与反引号在面板上是**原样显示**的）→ 清掉并加测试守住。
  该测试后来**从 1 种渲染态扩到 3 种**（收起 / 展开 / 展开且带数据），一扩又抓到两处漏网。
- **README 里「客户端排障」整节重复了两遍** —— 复查时发现，删掉残缺的那份、保留完整版。

#### 被实测推翻的结论

写代码与文档时想当然的判断，逐条用真数据推翻。**这些是本版最有价值的部分**：

| # | 原来的说法 | 实测事实 |
|---|---|---|
| 1 | `.gia` 文件名里的数字 = 游戏进程 pid | ❌ **不是**。`YuanShen.exe` 从 12:39 一直开着（pid **1088**），而文件里的号从 **135 逐局涨到 151** —— 它是**每次游戏启动**的会话序号 |
| 2 | 每次试玩 = 一个新 `.gia` 文件 | ❌ **也是错的**。一个 `.gia` 里可以有**多次试玩**：实测 `17-01-26_149` 里出现两遍同一份探针输出（`instance` 不同：17:01:21 / 17:01:58） |
| 3 | 「游戏进程在跑」≈ 在试玩 | ❌ **不能用**。同一个游戏进程跨了当天 13:40~18:44 的**全部**试玩 |
| 4 | `.gia` 里可能有框架级的「开始/结束」标记 | ⚠️ **默认没有**（只有脚本自己 `print` 的内容），但这条**取决于编辑器日志面板的配置** —— 不是平台的硬边界 |
| 5 | `Enum.KeyboardKeyCode` 里有物理键名 | ❌ **没有**。它只有语义键（`JumpKey` / `MoveLeftKey`…共 59 项），所以 `Enum.KeyboardKeyCode.W` **恒为 nil 且不报错**。绑键只能认 `Enum.KeyEventType` |
| 6 | 能从 `.gil` 里直接读出「哪些号能被创建」 | ⚠️ 只能读**候选**（无父节点 + 名字是控件类型）。实测 `1073741867`(文本框) / `1073741868`(图片) 真能创建，而 `1073741863~1866`（画布实例）一律 `nil` |
| 7 | 官方 API 文档里有「479 个函数 / 420 个枚举」 | ❌ **那个数字是错的**（把枚举成员当函数一起数了）。校准后是 **153 个接口 + 27 张枚举表 / 394 个成员**。教训：**报数之前先用正则量一遍** |

> 第 7 条是我在对话里先报错的，后来靠「先校准再报」纠正 —— 已写进工作区 `AGENTS.md` 的教训清单。
> 另外还有一条**抽取工具本身**的坑：官方原文把枚举成员写成「表名直接拼成员名」
> （`Enum.EaseTypeLinear`，不是 `Enum.EaseType.Linear`），
> 且参数里会**嵌套括号**（`onComplete: fun()`）—— 用 `[^)]*` 会整批漏掉 `Tween:SetOnComplete` 这类条目。

---

### 安全

活文件是**唯一副本**：它只活在米哈游的本地存档目录里，没有 git、没有撤销。
按这个前提把「能覆盖 / 能损坏它」的路径逐条审了一遍，**审出 2 个致命 + 4 个高危**并全部修掉：

| # | 级别 | 问题（都实证过） | 修法 |
|---|---|---|---|
| 1 | 🔴 | `deploy` 里**备份失败后没有 return，照样覆盖** —— 磁盘满 / 无权限时会把唯一副本直接抹掉 | **备份失败立即中止**，活文件零改动，并给出「去检查 `_backup` 是否可写 / 磁盘是否已满」 |
| 2 | 🔴 | 写文件**非原子**（`copyFileSync` 直写目标）—— 断电 / 崩溃会留下半截损坏的文件（备份同样） | 新增 `atomicWriteFile`：同目录临时文件 → **fsync** → **rename 原子替换**（实测 `renameSync` 在 Windows 上可覆盖） |
| 3 | 🟠 | 备份 mtime 沿用源文件 → 「最新备份」会选错 | 见「修复 · 备份与还原」 |
| 4 | 🟠 | 备份名时间戳用 UTC | 改本地时间 |
| 5 | 🟠 | `miliastra_probe op=deploy` 把 `_探针_*.lua` 写进**活文件目录**，而「当前文件」按 mtime 取最新 → **探针成了当前文件**，后续不带 `file` 的操作全打错目标 | 探针源码改写到**备份目录**（紧挨着被替换的文件）；另加名字护栏：`pickLuaFile` / `chooseLua` 都会跳过附属文件（`_探针_*` / `*_备份.lua` / `*.bak`）并报告跳过了几个 |
| 6 | 🟡 | `noBackup:true` 可随手传 → 绕过唯一安全网 | 改成**双钥匙**：必须同时传 `allowNoBackup:true`，否则拒绝 |
| 7 | 🟡 | 没有固定名备份，还原得自己翻时间戳挑 | 固定名 `<原名>.bak` + `op=restore` **不传 backup 就用它** |
| 8 | 🟡 | 还原失败只报一句 error | 失败给 `nextSteps`；**校验不过自动回滚**到覆盖前那一版并明确告知；部署同理 |
| 9 | 🟡 | 自覆盖（`source == dest`）没拦 | 拒绝并说明 |

**给人的提示（面板）**：备份卡片显示「位置 / 固定名备份 / 备份写在脚本旁边」、
**「还原到最新备份」一键按钮**（不用挑版本）、列表里固定名那份标 `★`、
覆盖前确认语写明「覆盖谁 + 会先备份 + 校验不过会自动回滚」、失败时把 `nextSteps` 一起显示。

**给 AI 的提示（工具描述）**：单列「安全约定（写活文件的地方都遵守，别绕过）」7 条；
`file` / `backup` / `noBackup` / `allowNoBackup` / `backupDir` 每个参数写清后果；
所有写操作回执 `restoreWith`（照着跑就能还原）。

---

### 文档

- **本仓库**：`README.md`（装、用、关键知识、能力边界、验证阶梯）、`CHANGELOG.md`（本文件）、`LICENSE`。
  README 顶部新增**文档索引**，说明哪些文档在仓库里、哪些在工作区、以及**为什么不进仓库**。
- **工作区 `AGENTS.md`**：本工作区的长期约定。本版新增/重写了
  §8.1 按键事件（含两条静默出错的坑）、§8.2 运行时能枚举什么、§8.3 `.gia` 真实结构（四条纠正）、
  §9 的「活文件安全约定」表与「高级诊断」两块风险说明。
- **工作区 `docs/`**：三本离线参考（API / 按键枚举 / 补间动画），全部由工具抽取、**不要手改**。
- **新增开发工具**（不随包发布）：
  `lint-probes` · `lint-all` · `inspect-gia` · `gil-field` · `gil-info` · `gia-runs` ·
  `render-probe` · `live-render-check` · `watch-playtest` · `dump-panel-text`

> `dump-panel-text` 是这一版**特意做出来的**：作者两次反馈「界面看不懂」，
> 根因都是**没人以使用者视角读过一遍**。现在改文案前先把它打出来自己读。

---

### 已验证

| 层 | 命令 | 项数 |
|---|---|---|
| L1 工具层（lossless JSON / JSON Schema / 只读用例） | `node tests/smoke.mjs` | 22 |
| L1 部署与备份安全（备份失败不覆盖 / 原子写 / 固定名 / 回滚 / 双钥匙 / 自覆盖拦截） | `node tests/deploy-test.mjs` | 28 |
| L1 探针部署 / 多活文件 / 两种关卡布局 / 模板结构 | `node tests/probe-deploy-test.mjs` | 10 |
| L1 Lua 结构校验器（不误报 / 报对行号 / 15 个真文件回归） | `node tests/lualint-test.mjs` | 32 |
| L1 试玩体检判据（四种结论 / 阈值边界 / 拿不到进程也不能乱猜） | `node tests/logdiag-test.mjs` | 9 |
| L3 真实 React 渲染（窄态 / 视觉身份 / 信封剥离 / 日志格式化 / 自动取回判据 / 备份卡片 / 读界面控件 / 试玩体检） | `node tests/client-render-test.mjs` | 28 |
| L1 技能契约自检（`dsh-plugin-win10` 的 `selftest.mjs`，原名 `dsh-plugin-dev`） | — | 16 |
| **L4 真机**（对着运行中的 `dsh web`） | `node tests/live-check.mjs` | 9 |

**合计 154 项**（含自检 16 项）。契约取证自本机 **`dsh 0.1.5-rc.1`**。

两格比数字更值钱的证据：

1. **`clientHalf` 断言**（L4 内）—— 查宿主 `dsh-client-modules` 的 boot 图，
   机器证明「面板 bundle 已进 `window.__DSH_BOOT__`」，并给出 `rev`（bundle 内容哈希）。
   **这一格以前只能靠人眼看面板出没出来。**
2. **枚举数量双向对上** —— 运行时探针数出 `KeyEventType` 164 / `KeyboardKeyCode` 59 / `ControllerKeyCode` 25，
   与**离线文档抽取**逐项一致。文档与真机互相印证，而不是各自声称。

**真机跑过的东西**（`observed`）：`api-surface` 探针（部署 → 试玩 → collect → 还原全程走通）、
`双相_v8_正式服` 脚本、`.gil` 静态读取、备份与还原。

**还没真机跑过的**（`pending`，别当已验）：
`api-check` 探针（只过了结构校验）、文档抽出来的 `Tween` / `TweenSequence` / `EaseType` 那套动画 API、
`game.IsTestPlay()` 的返回值、以及 **`.gia` 到底在试玩开始还是结束时落盘**（体检里如实标了「尚未完全实测」）。

---

### 已知边界

- **编辑器 UI 操作没有自动化通道**：在「客户端控件模板」里点【添加客户端控件】、给容器节点挂脚本、
  建容器节点 —— 必须由人在编辑器里完成。本插件替代的是「人和 AI 之间的来回搬运」，不是编辑器操作本身。
- **不做场景写操作**：工具只读，或只写「活文件」（脚本本身），不碰地图数据。
- **「是否在试玩」没有权威信号**：只能用「`.gia` 还在不在变大」代理，滞后数秒。
  `YuanShen.exe` 在跑**不能**当判据（见「被实测推翻的结论」第 3 条）。
- **负载 / 节点图统计尚未实现**：官方文档确认「负载计算」是编辑器 UI 功能（静态菜单 + 动态试玩报告）；
  「节点图」在 `.gil` 里只找到**复合节点名注册**（`#10`），逻辑本身不在存档里。
- **`.gil` 里 30+ 个顶层字段仍未解读**（本版只读了 `#1 #2 #7 #9 #11 #29 #39 #43 #50`）。
- **`patchReload: live` 不会重新 import Host 模块**（实测：改 `cordis.patch.yml` 触发重载后
  `/miliastra/status` 仍返回旧字段）—— 所以 **Host 改动一定要重启 `dsh web`**，
  只有 `lib/client.js` 刷新页面即可（`rev` 会变，可机器核对）。
