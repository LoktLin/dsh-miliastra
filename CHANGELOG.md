# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
变更分类参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)：
**新增 / 变更 / 修复 / 安全 / 文档 / 已验证 / 已知边界**。

> **证据标记约定**（与工作区 `AGENTS.md` 一致）：
> `documented` = 官方文档写了；`observed` = 本机实测过；`pending` = 还没在真机验过。
> 凡写「实测」的，都能在仓库的 `tests/` 或工作区的 `tools/` 里找到对应的探针与断言。

---

## [0.0.3] — 2026-09-23

按作者反馈做减法 + 补上截图真正缺的那一环。

两条反馈直接定了这一版：

1. 「**最近一局是 2 小时前写的（18:44:57）做个是什么鬼东西 下面一大段一直在**」
   —— 那是 0.0.1 加的「试玩体检」结论条。它**常驻**：只要最近一局是几小时前写的，
   红框 + 「可能原因」+「下一步」就永远挂在日志卡片最上面。作者当然知道自己没在试玩。
   → **整块删掉**（前端 + 工具 op + 判据模块 + 测试）。
2. 「**图片最好有缩略图预览**」→ 补上，并且**不让页面去拉原图**。

同一天里还有一条硬要求：**删除试玩自动化相关的越界线索**（详见「安全」）。

---

### 变更

#### 移除「试玩体检」（`miliastra_log op=diagnose` + 面板结论条）

删掉的东西（不留半截）：

| 位置 | 删了什么 |
|---|---|
| `miliastra_log` 工具 | `op=diagnose` 整个分支；`enum` 从 5 个变 4 个（`sessions` / `tail` / `grep` / `tags`） |
| `lib/logdiag.mjs` | 整个文件（判据纯函数） |
| `tests/logdiag-test.mjs` | 整个文件（9 项），并从 `npm test` 里摘掉 |
| 面板 | 结论条（红框 + 可能原因 + 下一步）、「试玩体检」按钮、「打开面板/切关卡自动跑一次」的 effect、`__logDiag` 测试钩子 |
| 渲染测试 | 两条针对它的断言块 |

**保留**：「试玩完自动取」—— 它只是守望 `.gia` 文件、试玩结束后把那一局日志填好，
是纯读取，与「替用户下结论」无关。

「最近一局是什么时候写的」这个**事实**本来就在日志卡片的 KV 行里，看一眼就够。
工具描述里也换成一句无结论的提醒：`.gia` 里只有脚本自己 `print` 的东西，
最坑的一次是**压根忘了开试玩**（游戏客户端开着 ≠ 在试玩）。

#### 截图：缩略图预览

面板上能看到缩略图，但**页面不会去拉原图** —— 一张原图 2.4 MB，列十几张就是 30 MB，
每次刷新都要重下。所以：

- **截图时就顺手生成预览**（`capture-window.ps1 -ThumbOut`）—— 用**同一个 bitmap** 缩小，
  **不额外起 PowerShell**（起一个进程约 1 秒，而面板一开就要十几张）
- 预览放 `<shots>\_thumbs\`，与原图**同名**；`listShots` 只收顶层文件，所以预览不会被当成截图
- `op=clean` 删截图时**连预览一起删**（否则 `_thumbs` 会攒一堆孤儿）
- 老的截图没有预览 → `GET /miliastra/shot?...&thumb=1` 会**按需生成**，`lib/thumbnail.ps1` 兜底
- 缩略图比原图旧就重做（改了图不会一直显示旧预览）
- 面板：最近一张**放大显示**（「我刚才截的对不对」是第一眼要看的），下面 6 张网格（可展开到 24），
  全部 `loading="lazy"`，点开在新标签页看原图

#### 新增只读图片路由 `GET /miliastra/shot?name=<文件名>[&thumb=1]`

插件里**唯一**返回非 JSON 的路由，所以守卫写死在这里：

| 检查 | 结果 |
|---|---|
| `name` 为空 / 含 `/` 或 `\` / 是 `.` 或 `..` | **400**（`../`、子目录、绝对路径一律拒绝） |
| 不是 `.png` | 400 |
| 解析后的父目录 ≠ 截图目录 | 400（越界） |
| 文件不存在 | 404 |
| 通过 | 200 + `image/png`，`Cache-Control: private, max-age=31536000, immutable`（文件名带时间戳，内容不会再变） |

判据抽在 `lib/shot.mjs` 的 `resolveShotFile()`（纯路径运算，**13 项测试**盯它）。

---

### 修复

#### 截图选错了窗口 —— 抓回一张 160×28 的标题栏碎片

**复现**：点「截编辑器」得到一张 **160×28** 的 PNG（1536 字节），内容是一小条菜单栏，而回执是 `ok:true`。

**根因**：进程 ≠ 一个窗口。本机实测 `BeyondEditor.exe` (5608) 同时有
**900×800 的日志窗**和 **160×28 的最小化残片**，而 `Process.MainWindowHandle` 指向了**后者**。
原实现 `foreach ($p in $procs) { if ($p.MainWindowHandle -ne 0) { ...; break } }`
就是「第一个有主窗口的进程」，于是挑中了碎片。

**修法**（`lib/capture-window.ps1` 重写选窗部分）：

1. 用 `EnumWindows` **枚举该进程的全部可见顶层窗口**（不再只看 `MainWindowHandle`）；
2. 按**面积从大到小**取第一个，同等大小优先没最小化的；
3. 最大的仍小于 **200×150** → **拒绝**（报错 + 列出所有候选），不往截图目录写垃圾；
4. 新增 `window=<标题子串>` 参数手动指定（例如 `window=日志`）；
5. 回执带 `candidates[]`（`title/w/h/area/minimized`），**选窗依据看得见**。

**顺带补的第三个可信度指标 —— `uniformRatio`**：
原来只量黑像素。但 **PrintWindow 返回全白**时黑比是 0，照样判「可信」。
现在同时量「**单一颜色占比**」> 98.5% 也判 `suspect`。
另外把「尺寸小得离谱」也纳入 `judgeCapture`（作为第二道防线）。

---

### 安全

- **不留越界线索**：上一版把「编辑器↔客户端存在一个私有本地通道、要驱动它就得冒充编辑器」
  连同端口号一起写进了文档。作者的要求是**全删，只留一句「没有自动化通道，不做」** ——
  已从 README / CHANGELOG / AGENTS.md 一并清掉，避免以后（人或 AI）照着去试。
- **只读图片路由的路径守卫**：见上。删截图时连预览一起删，不留孤儿文件。

---

### 已验证

| 层 | 命令 | 项数 |
|---|---|---|
| L1 工具层 | `node tests/smoke.mjs` | 30 |
| L1 部署与备份安全 | `node tests/deploy-test.mjs` | 28 |
| L1 探针部署 / 多活文件 / 两种关卡布局 | `node tests/probe-deploy-test.mjs` | 10 |
| L1 Lua 结构校验器 | `node tests/lualint-test.mjs` | 32 |
| L1 **截图（含缩略图路径、路径守卫、可信度四查）** | `node tests/shot-test.mjs` | **86** |
| L3 真实 React 渲染（含缩略图走小图路由） | `node tests/client-render-test.mjs` | 29 |
| L1 技能契约自检 | — | 16 |
| L4 真机 | `node tests/live-check.mjs` | 9 |

**合计 231 项**（215 + 自检 16）。

**本版真机验到的**（`observed`）：

- 选窗修复：`editor` 目标现在挑中 **900×800 的日志窗**（回执 `title=日志`，候选里能看到那个
  160×28 的碎片被标了 `minimized:true`）。**打开 PNG 看过**，确实是日志窗。
- `capture` 一次同时产出原图（92.7 KB）与预览（39.2 KB）——**打开预览看过**，是同一张图。
- 游戏目标回归正常：`YuanShen` → 1456×939、`printwindow`、黑比 0.0312。
- 活文件**未被本轮改动触碰**。

**仍未真机验的**（`pending`）：`api-check` 探针、`Tween`/`EaseType` 那套动画 API、
`game.IsTestPlay()` 的返回值、`.gia` 的落盘时机（承接前两版，仍标「尚未完全实测」）。

---

## [0.0.2] — 2026-09-23

补上工具链里一直缺的一环：**画面取证**。

前三件工具回答的是「代码对不对」（活文件 / 地图 / 日志），但**回答不了「画面对不对」** ——
控件到底挂上去了没、位置歪没歪、颜色对不对。之前这一环只能靠人截图再手动贴回来，现在做成工具；
同时把作者提的两件要求落进设计里：**它得跟日志一样主动告诉用户东西存在哪，而且要提示清理。**

本版**没有做任何侵入行为**：不碰游戏进程、不碰地图、不碰存档；唯一的写操作是往插件自己的数据目录写 PNG。
顺带把「试玩按钮能不能自动化」查清了，结论是**不能**（见「已知边界」）。

---

### 新增

#### `miliastra_shot` —— 第 7 个 Host 工具

| op | 干什么 |
|---|---|
| `capture`（默认） | 立刻截一张。`target=game`（`YuanShen.exe`，默认）/ `editor`（`BeyondEditor.exe`），也可 `process=` 指定任意进程名 |
| `list` | 截到哪去了、有多少张、占多大、最近一张 |
| `clean` | 清理。**默认 `dryRun`（只报告）**；真删要 `dryRun:false` + `confirm:true` 双钥匙 |
| `targets` | 可截目标 + 它们此刻在不在跑（复用 `lib/proc.mjs` 的 10 秒缓存） |

**回执恒带窗口身份**：`process` / `pid` / `title` / `mode` / `blackRatio`。
这不是装饰 —— 第一版抓回来的其实是**恰好压在前面的浏览器**，而当时回执只有 `{ok:true,path}`，
**光看返回值完全无法发现**，是靠人眼看 PNG 才暴露的。

#### `lib/shot.mjs` —— 判据与 IO 分开

里面有**删除判据**，所以按纯函数写、可单独测：

| 导出 | 性质 | 说明 |
|---|---|---|
| `shotFileName` / `sanitizeLabel` / `stampOf` / `nextFreeName` | 纯 | `game-<标签>-20260923-204915.png`；**允许中文**（只清 Windows 非法字符与控制字符）；同秒重复自动顺延 `-2` |
| `planClean` | 纯 | **删除判据**：`all` 或 `olderThanDays` 必须显式给；`keepLast` 是任何模式下都生效的保护网；**既不给条件就一张都不删**并说明原因 |
| `judgeCapture` | 纯 | **可信度判据**：`screen` 路线且不在前台 → `suspect:true`；黑像素 > 98.5% → `suspect:true` |
| `listShots` / `removeShots` | IO | 只收 `.png` **文件**（同名目录不算）；删除失败进 `failed[]`，**不抛** |
| `dataRoot` / `shotsDir` | IO | `MILIASTRA_DATA_DIR` > `DSH_HOME/miliastra` > `~/.dsh/miliastra`，再下 `shots\` |

#### `lib/capture-window.ps1` —— 窗口截图助手

Node 在 Windows 上没有内建窗口截图能力，所以这一段是 PowerShell。**源码刻意保持纯 ASCII**
（PowerShell 5.1 在没有 BOM 时按 ANSI 解码 `.ps1`，任何中文都会变乱码并可能打断解析）。

两条路线按可信度排序，不靠运气：

| 顺序 | 路线 | 何时用 | 可信度 |
|---|---|---|---|
| 1 | `PrintWindow(hwnd, dc, PW_RENDERFULLCONTENT=2)` | 总是先试 | **不需要窗口在前台**，被遮挡也行 |
| 2 | 屏幕抓取（`CopyFromScreen` + `AttachThreadInput`/`SetForegroundWindow` 抢前台） | PrintWindow 失败，**或第 1 步的黑像素 > 98.5%** | 只有目标窗口真的在前台才对 → 回执里标 `front` |

黑比是**量出来的**，不是猜的：Unity/D3D 的 swap chain 有时会让 PrintWindow 返回全黑，
所以不能「返回成功就当作成功」；`judgeCapture` 会把这种情况标成 `suspect`。

同时做了 **DPI 感知**（`SetProcessDPIAware`）—— 不声明的话，缩放不是 100% 时窗口矩形与像素对不上，图会被裁。

#### 面板 · 「游戏截图」卡片（第 ③ 栏改名 **「③ 日志与画面」**）

作者的要求原话：「**做游戏截图希望有这个功能跟日志一样要提示用户，是存到插件运行目录要提示用户清理**」。
所以卡片的设计目标**不是「能截到图」**（工具已经截出来了），而是**让用户始终知道东西落在哪**：

| 卡片上永远可见 | 为什么 |
|---|---|
| **存放目录**（绝对路径，随 `status` 一起来，不用点按钮才看得到） | 不然用户根本不知道图去哪了 |
| **现有 N 张 · 占多大** + 最近一张文件名 | 不然不知道要不要清 |
| **「不会自动删。清理要你自己点：先看『会删哪些』，确认了才真删。」** | 用户会默认工具会自己收拾 —— 这句必须写在卡片上，不能藏在文档里 |
| 截图回执：截到的是「原神」pid=3284 · 1456×939 · 窗口自绘 · 黑比 0.0312 | **自证**：不用相信工具，一眼看出它截的是不是那个窗口 |

按钮：`截取游戏画面` / `截编辑器` / `列目录` / `清理…`。清理是**两步**：
未规划时**不给**「确认删除」按钮（测试守住这条），规划出来后列出将删清单 + 确认 + 取消。
`suspect=true` 时用醒目样式（`-err`）弹出 `warning`。

#### `tools/shot-live.mjs`（开发工具，不随包发布）

真机验收用：截图这件事的正确性**只有看图才知道**，单元测试能测的只有命名 / 清理规划 / 目录解析。
`node tools/shot-live.mjs game` 会真截一张并把路径打出来让人去看。

---

### 变更

#### 探针描述的模板清单改为**生成**，不再硬编码

`miliastra_probe` 的工具描述原本写死「**四个模板** `ping` / `tree` / `instantiate` / `api-surface`」——
第 5 个 `api-check` 加进去之后**描述就悄悄过期了**（真机上一查才发现：描述里只有 4 个，`enum` 里却是 5 个）。
现在数量与清单都从 `PROBE_TEMPLATES` / `PROBE_INFO` 生成，**加模板不可能再漏**。
测试名与注释里同样过期的「四个模板」一并修掉。

---

### 修复

- **PowerShell 5.1 重定向 stdout 走的是控制台代码页（本机 936/GBK），不是 UTF-8**。
  窗口标题「原神」出来是 `d4ad c9f1`，Node 按 utf8 解码成 `ԭ��`。
  确认方式：`execFileSync` 拿原始 bytes 看 hex 尾巴。修法：脚本把**所有非 ASCII 转义成 `\uXXXX`**，
  整行输出纯 ASCII，任何解码器都不会错（已复验：`title` 解析回 `原神`）。
- **`package.json` 的 `files` 只有 `lib/**/*.js` 和 `lib/**/*.mjs`，漏了 `lib/**/*.ps1`** ——
  **发布到 npm 会缺掉 `capture-window.ps1`，别人装上截图直接坏**。已补。
  （这类「本地测全绿、装给别人就坏」的问题只有对着发布清单逐条核才看得见。）

---

### 安全

截图模块按**「磁盘是用户的」**这个前提设计，和备份同一套原则：

| 约定 | 为什么 |
|---|---|
| **绝不自动删** | 删除不可恢复。`clean` 只按**显式条件**干活（`all` 或 `olderThanDays`） |
| 没给条件 → **一张都不删**并说明原因 | 「默认帮你清一清」是最糟的行为 |
| 真删要 **`dryRun:false` + `confirm:true` 双钥匙** | 和 `noBackup` 双钥匙同源：不给随手绕过 |
| `keepLast` 在**任何模式**下都生效 | 保护网：哪怕 `all:true` 也至少留最新 N 张 |
| 落盘**不放两个地方** | ① 不放 `external_lua_file\`（米哈游存档，会污染「活文件 = 目录里的 .lua」这类判断）；② 不放 `node_modules\dsh-miliastra\`（**插件升级/重装会整个替换掉，用户的图会凭空消失**）。所以放第三个地方：插件自己的数据目录 |
| 工具回执里**永远报绝对路径 + 张数 + 体积** | 用户要能自己决定清不清 |

**本版没有做任何侵入行为**：不读游戏内存、不连游戏端口、不改地图、不动存档。

---

### 文档

- 本文件新增 `[0.0.2]` 段；`README.md` 工具表加 `miliastra_shot`、新增「截图」小节与「试玩按钮为什么不能自动化」；
  版本号 `0.0.1` → `0.0.2`。
- 工作区 `AGENTS.md` §9 工具表加 `miliastra_shot`、测试项数更新、新增**画面取证纪律**与
  **试玩按钮通道对照表**（三个通道逐条给证据）。

---

### 已验证

| 层 | 命令 | 项数 |
|---|---|---|
| L1 工具层 | `node tests/smoke.mjs` | 30 |
| L1 部署与备份安全 | `node tests/deploy-test.mjs` | 28 |
| L1 探针部署 / 多活文件 / 两种关卡布局 | `node tests/probe-deploy-test.mjs` | 10 |
| L1 Lua 结构校验器 | `node tests/lualint-test.mjs` | 32 |
| L1 试玩体检判据 | `node tests/logdiag-test.mjs` | 9 |
| L1 **截图：命名 / 清理规划 / 目录解析 / 可信度判据 / 真删双钥匙** | `node tests/shot-test.mjs` | **59** |
| L3 真实 React 渲染（+ 截图卡片、两步清理） | `node tests/client-render-test.mjs` | 30 |
| L1 技能契约自检 | — | 16 |
| **L4 真机** | `node tests/live-check.mjs` | 9 |

**合计 214 项**（含自检 16 项）。

**本版真机验到的**（`observed`）：

- 截图抓的是**原神窗口本体**：`process=YuanShen pid=3284 title=原神`，`mode=printwindow`、
  `blackRatio=0.0312`、1456×939；**打开 PNG 看过**，是游戏画面（UID 201170108），不是浏览器。
- `PrintWindow` 路线**确实不需要窗口在前台**（回执 `front:false` 而图是对的）。
- 端到端：`op=targets` / `op=list` / `op=capture` 走通，落盘 `~/.dsh/miliastra/shots/`。
- 活文件**未被本轮改动触碰**：`双相.lua` = `97266B79…E028`、22446 字节（写操作只发生在插件数据目录）。

**仍未真机验的**（`pending`）：`api-check` 探针、`Tween`/`EaseType` 那套动画 API、
`game.IsTestPlay()` 的返回值、`.gia` 的落盘时机（维持 0.0.1 的结论：尚未完全实测）。

---

### 已知边界

- **「试玩」按钮没有自动化通道 —— 不做。** 编辑器没有官方 CLI、没有插件/本地 RPC 通道，
  主界面是 **Unity 自绘**的（UIA 子孙节点 = 0），没有可驱动的原生控件。
  试玩由人在编辑器里点；插件负责把人的动作接住（试玩完自动取回最新一局日志），
  **不做任何越界的事**（不读游戏内存、不连游戏进程的端口、不冒充编辑器）。

- **截图不能按组件取景**：没有蛋仔 `eggy_shot focus=prefix:椅子` 那种「按组件包围盒自动移镜头」，
  因为千星奇域这边**还没有读场景对象的工具**（`.gil` 的 `#7` 场景对象字段已能读，但没做成工具）。
  目前只截整个窗口。
- **`target=editor` 截到的是编辑器的日志/节点图窗口**，不是 3D 编辑视图 —— 3D 视图在**游戏客户端窗口**里
  （所以 `target=game` 才是看画面的那个）。
- **`.gil` 里 30+ 个顶层字段仍未解读**（承接 0.0.1）。

---

## [0.0.1] — 2026-09-23

首个版本。这套工具链不是设计出来的，是**在原神·千星奇域 7.1 正式服上排一次真实的 bug 排出来的** ——
那次「`game.InstantiateClientUIControl` 对任何索引号都返回 `nil`」折腾了好几小时，
根因是**「客户端控件模板库为空」**。踩过的坑都固化成了工具能力与 README 的「关键知识」。

本版开发过程里有相当一部分时间花在**推翻自己**上：先后纠正了 7 条想当然的结论
（见「修复 · 被实测推翻的结论」）。所以下面每条「实测」都尽量带证据、标状态。

---

### 新增

#### Host 半边 · 6 个工具

| 工具 | 能力 |
|---|---|
| **`miliastra_health`** | 环境体检：客户端安装 / 关卡 / 活文件 / 地图 / 日志目录定位。**两种关卡布局都扫**（`<id>\<id>.gil` 与根目录的 `<id>.gil` —— 只扫前者会漏掉大部分图）。另含编辑器 / 游戏**进程状态**（`BeyondEditor.exe` / `YuanShen.exe`，10 秒缓存、best-effort 不抛） |
| **`miliastra_code`** | 活文件 `inspect` / `read` / `deploy` / `backup` / `backups` / `restore`。部署一律 **先备份 → 二进制拷贝 → 比对 SHA-256 → 校验无 UTF-8 BOM** |
| **`miliastra_map`** | 读 `.gil`（protobuf）：`summary` / `clientui` / `script` / `strings`。`clientui` 给出**客户端控件谱系 + 可被脚本创建的模板清单**；`script` 给出**地图快照 vs 活文件的一致性** |
| **`miliastra_log`** | 读 `.gia` 运行时日志：`sessions` / `tail` / `grep` / `tags`，结构化到 `{时间, 账号, 玩家, 关卡, 正文}` |
| **`miliastra_probe`** | 探针模板化（见下），`render` → `deploy` → `collect` |
| **`miliastra_echo`** | 回显参数，确认参数真的传到了 Host |

另外：

- **同源 HTTP 路由** `/miliastra/{status,tools,tool}` —— 面板取数据的唯一合法通道，
  带**本机访问守卫**（非 `127.0.0.1`/`localhost` 一律 403）、未知路由 404、`Cache-Control: no-store`。
- **系统提示段**：让 agent 知道「有这么个插件、什么时候该用它」，而不是去拼 PowerShell。
- **`/status` 的 `clientHalf` 自检**：查询宿主 `dsh-client-modules` 的 boot 图，
  直接回答「面板 bundle 有没有进 `window.__DSH_BOOT__`」——
  把原先「只能靠人眼看面板出没出来」变成可断言的数据（含 `rev` = bundle 内容哈希）。

#### Client 半边 · 侧边栏面板

- 注册到**官方槽位** `sidebar.footer.action`（不是 DOM 注入）；**三栏**布局（① 关卡 / ② 代码 / ③ 日志），每栏独立滚动
- 粉蓝配色；入口图标内联 PNG（像素画，`image-rendering: pixelated`）
- **关卡切换 + 自动跟随**：默认跟着 `miliastra_health` 判定的「当前关卡」走；点某一关切成手动锁定，一键回自动
- **进程状态**：免截图看编辑器 / 游戏在不在跑（实测：编辑器是**独立进程**，不是游戏子进程）
- **多活文件**：一个关卡可以有多个 `.lua`（不同角色各一个），选择器切换，所有操作跟着走
- **活文件体检**：大小 / 行数 / SHA-256 / 是否带 BOM / 编码是否合法 / 有无中文注释
- **脚本一致性**：地图里嵌的脚本快照 vs 本地活文件 —— 回答「跑的是不是本地这版代码」
- **备份清单 + 一键还原**（详见「安全」）；份数过多只提醒，**永不自动删**
- **试玩完自动取回**：守望 `.gia`，试玩结束自动把那一局的日志填好（见「变更」）
- **历史日志**：列出最近 12 局，点某一局只看那一局；TAG 汇总与按 TAG 过滤
- **诊断日志三连**（刻意保留）：`factory 已执行` / `apply 运行中` / `入口已注册` ——
  client 半边失败默认是**全静默**的，这三行让排障一眼定位卡在哪一步

#### 部署前的 Lua 结构校验（`lib/lualint.mjs`）

**一段语法错的 Lua 投进沙箱，试玩会静默不生效** —— 脚本根本没起来，日志里只会是「什么都没有」，
这比报错难查得多。所以 `miliastra_code op=deploy` 与 `miliastra_probe op=deploy` 默认先扫一遍结构：

| `lintMode` | 行为 |
|---|---|
| `strict`（默认） | 缺 `end` / 括号不配平 / 字符串或长注释没闭合 / `repeat` 少 `until` → **拒绝部署，且不碰活文件、不产生备份** |
| `warn` | 照投，但问题进 `warnings[]`，不静默吞掉 |
| `off` | 不校验（逃生舱）；未知取值明确报错，**不静默降级成 off** |

零依赖的结构级启发式（**不是**完整 Lua 解析器）：先严格剥注释与字符串（含 `--[[ ]]` / `[==[ ]==]`），
再统计块开闭与括号配对；报的是**「第 N 行的 function 没有对应的 end」**，不是「文件最后一行」。

#### 5 个探针模板（`lib/probes.mjs`）

| 模板 | 大白话 | 干什么 |
|---|---|---|
| `ping` | 探活 | 确认脚本到底有没有跑起来（日志空着时先跑它） |
| `tree` | 看控件 | 屏幕上挂着哪些客户端控件、父子关系、画布多大 |
| `instantiate` | 试钥匙 | 拿一串索引号去试，看哪个真能被脚本创建 |
| `api-surface` | 翻字典 | 把枚举和成员列出来（比如按键的真名） |
| `api-check` | 核文档 | 官方文档写的接口，真机上到底有没有（见「文档」） |

> 所有模板都内建 `script:EnableUpdate(true)`（漏了它 `OnUpdate` 永不触发，只会打三行空壳），
> 且长输出统一走 `pChunked()` 分片（见「修复」）。

> **⚠️ 下面这一节已在 0.0.3 移除**（作者反馈：那段结论条**常驻**在面板最上面，
> 「最近一局是 2 小时前写的」+ 一大段「可能原因/下一步」对用户没有信息量）。保留原文仅作历史记录。

#### 试玩体检（`miliastra_log op=diagnose`）

背景：作者试玩了一会儿回来说「看看」，**磁盘上却没有新的 `.gia`** —— 而当时的工具只会
安安静静把**上一次的旧日志**端上来。他不知道漏了哪一步，最后自己查出来是**忘了从编辑器开试玩**。
（原话：「是我忘记开了 **这情况要提示用户**」。）

这类「空结果」最坑的地方是：它和「脚本没 print」「日志被配置关掉」「试玩还在进行中」
**长得一模一样**。所以把「人肉判断」抽成可断言的**纯函数** `lib/logdiag.mjs`：

| 结论 | 触发 | 给什么 |
|---|---|---|
| `fresh` | 最近一局 ≤ 120 秒前写的 | 一句话：这应该就是你刚才那局 → 直接取日志 |
| `stale` | 最近一局更旧 | 醒目提示「**你刚才那局没有写出日志**」+ 按证据给可能原因 + 下一步 |
| `no-logs` | 一个 `.gia` 都没有 | 这张图从没试玩过（并点出编辑器 / 游戏在不在跑） |

`stale` 时的可能原因**按「能拿到的事实」排**，不笼统说「不知道」：

1. **其实没点「试玩」**（最常见）—— 实测同一个游戏进程跨了一整天的全部试玩，所以「进程在跑」不能当判据
2. 或者试玩还在进行中 —— ⚠️ `.gia` 落盘时机**尚未完全实测**，这里如实标「有可能只在结束时写」，**不写成定论**
3. 地图刚存过盘（有事实才提；地图比日志旧就不提，免得误导）
4. 编辑器「日志」面板里 `客户端脚本` 没勾选 → 脚本 print 不落盘

面板上放在**日志卡片最上面**（`stale` 用醒目样式、`fresh` 只一句淡色说明），
打开面板 / 切关卡时**自动跑一次**，并留一个「试玩体检」按钮随时再问。
`logdiag-test.mjs` 9 项专门盯这个判据（**判错会把人带偏**：比如游戏都没跑却说「试玩进行中」）。

#### 离线知识库抽取（工作区产物，**不进本仓库**）

内容是官方文档的抽取产物（版权归米哈游），本仓库只放可发布的代码。产物在工作区 `docs/`：

| 产物 | 抽取工具 | 规模 |
|---|---|---|
| `千星奇域_API参考.md` | `tools/extract-api-reference.mjs` | **153 个接口 + 27 张枚举表 / 394 个成员** |
| `千星奇域_按键事件枚举对照表.md` | `tools/extract-key-enums.mjs` | `KeyEventType` 164 / `KeyboardKeyCode` 59 / `ControllerKeyCode` 25 |
| `千星奇域_补间动画.md` | `tools/extract-api-reference.mjs` | `Tween` / `TweenSequence` / **`EaseType` 31 条缓动曲线** |

---

### 变更

#### 探针从面板上撤下来，收进可折叠的「高级诊断」

背景：作者反馈「**折腾探针对人没啥用啊**」。这是对的 —— 探针解决的是「AI 写代码时不知道某件事」，
而创作者要为它付出四次手工操作（部署 → 停试玩 → 重开 → 收回 → 还原）。

- 探针收进右栏「高级诊断」折叠卡片，**默认收起**（收起态不渲染内容，不是藏起来）
- 同一折叠区里放进**「读界面控件」**（只读，从 `.gil` 读，不碰任何文件），
  两块**风险分开标注**：① 只读 ／ ② 会临时覆盖你的脚本

#### 日志从「一坨文本」改成格式化列表

| 改前 | 改后 |
|---|---|
| 时间/标签/正文同色同字号挤成一行 | 时间（暗）+ `[TAG]`（胶囊）+ 正文（自动折行）分列 |
| 出错那行混在几十行里看不出 | 疑似异常 → 左红竖线 + 底色；警告 → 粉线 |
| 超长行把整块撑爆（枚举 dump 上万字符） | 超 400 字符折叠成「共 N 字符，点开看全」 |
| 高度上限 210px | 52vh（约 3.7 倍） |
| 无筛选 | 「只看异常」开关 + `N 行 · 疑似异常 X · 警告 Y` 计数 |

> ⚠️ 异常判色是**关键词启发式**（地图脚本 `print` 不带级别），只用来「显眼」，不用来「判定」。
> 有反例测试守住：`err=nil` 是**正常**输出，不能一见 `nil` 就标红（否则「只看异常」全是噪音）。

#### 试玩结束自动取回最新一局

守望 `.gia` 文件：出现新文件 / 文件变大 → 说明试玩在动；安静 6 秒后自动取回那一局。
判据抽成**纯函数** `autoFollowStep`（定时器不好测，判据好测）：

| 情况 | 行为 |
|---|---|
| 出现新文件 / 文件变大 | 记为「在动」，不取 |
| 还在变大 | 状态显示「这一局还在写（试玩中）」 |
| 安静够 6 秒且「看见动过」 | **自动取回** |
| 打开面板时最新一局是旧的（几小时前） | **不取** —— 别拿旧局面糊用户 |
| 打开面板时最新一局刚写完（30 秒内） | 算「刚玩过」，直接取 |
| 同一版日志（名字 + 大小没变） | 不重复取 |
| 0 字节 | 不取，显示「这一局还没写出内容」 |

#### 备份命名统一：固定名 + 本地时间戳各一份

每次备份写**两份**：固定名 `<原名>.bak`（= 最近一次覆盖前的版本，**还原默认用它**）
+ 历史 `<原名>.<本地时间戳>_备份.lua`（永不自动删，同秒撞名顺延 `-2`/`-3`）。
时间戳从 **UTC 改成本地时间**（原来比本地少 8 小时，看着像错的）。

#### 地图体检扩展（静态读 `.gil`，不用试玩、不碰任何文件）

| 字段 | 读出来是什么 | 实测（本机地图） |
|---|---|---|
| `#29` | 客户端 + 资源版本 | `CNRELWin7.1.0`　资源 `48145775 / 48379043 / 48455349 / 48474090` |
| `#11` | 阵营 / 出生点 / 预设点 | 阵营 3 个（初始玩家/物件/造物阵营 + `UI_MarkPlayer_Faction_*`）、出生点「出生点1」、预设点「新建预设点」 |
| `#7` | 场景对象 | **403 条**（`地形01` …） |

#### 界面文案改成「使用者视角」

背景：作者反馈「**这探针描述很抽象啊 有没有大白话的说明**」「**gui 界面也没看懂探针作用**」。
根因是按**实现者视角**写文案（「只读诊断脚本」「最小连通性」「读画布上所有活控件的控件模板索引并逐个试创建」）。

| 位置 | 改前 | 改后 |
|---|---|---|
| 工具描述 | 「探针（只读诊断脚本）模板化」 | 「探针 —— **「问游戏一句」的工具**」，写明为什么需要它、代价是什么、四步怎么走 |
| 面板小标题 | 只读诊断脚本 | 问一句就答 |
| 模板按钮 | `ping` / `tree` / `instantiate` | **探活（ping）/ 看控件（tree）/ 试钥匙（instantiate）/ 翻字典（api-surface）/ 核文档（api-check）** |
| 选中的模板 | 无说明 | 「做什么 / 什么时候点它」两行展开（来自 Host 的 `PROBE_INFO`，单一口径） |
| 面板顶部 | 无 | 「探针是干嘛的 / 什么时候用 / 要付什么代价」+ 四步流程 |

---

### 修复

#### 契约类（错过一次就够难受的）

- **`exports.inject` 没声明 `slots`** → cordis 的服务代理直接抛
  `cannot get property "slots" without inject`，**整条 loader entry 失败**。
  症状最难看：宿主报 `failed to apply loader entry`，而页面上**一条我们的日志都没有**。
  更坑的是当时把诊断日志写在了 `try` 外面 —— 一行探针把整个插件打死了。
- **Client 的 `callTool` 少剥一层信封** → 面板上「本机存档 —」「关卡数 undefined」全是空的。
  信封是三层 `body{ok,data:{name,ok,data:<业务返回体>}}`，这个错**犯了两次**
  （一次在 `live-check.mjs`、一次在面板里）。现在剥离逻辑抽成独立函数并加了回归测试。

#### 扫描与路径

- **关卡扫描盲区**：`Beyond_Local_Save_Level` 下有两种布局，只扫带文件夹的那种会**漏掉大部分图**
  （实测 8 个只看见 3 个；修复后 18 个）。
- **`MILIASTRA_LOCALLOW` 写错会静默回退到真实存档根** —— 手滑就会去动真文件。
  改成「显式指定就照做，指错就明确报错」。
- **解析 5 字节 varint 只解 4 字节**，漏掉整整一段 ID 区间（工具早期版本）。

#### 备份与还原

- **备份同秒撞名**：时间戳只到秒，同一秒连部署两次会覆盖前一份备份。改为自动顺延 `-2` / `-3`。
- **备份文件的 mtime 沿用源文件**（`copyFileSync` 的行为，已实测）→ 若按 mtime 排序取「最新备份」会**选错**，
  界面上显示的备份时间也是错的。改为：备份 mtime 显式设成**创建时间**，排序**优先用文件名里的时间戳**。
- **`stampOfName` 解析下标算错**（按含 `-` 的字符串算的，实际拼起来时 `-` 已去掉）——
  由新加的断言抓出。

#### 结构校验器

- **换行被丢掉** → 「行尾标识符 + 下一行行首词」粘成一个标识符
  （`local sx, sy` 换行接 `local` → `sylocal`），**行首的 `end` 被吞进上一行尾巴**。
  症状极具迷惑性：**真文件恒报「缺 29 个 end」，而单行小样例全绿**。
  逐行拆 token 才抓到（`L130:sylocal`）。现在换行占位保留，有 4 条回归断言盯它。
- **缺 `end` 时报的是「文件最后一行」**，对排障几乎没用。改为记开块栈，报**最内层没关上的那个块**的行号。

#### 日志与探针

- **探针漏 `script:EnableUpdate(true)`** → `OnUpdate` 永不触发，游戏里只打
  `OnInit`/`OnEnable`/`OnStart` 三行空壳。模板已内建这行，并有断言守住。
- **探针长输出被日志静默截断**（实机跑 `api-surface` 时发现）：
  **单条日志消息上限实测正好 10000 字符**，超出部分不报错、不提示，直接没了。
  `Enum.KeyEventType`（164 项）那份刚好打满，排在字母表后面的
  `KeyboardMoveLeftKeyDown` / `KeyboardJumpKeyDown` 恰好全被切掉 ——
  **看日志的人会以为「枚举里没有这个键」**。现在长输出走 `pChunked()`（3500 字符 / 片，带 `(1/3)` 标记），
  测试守卫「模板必须有分片 helper 且 `CHUNK < 10000`」。
- **`api-surface` 模板里的语法错**（`if … then … end else … end`）—— 由新加的模板结构校验抓出。
  那种错**投进去就是整段静默失效**。

#### 面板

- **漏列 `api-surface`**（硬编码了 3 个模板名）→ 改成**从 Host 的 `op=list` 拿清单**，
  加模板不用改面板；拿不到才用面板内兜底文案，并有测试核对两边不脱节。
- **按钮编号缺「②」**（只有 ① 部署 / ③ 收回，中间那步要人做却没写）→ 补一行显式说明。
- **部署后没有「还原我的脚本」入口** → 新增 **「④ 还原我的脚本」** 一键按钮
  （用部署时自动生成的那份备份；未部署时该按钮不出现）。
- **界面文案里残留 Markdown 记号**（`**粗体**` 与反引号在面板上是**原样显示**的）→ 清掉并加测试守住。
  该测试后来**从 1 种渲染态扩到 3 种**（收起 / 展开 / 展开且带数据），一扩又抓到两处漏网。
- **README 里「客户端排障」整节重复了两遍** —— 复查时发现，删掉残缺的那份、保留完整版。

#### 被实测推翻的结论

写代码与文档时想当然的判断，逐条用真数据推翻。**这些是本版最有价值的部分**：

| # | 原来的说法 | 实测事实 |
|---|---|---|
| 1 | `.gia` 文件名里的数字 = 游戏进程 pid | ❌ **不是**。`YuanShen.exe` 从 12:39 一直开着（pid **1088**），而文件里的号从 **135 逐局涨到 151** —— 它是**每次游戏启动**的会话序号 |
| 2 | 每次试玩 = 一个新 `.gia` 文件 | ❌ **也是错的**。一个 `.gia` 里可以有**多次试玩**：实测 `17-01-26_149` 里出现两遍同一份探针输出（`instance` 不同：17:01:21 / 17:01:58） |
| 3 | 「游戏进程在跑」≈ 在试玩 | ❌ **不能用**。同一个游戏进程跨了当天 13:40~18:44 的**全部**试玩 |
| 4 | `.gia` 里可能有框架级的「开始/结束」标记 | ⚠️ **默认没有**（只有脚本自己 `print` 的内容），但这条**取决于编辑器日志面板的配置** —— 不是平台的硬边界 |
| 5 | `Enum.KeyboardKeyCode` 里有物理键名 | ❌ **没有**。它只有语义键（`JumpKey` / `MoveLeftKey`…共 59 项），所以 `Enum.KeyboardKeyCode.W` **恒为 nil 且不报错**。绑键只能认 `Enum.KeyEventType` |
| 6 | 能从 `.gil` 里直接读出「哪些号能被创建」 | ⚠️ 只能读**候选**（无父节点 + 名字是控件类型）。实测 `1073741867`(文本框) / `1073741868`(图片) 真能创建，而 `1073741863~1866`（画布实例）一律 `nil` |
| 7 | 官方 API 文档里有「479 个函数 / 420 个枚举」 | ❌ **那个数字是错的**（把枚举成员当函数一起数了）。校准后是 **153 个接口 + 27 张枚举表 / 394 个成员**。教训：**报数之前先用正则量一遍** |

> 第 7 条是我在对话里先报错的，后来靠「先校准再报」纠正 —— 已写进工作区 `AGENTS.md` 的教训清单。
> 另外还有一条**抽取工具本身**的坑：官方原文把枚举成员写成「表名直接拼成员名」
> （`Enum.EaseTypeLinear`，不是 `Enum.EaseType.Linear`），
> 且参数里会**嵌套括号**（`onComplete: fun()`）—— 用 `[^)]*` 会整批漏掉 `Tween:SetOnComplete` 这类条目。

---

### 安全

活文件是**唯一副本**：它只活在米哈游的本地存档目录里，没有 git、没有撤销。
按这个前提把「能覆盖 / 能损坏它」的路径逐条审了一遍，**审出 2 个致命 + 4 个高危**并全部修掉：

| # | 级别 | 问题（都实证过） | 修法 |
|---|---|---|---|
| 1 | 🔴 | `deploy` 里**备份失败后没有 return，照样覆盖** —— 磁盘满 / 无权限时会把唯一副本直接抹掉 | **备份失败立即中止**，活文件零改动，并给出「去检查 `_backup` 是否可写 / 磁盘是否已满」 |
| 2 | 🔴 | 写文件**非原子**（`copyFileSync` 直写目标）—— 断电 / 崩溃会留下半截损坏的文件（备份同样） | 新增 `atomicWriteFile`：同目录临时文件 → **fsync** → **rename 原子替换**（实测 `renameSync` 在 Windows 上可覆盖） |
| 3 | 🟠 | 备份 mtime 沿用源文件 → 「最新备份」会选错 | 见「修复 · 备份与还原」 |
| 4 | 🟠 | 备份名时间戳用 UTC | 改本地时间 |
| 5 | 🟠 | `miliastra_probe op=deploy` 把 `_探针_*.lua` 写进**活文件目录**，而「当前文件」按 mtime 取最新 → **探针成了当前文件**，后续不带 `file` 的操作全打错目标 | 探针源码改写到**备份目录**（紧挨着被替换的文件）；另加名字护栏：`pickLuaFile` / `chooseLua` 都会跳过附属文件（`_探针_*` / `*_备份.lua` / `*.bak`）并报告跳过了几个 |
| 6 | 🟡 | `noBackup:true` 可随手传 → 绕过唯一安全网 | 改成**双钥匙**：必须同时传 `allowNoBackup:true`，否则拒绝 |
| 7 | 🟡 | 没有固定名备份，还原得自己翻时间戳挑 | 固定名 `<原名>.bak` + `op=restore` **不传 backup 就用它** |
| 8 | 🟡 | 还原失败只报一句 error | 失败给 `nextSteps`；**校验不过自动回滚**到覆盖前那一版并明确告知；部署同理 |
| 9 | 🟡 | 自覆盖（`source == dest`）没拦 | 拒绝并说明 |

**给人的提示（面板）**：备份卡片显示「位置 / 固定名备份 / 备份写在脚本旁边」、
**「还原到最新备份」一键按钮**（不用挑版本）、列表里固定名那份标 `★`、
覆盖前确认语写明「覆盖谁 + 会先备份 + 校验不过会自动回滚」、失败时把 `nextSteps` 一起显示。

**给 AI 的提示（工具描述）**：单列「安全约定（写活文件的地方都遵守，别绕过）」7 条；
`file` / `backup` / `noBackup` / `allowNoBackup` / `backupDir` 每个参数写清后果；
所有写操作回执 `restoreWith`（照着跑就能还原）。

---

### 文档

- **本仓库**：`README.md`（装、用、关键知识、能力边界、验证阶梯）、`CHANGELOG.md`（本文件）、`LICENSE`。
  README 顶部新增**文档索引**，说明哪些文档在仓库里、哪些在工作区、以及**为什么不进仓库**。
- **工作区 `AGENTS.md`**：本工作区的长期约定。本版新增/重写了
  §8.1 按键事件（含两条静默出错的坑）、§8.2 运行时能枚举什么、§8.3 `.gia` 真实结构（四条纠正）、
  §9 的「活文件安全约定」表与「高级诊断」两块风险说明。
- **工作区 `docs/`**：三本离线参考（API / 按键枚举 / 补间动画），全部由工具抽取、**不要手改**。
- **新增开发工具**（不随包发布）：
  `lint-probes` · `lint-all` · `inspect-gia` · `gil-field` · `gil-info` · `gia-runs` ·
  `render-probe` · `live-render-check` · `watch-playtest` · `dump-panel-text`

> `dump-panel-text` 是这一版**特意做出来的**：作者两次反馈「界面看不懂」，
> 根因都是**没人以使用者视角读过一遍**。现在改文案前先把它打出来自己读。

---

### 已验证

| 层 | 命令 | 项数 |
|---|---|---|
| L1 工具层（lossless JSON / JSON Schema / 只读用例） | `node tests/smoke.mjs` | 22 |
| L1 部署与备份安全（备份失败不覆盖 / 原子写 / 固定名 / 回滚 / 双钥匙 / 自覆盖拦截） | `node tests/deploy-test.mjs` | 28 |
| L1 探针部署 / 多活文件 / 两种关卡布局 / 模板结构 | `node tests/probe-deploy-test.mjs` | 10 |
| L1 Lua 结构校验器（不误报 / 报对行号 / 15 个真文件回归） | `node tests/lualint-test.mjs` | 32 |
| L1 试玩体检判据（四种结论 / 阈值边界 / 拿不到进程也不能乱猜） | `node tests/logdiag-test.mjs` | 9 |
| L3 真实 React 渲染（窄态 / 视觉身份 / 信封剥离 / 日志格式化 / 自动取回判据 / 备份卡片 / 读界面控件 / 试玩体检） | `node tests/client-render-test.mjs` | 28 |
| L1 技能契约自检（`dsh-plugin-dev` 的 `selftest.mjs`） | — | 16 |
| **L4 真机**（对着运行中的 `dsh web`） | `node tests/live-check.mjs` | 9 |

**合计 154 项**（含自检 16 项）。契约取证自本机 **`dsh 0.1.5-rc.1`**。

两格比数字更值钱的证据：

1. **`clientHalf` 断言**（L4 内）—— 查宿主 `dsh-client-modules` 的 boot 图，
   机器证明「面板 bundle 已进 `window.__DSH_BOOT__`」，并给出 `rev`（bundle 内容哈希）。
   **这一格以前只能靠人眼看面板出没出来。**
2. **枚举数量双向对上** —— 运行时探针数出 `KeyEventType` 164 / `KeyboardKeyCode` 59 / `ControllerKeyCode` 25，
   与**离线文档抽取**逐项一致。文档与真机互相印证，而不是各自声称。

**真机跑过的东西**（`observed`）：`api-surface` 探针（部署 → 试玩 → collect → 还原全程走通）、
`双相_v8_正式服` 脚本、`.gil` 静态读取、备份与还原。

**还没真机跑过的**（`pending`，别当已验）：
`api-check` 探针（只过了结构校验）、文档抽出来的 `Tween` / `TweenSequence` / `EaseType` 那套动画 API、
`game.IsTestPlay()` 的返回值、以及 **`.gia` 到底在试玩开始还是结束时落盘**（体检里如实标了「尚未完全实测」）。

---

### 已知边界

- **编辑器 UI 操作没有自动化通道**：在「客户端控件模板」里点【添加客户端控件】、给容器节点挂脚本、
  建容器节点 —— 必须由人在编辑器里完成。本插件替代的是「人和 AI 之间的来回搬运」，不是编辑器操作本身。
- **不做场景写操作**：工具只读，或只写「活文件」（脚本本身），不碰地图数据。
- **「是否在试玩」没有权威信号**：只能用「`.gia` 还在不在变大」代理，滞后数秒。
  `YuanShen.exe` 在跑**不能**当判据（见「被实测推翻的结论」第 3 条）。
- **负载 / 节点图统计尚未实现**：官方文档确认「负载计算」是编辑器 UI 功能（静态菜单 + 动态试玩报告）；
  「节点图」在 `.gil` 里只找到**复合节点名注册**（`#10`），逻辑本身不在存档里。
- **`.gil` 里 30+ 个顶层字段仍未解读**（本版只读了 `#1 #2 #7 #9 #11 #29 #39 #43 #50`）。
- **`patchReload: live` 不会重新 import Host 模块**（实测：改 `cordis.patch.yml` 触发重载后
  `/miliastra/status` 仍返回旧字段）—— 所以 **Host 改动一定要重启 `dsh web`**，
  只有 `lib/client.js` 刷新页面即可（`rev` 会变，可机器核对）。
