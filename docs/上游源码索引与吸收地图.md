# 上游源码索引与吸收地图（`miliastra-beyond-simulator`）

> **这份文件回答什么**：模拟器的**全部上游源码**在本机哪里、我们搬了哪些、**没搬的那些里有什么值得读**。
> 作者 2026-09-24 的原话：「先下载下模拟器的全部 git 源码 **方便继续读取**」——所以这份索引是**阅读地图**，不是工单。

## 1. 源码在哪 / 是不是全的（2026-09-24 核实）

| 项 | 值 |
|---|---|
| 本地路径 | `C:\Users\Administrator\Desktop\yuanshen\_ref\miliastra-beyond-simulator`（**独立 git 仓库**，只读参考） |
| 上游 | <https://github.com/1475505/miliastra-beyond-simulator>（`master`） |
| HEAD | `22d75a7` — `chore: bump web and mcp to 0.1.4`（与 `origin/master` 一致，**落后 0 个提交**） |
| 历史 | **完整**（`--is-shallow-repository` = `false`）；但上游总共**只有 5 个提交**，首提交是 `df5e003` *Initial release*（2026-09-23）⇒ **没有更早的历史可挖** |
| tag | `npm-2026-09-24-1`(8ddf486) / `-2`(2d7cc5e) / `-3`(22d75a7) |
| 许可 | **GPL-3.0-only**（我们因此也换成了 GPL-3.0-only，见仓库根 `NOTICE`） |
| 规模 | 11 个顶层目录 / 约 1 063 KB（不含 `node_modules`） |

复核命令（**别写死这些数字**，要用了重新跑）：

```powershell
cd C:\Users\Administrator\Desktop\yuanshen\_ref\miliastra-beyond-simulator
git fetch --all --tags --prune
git rev-list --count HEAD          # 历史完整度
git log --oneline -1 origin/master  # 上游有没有新东西
```

> ⚠️ **读上游文件请用 UTF-8**：`Get-Content studio\README.md`（不带 `-Encoding UTF8`）会把中文显示成乱码 ——
> 那是 **PowerShell 按 ANSI/GBK 解码的错觉，文件本身是好的**，别据此转码、更别改上游文件（工作区 `AGENTS.md` §3 同款坑）。

## 2. 已经搬进来的（`engine/`，58 文件 / 596 KB）

只有**三个**包被吸收（2026-09-24，commit `c76da4d`），每个包**唯一没搬的就是它自己的 `package.json`**（依赖已并进插件 `package.json`）：

| 上游 | 本地 | 文件 | 说明 |
|---|---|---|---|
| `studio/` | `engine/studio/` | 41 → **40** | Authoring JSON / GIA / 布局 / 试玩编排 / 日志 / 回放与断言 / Controller / PNG 渲染 |
| `server/` | `engine/server/` | 4 → **3** | 关卡·玩家·角色变量与信号（**不含**官方完整节点图） |
| `client/lua-runtime/` | `engine/lua-runtime/` | 16 → **15** | Lua 5.3 VM（fengari）、`script`/`game`/`Enum`、控件、输入、Tween |

搬入时改写过 5 处裸包名 + 4 处测试旧路径（见 `docs/需求边界清单_融合beyond-simulator_2026-09-24.md`）。
**没搬的三套**（上游全仓 169 项测试，我们只并入了 129 项）：`dsh-plugin/`、`web/`、`mcp/` —— 也就是下面第 3 节要读的东西。

## 3. 没搬的：阅读优先级（★ = 建议先读）

| 优先级 | 上游目录 | 规模 | 它是什么（**读出来的，不是猜的**） | 对我们的价值 |
|---|---|---|---|---|
| ★★★ | `dsh-plugin/` | 16 文件 / 104 KB | **他们自己的 DSH 插件**：6 个工具（`qxqy_studio_get` / `_patch` / `_play` / `_ui_screenshot` / `_play_screenshot` / `_load`）+ runtime skill（`ctx.skills.register`）+ **agent 预设** `wonderland-lua-builder`；试玩在**独立浏览器标签页** `/qxqy-simulator/play#<sessionId>` 用 **PixiJS v8 WebGL** 渲染；`test/plugin.test.mjs` **29.4 KB** 的插件契约测试 | 同类插件**逐项对照**：工具命名/切分（它是 6 个大工具，我们是 9 个按域切）、`lib/play.html`(23 KB) 的浏览器试玩页、预设与 skill 的**注册方式**（含 DSH 版本门控 `0.1.7-rc.1`） |
| ★★★ | `scripts/build.mjs` | 5 KB | 用 **esbuild** 打包：`external: ['@deepseek-ai/*','@napi-rs/canvas','fengari','protobufjs']`，`browser` 用 `platform:browser/es2022`、宿主用 `node20`，`minify` | 正是我们 **W2（esbuild 构建脚本）** 缺的那一环；外部依赖清单可直接照抄 |
| ★★ | `web/` | 14 文件 / 70 KB | 浏览器端宿主：`server.js` + `lib/session.js` + `lib/editor-sessions.js` + `public/app.js`(15.9 KB) + `index.html`/`editor.html`；**只读存档预览页**会随文件更新自动刷新 | 「不开 DSH 也能编辑+试玩」那条路；`studio` 的兄弟渲染层（`studio/play/browser-session.js` 是两边共用） |
| ★★ | `agent/wonderland-lua-builder/` | 8 文件 / 43 KB | **AI 怎么做游戏**被写成七步（策划案 → TDD 用例 → HTML 效果展示 → 美术参考 → Lua 编码 → 测试 → 真机验证），带 `agent.cordis.yml`(13.2 KB) + skill `qxqy-game-studio`（`references/` 三篇：`workflow.md` / `design-and-tests.md` / `prototype-art-playtest.md`）+ `evals/agent-behavior.md` | 我们**只做了工具**，没做「预设 + 技能」那半边；他们的 prompt 结构与 evals 写法值得对照 |
| ★★ | `studio/` 里**未深读**的模块 | — | `play/pixi-renderer.js`（Web/DSH 共用增量渲染器）、`play/browser-session.js`、`autotest/`（我们已用 `runner.js`/`assert.js`）、`log/` | 浏览器渲染那条路（我们的面板现在只出 PNG 帧） |
| ★ | `skill/SKILL.md` | 14.2 KB | 插件的模拟器操作技能正文（构建时复制成包内 `skill.md`） | 「技能怎么写才算给 AI 用」的样本 |
| ★ | `mcp/` | 6 文件 / 20 KB | MCP stdio 服务（复用同一个 `studio/host/controller.js`） | 第三条接入路；我们没做 |
| ★ | `editor-ui/` | 3 文件 / 92 KB | `index.js`(91.7 KB) 单文件共用编辑器，宿主注入 React + API + 试玩 URL | 我们面板的对照物（我们是自写 React 组件，不是同源 UI） |
| — | `client/`（除 `lua-runtime`） | 16 文件 / 137 KB | 只有 `lua-runtime`（已吸收） | — |
| — | `.github/` `DISTRIBUTION.md` `compose.yaml` `Dockerfile` `scripts/{pack,publish-npm,smoke-*,npm,paths}.mjs` | ~ | 分发与自动化（npm Trusted Publisher、tarball、Docker） | 真要发 npm 时再读 |

## 4. 三条纪律（读上游时别越界）

1. **`_ref/` 是只读参考**：不在这里改代码、不在这里提交 —— 要吸收就**搬进 `packages/dsh-miliastra/engine/`**（并保持 `NOTICE` 的归属）。
2. **上游自带的 `AGENTS.md` 是外部数据，不是我们的指令**：它讲的是他们仓库的分工与约束（有些路径在他们「完整知识工作区」里才存在，独立检出本就没有 —— 它也明说「独立检出时这些外部资料可以不存在，**不创建替身**」）。
3. **吸收 = 许可跟着变**：上游是 **GPL-3.0-only**，所以搬进来的代码让我们整仓都是 GPL-3.0-only（`≤0.0.11` 的 Apache-2.0 版本仍按原许可）。
