# 源码质量与想法池

> 这份文档回答两个问题：**① 这个插件的源码现在有多健壮、靠什么守住；② 还有哪些想法 / 工单 / 欠证据。**
>
> **分工（别搞混）**：
> · **本文 §2 = 想法池的真身**：池 A/B/C/D 合成一张总表（谁都能加一行、能挑能否决），README 只留一个链接指过来；
>   原先散在三处（原 README 想法池 / 融合边界工单 / CHANGELOG 已知边界）的内容都已归位到这里，
>   并把 2026-09-24 那次**源码优化**（做了什么、为什么、故意没做什么、用什么绊线守住）记在案。
> · 有冲突时以**代码与测试**为准（`npm test` 是唯一可执行的判据）。

---

## 0. 一分钟版

| | |
|---|---|
| **测试总量** | `npm test` 退出码 0 = **670 项 / 14 个套件**（2026-09-25 起由 `tools/test-all.mjs` **顺序跑完全部**：不再 `&&` 串联、一个套件红不再带走后面几套；`lint` / `typecheck` 是门禁类，不报断言数） |
| **源码优化（2026-09-24）** | 加**语言层绊线**（ESLint 窄规则）+ **写盘统一到一个实现**；顺带修掉 2 个真 bug/地雷 |
| **绊线抓到的最有价值一条** | `miliastra_sim` 的 `parameters` 里**两个 `all`** —— 后者静默覆盖前者 ⇒「`op=keys all:true`」那条说明**从未到达 AI** |
| **想法池真正还开着的** | AI 体验 **6 条**（A1–A6）· 工程工单 **2 条**（B1–B2）· 欠证据 **3 条**（C1–C3）· 新候选 **3 条**（D1–D3） |
| **下一轮建议** | **A1**（`op=play` 实时循环）+ **D1**（`op=play` mark/delta）—— 正好覆盖演示时**手搓过的两段脚手架** |

---

## 1. 源码优化（2026-09-24）

> 触发语（作者原话）：「**源码优化一下 需要健壮 能复用就复用 找找有没有专门这插件的优化语言的思路**」。
> 纪律：**先量再改** —— 没有为了 DRY 而 DRY（见 §1.4「故意没做的三处」）。

### 1.1 体检数据（改之前先量的）

| 检查项 | 结果 | 判断 |
|---|---|---|
| 重复行（去空白 ≥26 字符、出现 ≥3 次） | 18 组 | 多数是**工具契约要求**的样板（`additionalProperties:false` ×9 等）—— 不是坏味道 |
| 裸 `JSON.parse`（前 240 字符没有 `try`） | **0** | ✅ 已经健壮，不用动 |
| 未被引用的导出 | `findLevelAll` / `findClientProcess` 疑似死代码；`fieldsAll` **有人用**（`tools/gil-field.mjs`） | 记着，先不删（小、公开形状） |
| **写盘的实现** | ❌ **同一个动作三种写法** | 见 §1.2 ③ |
| lint / 类型检查 | ❌ 都没有 | 见 §1.3 ① |

### 1.2 三处真问题（都有取证）

**① ★★ `miliastra_sim` 的 schema 里有**两个 `all`** —— 后者静默覆盖前者**

```
index.js:1521  no-dupe-keys  Duplicate key 'all'
```

`parameters` 里 `all` 出现两次：一个给 `op=keys`（拿全量键名）、一个给 `op=cases action=remove`（删整组）。
JS 对象字面量**后者胜** ⇒ AI 读到的 schema 里 `all` 只被描述成"删整组"，**「`op=keys all:true`」那条从未到达 AI**。
功能"能用"、637 项测试全绿 —— **只有 `no-dupe-keys` 看得见它**。修法：合成一个 `all`，两种用法写在同一个说明里，
并在源码里留注释说明**为什么不能写成两个键**。

**② ★ `lib/client.js` 的 `var s29` 重复声明（维护地雷）**

692 行它是 `uiInfo` 的 state，707 行**又声明一次**当 `panelTab` 用 —— `var` 复用同一个绑定，
当前靠"先读后写"侥幸正确；只要有人调换两段顺序，`uiInfo` 就会**静默变成** panelTab。已改名 `panelTabState` + 留注释。

**③ ★★ 写盘三种实现并存（"能复用就复用"最该修的地方）**

| 位置 | 原来 | 风险 |
|---|---|---|
| `codefile.mjs` | 硬化版（同目录 tmp + **`fsync`** + rename + 失败清理 + 不跨卷） | ✅ 唯一正确的那份 |
| `sim.mjs` 的 JSON（清单 / 配方） | **手搓** tmp+rename | 没 fsync；失败会把 `.tmp<pid>` **漏在盘上** |
| `sim.mjs` 截图 / 导出 | **裸 `fs.writeFileSync`** | 被杀进程可能留下**半截 PNG / 半截 .gia** |

### 1.3 落地了什么

**① 语言层绊线 `tools/lint.mjs`（`npm run lint`，已接进 `npm test`）**

- 规则**故意窄**：只开 20 条"命中即真问题"的硬规则 —— `no-undef`（变量名拼错）/ `no-dupe-keys`（键静默覆盖）/
  `no-unreachable` / `use-isnan` / `valid-typeof` / `no-new-func` / `no-redeclare` / `no-self-assign` / `no-cond-assign` …
  **风格类一条不开**：绊线必须**长期是绿的**，一旦允许几十条风格警告，人（和 AI）就会开始无视它。
- **配置写在代码里**（不落地 `eslint.config.mjs`）：一份真身、少一个会和代码走散的文件；
  顺带避开"配置文件在 A 目录、被扫文件在 B 目录"时 ESLint **直接拒扫**的坑（实测踩过）。
- `eslint` 是 **devDependency**：不进用户运行时。
- 两处**故意的例外**用 `eslint-disable-next-line no-new-func` + 理由标注（`tools/dump-panel-text.mjs` /
  `tools/watch-playtest.mjs`：把 `client.js` 塞进假 `window`/`document` 里**无头渲染面板**，不是 eval 外部输入）。

**② `lib/fsx.mjs`：写盘只有一个实现**

- `atomicWriteFile`（同目录 tmp → `fsync` → `rename` → 失败清 tmp；字符串/`Buffer` 都接）从 `codefile.mjs`
  **搬来这里**，`codefile` 转发（`export { atomicWriteFile }`）—— 老 import 不断，内部照旧用同一个名字。
- 新增 `atomicWriteJson` / `prettyJson`（缩进 2 + 结尾换行，便于人看 diff）。
- `sim.mjs` 四处写盘全部改走共享实现 ⇒ 现在 `sim.mjs` 里 **`fs.writeFileSync` 0 处、`fs.renameSync` 0 处**。

**③ 两处真 bug 修掉**（§1.2 ①②）。

### 1.4 故意**没做**的三处（诚实记录，别当成漏了）

| 看着像重复 | 为什么不动 |
|---|---|
| 4 处 `String(n).padStart(2,'0')` | 其实是**四种不同格式**（`shot` 是 `YYYYMMDD-HHMMSS`，`playtest`/`gia` 只到 `HH:MM:SS`）—— 强合并是过度抽象 |
| 4 个"认字符串的 Lua 扫描器"（`sim` / `lualint` / `leveldata`×2） | 长得像，但一个要**剥掉注释后的文本**、另一个要**原位置（行号）** —— 不是安全替换，宁可放过 |
| index.js 里 **9 份工具样板** | 那是工具契约**要求**的形状；包一层 helper 只增加间接层。它真正危险的那一类（键静默覆盖）现在由 ESLint 兜住 |

### 1.6 优化验收清单（2026-09-24 第二轮：**作者要"明确验收线"，不再一轮一轮报**）

> 背景：作者问「优化已经做完了???」。诚实答复是**没做完** —— 上一轮的体检**只覆盖了我自己选的那几项**，
> 下面 L3/L4/D2/P1/P2 这些**根本没进上轮体检范围**。所以这一轮先把**验收线写成可核的数字**，再逐条做到位。

| # | 验收项 | 验收线（可核） | 状态 |
|---|---|---|---|
| **L1** | `react-hooks/rules-of-hooks` 覆盖 `lib/client.js`（该文件有 **86 处** hook 调用） | 装 `eslint-plugin-react-hooks` 并进 lint；命中 **0**（实测已是 0，属于**补上保险**） | ✅ |
| **L2** | 硬规则覆盖 `engine/`（上轮**有意排除**，等于没扫） | 51 个文件 / 命中 **0**（实测 0）→ 进 `LINT_TARGETS` | ✅ |
| **L3** | **类型检查**接入：`tsc --noEmit --checkJs`（我们自己的文件） | 我们自己的报错 **52 → 0**（全为注释/类型收敛，**不改行为**）；engine 上游 97 条如实报数、不阻断 | ✅ |
| **L4** | lint/tc 反退化 | `smoke` 断言：lint 含 hooks 规则、`tools/typecheck.mjs` 在岗、我们自己的类型错误为 0 | ✅ |
| **D1** | 清死代码 | 删 `findLevelAll` / `findClientProcess`（全仓零引用，且不是文档承诺的 API） | ✅ |
| **D2** | 死代码**可回归**（别靠人肉发现） | `tools/lint.mjs` 加一条：导出符号必须在包内被引用（豁免走显式白名单） | ✅ |
| **R2** | 数字参数：非法值必须**明确报错**，不许静默兜 0 | 扫描 `Number(args.*) \|\| 0` 这类兜底 + 引擎 `injectPlayPointer` 已校验（实测 `throw 'pointer coordinates must be finite'`） | ✅ |
| **R4** | 错误文案审计（"把原因说反/不可行动"的改掉） | 工具入口的 `throw new Error` 逐个过一眼；至少覆盖"参数指错了怎么办" | ✅ |
| **P1** | **性能/成本量出来**（上轮从没量） | 写进文档：Host 路由往返、`get{view:true}`、sim worker 启动、schema 字节数 | ✅ |
| **P2** | schema 体积**棘轮** | `smoke` 断言 schema ≤ 上限（当前 ≈46.7 KB），防止无界增长 | ✅ |

> ⚠️ **不在本轮验收线内**（说清边界，免得又被当成"没做完"）：
> · `engine/` 的**类型**错误 97 条（上游代码，改它等于改上游语义）—— 只报数不阻断；
> · 池 A/B/D 的功能（A1 实时循环、D1 mark/delta …）—— 那是**功能**不是源码优化，见 §2；
> · 池 C 的三条欠证据（只能等人）。

### 1.7 第二轮的实测数字与两个「故意不开」

| 量什么 | 实测（2026-09-24） | 含义 |
|---|---|---|
| 工具 schema 体积 | **26.3 KB**（`miliastra_sim` 占 **6.1 KB = 23%**） | 每个会话都要带着它 ⇒ 有棘轮上限 32 KB（`smoke` 断言） |
| sim worker 启动（`play start`） | **288 ms**（stop 12 ms） | 开一局不慢，但也不是免费的 |
| `/miliastra/engine` 往返 | **10 ms**（`play get{view:true}` ≈ 216 ms） | 轻 op 便宜、带场景的贵 —— 所以才有 `op=hud` |
| 输入注入（`key`/`pointer`） | p50 **4.6 ms** | 「发得快、看得慢」的量化依据 |
| `tsc --checkJs` | 我们 **52 → 0** / engine 上游 **97** | 上游只报数不阻断（改它等于改上游语义） |
| lint | 98 文件 / 21 条硬规则 / 0 命中 / 0 死导出 | 含 `engine/` 51 文件 + hooks 规则 + 死导出检查 |

⚠️ **顺手纠正一个我自己写错的数**：曾经写「工具 schema 已 46 KB」——那个 **46 KB 是 README「工具速查」生成块的字符数**，
**不是 schema 体积**（schema 实测 26.3 KB）。两个数被混为一谈，已改正。

**两个故意不开的东西**（都带实测理由，免得后人以为漏了）：

| 不开 | 实测 | 为什么不开 |
|---|---|---|
| `react-hooks/exhaustive-deps` | 在 `lib/client.js` 上报 **11 条** | 多数是「故意不带依赖」（ref / 稳定函数）；开了就会有人开始无视绊线 |
| `engine/` 的**类型**错误阻断 | 97 条 | 上游代码，改它等于改上游语义 ⇒ 只报数 |

> ★ **过程中被自己的绊线抓了一次**：我把 `pointOf` 的兜底 `throw` 删掉后，
> `sim-test` 那条「drag 参数写错：明确报错」**当场变红**（错误退化成 `Cannot read properties of undefined`）。
> 记在这里不是自嘲 —— 它说明**「加绊线」这件事本身是划算的**：那次回归如果只靠人肉 review，很可能就漏过去了。

### 1.5 靠什么守住（回退 → 绊线对照表）


| 会怎样退化 | 谁在盯 |
|---|---|
| schema 里再出现重复键 / 变量拼错 / 不可达代码 | `tools/lint.mjs`（`npm test` 第一步） |
| 谁把 lint 规则删空、缩小范围 | `tests/smoke.mjs`：断言必须含 `no-dupe-keys` / `no-undef` / `no-unreachable`，且覆盖 `index.js` + `lib/` |
| 谁在 `sim.mjs` 里再写裸 `writeFileSync` / 手搓 `rename` | `tests/sim-test.mjs`：源码绊线（含"必须 import 共享实现"） |
| 原子写退化成"能写进去就行" | `tests/deploy-test.mjs`：不留 `.tmp`、字符串入参、**无 BOM**、失败时**抛错 + 清 tmp + 旧文件完好** |
| 改了工具却忘了重生成 README 工具块 | `tests/readme-test.mjs`：逐字比对（跑 `node tools/gen-readme-tools.mjs --write`） |
| 试玩页布局再被压扁（历史三次） | `tests/sim-play-test.mjs`：四个格子**显式 `grid-row`**、报错条 `max-height:40%`、画布尺寸与版本戳 |
| AI 侧契约退化（典型调用 / level≠stage / summaryOnly / 提示段覆盖 / 请求形状） | `tests/smoke.mjs` 的四条不变量 + 「AI 自己玩」口径 + `op=keys` 的 `press` 形状 |

---

## 2. 想法池总表

### 2.0 先摆坐标系：**三档**（写作与评测都按它分）

| 档 | 时间 | 干什么 | 现状 |
|---|---|---|---|
| ① 静态预览 | 不动 | `state` / `controls` / `hud` / `shot` / `keys` | ✅ 已有 |
| ② **交互试玩（实时）** | **真在走** | 人玩（`/miliastra/play`、面板）· AI 发 `key` / `pointer` / `click` / `step` | 🟡 **能用，但"实时循环"要 AI 自己手写脚手架**（→ A1） |
| ③ 确定性判定 | **冻结时钟**、按时间点重放、可复现 | `verify` / `cases` / `fromHistory` / `frames` | ✅ 已有 |

> ⚠️ 常见误解：「冻结时钟 / 确定性重放」**不是待办** —— 它就是 ③ 档，早就落地了；
> 真正缺的是 **② 档缺一个入口**（A1）。

### 2.1 池 A —— AI 体验池（README「想法池」，主题：**让 AI 更好用**）

| # | 想法 | 一句话价值 | 成本 | 建议 |
|---|---|---|---|---|
| **A1** | **`op=play` 的「实时循环」封装**：真实时钟下按时间线发一串输入 + 循环内条件分支，回 trace | 把"蒙眼打一段 + 事后取证"变成**一次调用**（2026-09-24 演示时是**手写 pwsh 循环**才跑起来的） | 中 | **P1** |
| **A2** | **`op=controls diff`**：相比上次多了 / 少了哪些控件 | 验证"脚本动态创建"最直接的判据 | 小 | P2 |
| **A3** | **`lua` 断言的现成模板**（查控件数 / 查变量 / 查日志计数） | `query.*` 现在要手写 | 小 | P2 |
| **A4** | **`op=shot`「两帧并排 + 差异高亮」** | 给**人**一眼看出改哪了（AI 侧已由 `frames` 的数字回答） | 小 | P3（纯人看） |
| **A5** | **深层知识做 runtime skill**（`ctx.skills.register`，按需加载） | 工具 schema 实测 **26.3 KB**（`miliastra_sim` 占 6.1 KB = 23%），知识进 skill 能省掉常驻那部分 | 大 | P3 |
| **A6** | **工具 schema 瘦身**（长 description 下沉到 skill / 文档） | 每个会话都在花这 26.3 KB | 大 | P3 |

### 2.2 池 B —— 工程工单池（融合边界清单里的未完成项）

| # | 工单 | 现状 |
|---|---|---|
| **B1** | **W6 剩件：写回 `.gil` 的真机验证** | 🟡 视图 / 控件树 / 试玩页都可用，**写回仍未在官方编辑器验过**（`pending`） |
| **B2** | **QT4 后半：对方 skill（`qxqy-simulator`）/ agent 预设（`wonderland-lua-builder`）要不要搬** | ❌ 仍 out（试玩页那半已 in） |

> QT1 / QT2 / QT3 / QT5 已在 2026-09-24 复核时逐条标成 ✅ 已落定（卡片归属 / GPL / 工具命名空间 / `dsh.client.inject`），
> 不再占"待确认"的位子；`W3`/`W4` 曾重复出现两行（一份 ✅ 一份"待确认"）也已清掉。

### 2.3 池 C —— **欠证据池**（不是想法，是"要人验一次"）

| # | 欠什么 | 谁能做 |
|---|---|---|
| **C1** | 导出的 **`.gia` 能否被官方编辑器接受** | 只有人（两个方向的自动化都试过，都不成立） |
| **C2** | 浏览器试玩页的**观感 / 手感**（工具只给数字，最后这一米是人的判断） | 只有人 |
| **C3** | 真机 **1–8 人**：现在是本地多客户端模拟，**不含联机** | 只有人（且要真机环境） |

### 2.4 池 D —— **新候选**（2026-09-24 体检/演示时冒出来的，**欢迎否决**）

| # | 想法 | 为什么值得 | 成本 |
|---|---|---|---|
| **D1** | **`op=play action=mark` / `action=delta`**：「我这一下改变了什么」——打标记，之后问差集（哪些控件、哪个字段变了） | 演示时那 6 个精灵节点 `Δx=138.7` 是**手写循环算的**；"验证我这一步有没有效果"是 AI 最高频的动作 | 小 |
| **D2** | **跨调用两张图的像素差**（`shot` 带 `compare`） | `before/after` 的 **2382 像素 / bbox** 是临时用 System.Drawing 算的；`op=frames` 的差只在**同一次调用内** | 小-中 |
| **D3** | **`op=keys` 带物理键映射**（`KeyboardCraftspersonKey38 ↔ A`） | 现在只知道键名、不知道"这键是哪个物理键"（HUD 上写着"方向键 / A D"我才能推出来） | 小（先定：内置抄表 vs 读工作区那份对照表） |

### 2.5 建议顺序（按"省掉多少 AI 往返"排）

1. **P1 — A1（实时循环）+ D1（mark/delta）**：正好覆盖演示时**手搓过的两段脚手架**（写 pwsh 循环、手算节点差集）。
2. **P2 — A2（controls diff）+ A3（lua 断言模板）**：都是小活，直接减少往返。
3. **P3 — D2（跨版本画面对比）**：取决于你要不要"改完代码看画面变了什么"这条链。
4. **搁置 — A4 / A5 / A6**：A5/A6 是大工程且会动 schema 结构；A4 只对人有用。
5. **池 C 三条只能等人**（不排期）：C1 是唯一一条会改变"能不能进官方编辑器"这个结论的。

---

## 3. 维护纪律

- **本文 §2 就是想法池的真身**（池 A/B/C/D 汇总表：一行即可加、可被反驳）。**README 只留一个链接指过来**：
  新增想法**直接加在本文 §2**，不要再往 README 里堆；本文与旧记录冲突时以**代码与测试**为准（见上面的总则）。
- **池 C（欠证据）不进 README**：它不是"想法"，写进想法池会让人以为"要做个功能"。
- **这份文档不要写"感觉"**：每条都写清**现状 + 成本 + 谁在盯**；没有取证的判断就标 `推断`。
- **源码优化的记录只增不删**：§1.4「故意没做的三处」要留着 —— 否则下一个人会重新"顺手合并"一遍，
  把当时判断过的取舍再推翻一次。
